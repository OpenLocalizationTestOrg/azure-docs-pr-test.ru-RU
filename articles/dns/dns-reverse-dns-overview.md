---
title: "aaaOverview обратного DNS, в Azure | Документы Microsoft"
description: "Узнайте, как работает обратная зона DNS и как ее можно использовать в Azure."
services: dns
documentationcenter: na
author: jtuliani
manager: timlt
ms.service: dns
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: infrastructure-services
ms.date: 05/29/2017
ms.author: jonatul
ms.openlocfilehash: 687663fb83469ab8e696bb714649d0856915bad6
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="overview-of-reverse-dns-and-support-in-azure"></a>Основные сведения об обратной зоне DNS и ее поддержке в Azure

В данной статье приводится обзор как обратного DNS работает, а hello обратного DNS сценарии, поддерживаемые в Azure.

## <a name="what-is-reverse-dns"></a>Что такое обратная зона DNS

Обычной записи DNS включить сопоставление между именем (например, «www.contoso.com») tooan IP-адрес DNS (например, 64.4.6.100).  Обратного DNS позволяет hello перевод IP-адрес (64.4.6.100) назад tooa имя («www.contoso.com»).

Записи обратной зоны DNS используются в разных ситуациях. Например обратной записи DNS широко используются в борьбу с ним Нежелательная почта, проверяя hello отправителя сообщения электронной почты.  Hello принимающей почтового сервера извлекает hello обратной записи DNS hello отправки его IP-адресу и проверяет, если удаленный доступ, является авторизованным toosend электронной почты из hello исходящие домена. 

## <a name="how-reverse-dns-works"></a>Принцип работы обратной зоны DNS

Записи обратной зоны DNS размещены в специальных зонах DNS, известных как зоны ARPA.  Эти зоны образуют иерархию отдельные DNS параллельно с обычной иерархии hello размещение доменов, например «contoso.com».

Например DNS-запись «www.contoso.com» hello реализуется с помощью «A» DNS-запись с именем hello «www» hello зоны «contoso.com».  Эта запись A указывает toohello соответствующие IP-адреса, в данном случае 64.4.6.100.  обратный просмотр Hello реализуется отдельно, с помощью «PTR» запись с именем "100" hello зоны «6.4.64.in-addr.arpa» (Обратите внимание, что IP-адреса меняется в зоны ARPA.)  Этой записи PTR, если заданы верно, указывает имя toohello «www.contoso.com».

Когда организация назначается блок IP-адресов, они также получить соответствующей зоны ARPA hello правой toomanage hello. Здравствуйте зоны ARPA соответствующий IP-адрес toohello блоки, используемые в Azure размещенных и управляемых корпорацией Майкрософт. Поставщик услуг Интернета могут быть вложены зоны ARPA hello для IP-адреса для вас или может разрешить узла tooyou зоны ARPA hello в службе DNS по вашему выбору, такие как Azure DNS.

> [!NOTE]
> Прямые и обратные запросы DNS реализуются в отдельной параллельной иерархии DNS. — Hello обратного «www.contoso.com» **не** размещена в зоне hello «contoso.com», вместо этого она расположена в зоны ARPA hello для hello соответствующий блок IP-адресов. Для блоков адресов IPv4 и IPv6 используются отдельные зоны.

### <a name="ipv4"></a>IPv4

Hello имя зоны обратного просмотра IPv4 должно быть в кодировке hello: `<IPv4 network prefix in reverse order>.in-addr.arpa`.

Например при создании записей toohost зону обратного просмотра для узлов с IP-адресами, которые находятся в префикс 192.0.2.0/24 hello, имя зоны hello будут создаваться изолируя hello сетевой префикс адреса hello (192.0.2) и затем обращения порядка hello (2.0.192) и добавление hello суффикс `.in-addr.arpa`.

|Класс подсети|Сетевой префикс  |Обратный сетевой префикс  |Стандартный суффикс  |Имя обратной зоны |
|-------|----------------|------------|-----------------|---------------------------|
|Класс A|203.0.0.0/8     | 203        | .in-addr.arpa   | `203.in-addr.arpa`        |
|Класс B|198.51.0.0/16   | 51.198     | .in-addr.arpa   | `51.198.in-addr.arpa`     |
|Класс C|192.0.2.0/24    | 2.0.192    | .in-addr.arpa   | `2.0.192.in-addr.arpa`    |

### <a name="classless-ipv4-delegation"></a>Бесклассовое делегирование IPv4

В некоторых случаях hello диапазон IP-адресов, выделенных организации tooan меньше, чем класса C (/ 24) диапазона. В этом случае диапазон IP-адресов hello не входят в границы зоны в hello `.in-addr.arpa` зон иерархии и таким образом, не может быть делегирована как дочерней зоны.

Вместо этого используется другой механизм управления tootransfer отдельных обратного просмотра (PTR) записывает tooa выделенного DNS-зоны. Этот механизм делегирует дочерней зоны для каждого диапазона IP, а затем maps каждого IP-адреса в hello по отдельности диапазон toothat дочерней зоны с помощью записи CNAME.

Например предположим, что организации предоставляется 192.0.2.128/26 диапазона IP hello путем его поставщика услуг Интернета. Представляет 64 IP-адресов из 192.0.2.128 too192.0.2.191. Обратная зона DNS для этого диапазона реализуются следующим образом:
- Организация Hello создает вызывается 128-26.2.0.192.in-addr.arpa зоны обратного просмотра. префикс Hello, "128-26' представляет hello сети организации toohello сегмент, назначенный в рамках hello класса C (/ 24) диапазона.
- Hello ISP создает tooset записей NS копирование hello делегирование DNS для hello выше зоны из зоны родительского класса C hello. Он также создает записи CNAME в зоне обратного просмотра hello родительского (класс C), сопоставление каждого IP-адреса в hello IP диапазон toohello новую зону, созданную организацией hello:

```
$ORIGIN 2.0.192.in-addr.arpa
; Delegate child zone
128-26    NS       <name server 1 for 128-26.2.0.192.in-addr.arpa>
128-26    NS       <name server 2 for 128-26.2.0.192.in-addr.arpa>
; CNAME records for each IP address
129       CNAME    129.128-26.2.0.192.in-addr.arpa
130       CNAME    130.128-26.2.0.192.in-addr.arpa
131       CNAME    131.128-26.2.0.192.in-addr.arpa
; etc
```
- Затем организация Hello управляет hello отдельных записей PTR в их дочерней зоны.

```
$ORIGIN 128-26.2.0.192.in-addr.arpa
; PTR records for each UIP address. Names match CNAME targets in parent zone
129      PTR    www.contoso.com
130      PTR    mail.contoso.com
131      PTR    partners.contoso.com
; etc
```
Обратный просмотр для hello IP адрес "192.0.2.129» запросов для PTR-запись с именем «129.2.0.192.in-addr.arpa». Этот запрос разрешается через hello CNAME в hello родительской зоны toohello PTR-запись в hello дочерней зоны.

### <a name="ipv6"></a>IPv6

Имя зоны обратного просмотра IPv6 Hello должны находиться в hello следующие формы:`<IPv6 network prefix in reverse order>.ip6.arpa`

Предположим, При создании записей toohost зону обратного просмотра для узлов с IP-адресами, которые находятся в hello 2001:db8:1000:abdc:: / 64 префикс имени зоны hello будут создаваться изолируя hello сетевой префикс адреса hello (2001:db8:abdc::). Далее разверните tooremove префикс сети hello IPv6 [нуля сжатия](https://technet.microsoft.com/library/cc781672(v=ws.10).aspx), если он был tooshorten используется префикс адреса hello IPv6 (2001:0db8:abdc:0000::). В обратном порядке hello, с помощью точки как hello разделитель между каждое шестнадцатеричное число в префиксе hello, toobuild hello обратным префикс сети (`0.0.0.0.c.d.b.a.8.b.d.0.1.0.0.2`) и добавьте суффикс hello `.ip6.arpa`.


|Сетевой префикс  |Развернутый обратный сетевой префикс |Стандартный суффикс |Имя обратной зоны  |
|---------|---------|---------|---------|
|2001:db8:abdc::/64    | 0.0.0.0.c.d.b.a.8.b.d.0.1.0.0.2        | .ip6.arpa        | `0.0.0.0.c.d.b.a.8.b.d.0.1.0.0.2.ip6.arpa`       |
|2001:db8:1000:9102::/64    | 2.0.1.9.0.0.0.1.8.b.d.0.1.0.0.2        | .ip6.arpa        | `2.0.1.9.0.0.0.1.8.b.d.0.1.0.0.2.ip6.arpa`        |


## <a name="azure-support-for-reverse-dns"></a>Поддержка обратной зоны DNS в Azure

Azure поддерживает два отдельных сценариев, связанных tooreverse DNS.

**Размещение hello обратного просмотра зоны соответствующий tooyour блок IP-адресов.**
Azure DNS может использоваться слишком[размещения зон обратного просмотра и управления ими hello PTR-записей для каждого обратного просмотра DNS](dns-reverse-dns-hosting.md), а для IPv4 и IPv6.  Здравствуйте, процесс создания зоны обратного просмотра (ARPA) hello, Настройка делегирования hello и настройка PTR записей hello таким же как и для обычных зон DNS.  Hello только различия, делегирование hello должно быть настроено через поставщика услуг Интернета вместо регистратора DNS, которое следует использовать только hello записей типа PTR.

**Настройте hello обратную запись DNS для hello IP-адрес, назначенный tooyour службы Azure.** Azure позволяет слишком[Настройка hello обратного просмотра для IP-адреса hello выделяются tooyour службы Azure](dns-reverse-dns-for-azure-services.md).  Обратного поиска настраивается в Azure как запись PTR в соответствующей зоны ARPA hello.  Эти зоны ARPA, соответствующий tooall hello диапазоны IP-адресов используется Azure, размещенными в корпорации Майкрософт

## <a name="next-steps"></a>Дальнейшие действия

Дополнительные сведения см. [в статье Википедии об обратном просмотре DNS](http://en.wikipedia.org/wiki/Reverse_DNS_lookup).
<br>
Узнайте, каким образом слишком[зоны обратного просмотра hello узла для диапазона IP, назначенной поставщика услуг Интернета в Azure DNS](dns-reverse-dns-for-azure-services.md).
<br>
Узнайте, каким образом слишком[управление обратного DNS-записи для служб Azure](dns-reverse-dns-for-azure-services.md).

