---
title: "aaaIntro tooMicrosoft Azure | Документы Microsoft"
description: "Новый tooMicrosoft Azure? Общий обзор hello Get службах с примерами как они могут использоваться."
services: " "
documentationcenter: .net
author: rboucher
manager: carolz
editor: 
ms.assetid: 6f47f711-2208-4c21-8c1d-826a54c05c29
ms.service: multiple
ms.workload: multiple
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 06/30/2015
ms.author: robb
ms.openlocfilehash: 6791506abe813e19ca7b04d78d35cb46352a9028
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="introducing-microsoft-azure"></a>Введение в Microsoft Azure
Microsoft Azure — платформа приложений корпорации Майкрософт для hello общедоступное облако.  Hello цель этой статьи — toogive вы основу для понимания основ hello Azure, даже если ничего не известно о облачных вычислений.

**Как tooread в данной статье**

Azure становится все время hello поэтому очень легко tooget перегружен.  Запустить с помощью базовых служб hello, которые первыми будут отображаться в этой статье, а затем перейдите на tooadditional служб. Это не значит, нельзя использовать только hello дополнительные службы, которые сами по себе, но базовых служб hello составляют ядро hello приложения, запущенного в Azure.

**Отправка отзывов**

Ваши отзывы очень важны для нас. В этой статье мы попытаемся дать самые необходимые сведения об Azure. Если нет, сообщите нам в разделе комментариев hello hello нижней части страницы приветствия. Предоставить некоторые сведения о том, что ожидается toosee и как tooimprove hello статьи.  

## <a name="hello-components-of-azure"></a>Hello компонентов Azure
Azure группирует службы по категориям в hello портала управления и на различные визуальные подсказки, например hello [возможности Azure Инфографикой](https://azure.microsoft.com/documentation/infographics/azure/) . Hello портал управления является то, что используется toomanage наиболее (но не все) служб в Azure.

Эта статья будет использовать **другой организации** tootalk о службах на основе аналогичные функции и toocall важные вложенных служб, которые являются частью большие.  

![Компоненты Azure](./media/fundamentals-introduction-to-azure/AzureComponentsIntroNew780.png)   
 *Рисунок. Azure предоставляет службы приложений, доступных в Интернете, выполняющиеся в центрах данных Azure.*

## <a name="management-portal"></a>Портал управления
Azure имеет веб-интерфейс вызывается hello [портала управления](http://manage.windowsazure.com) , позволяет администраторам tooaccess и администрировать большинство, но не все Azure функции.  Обычно Корпорация Майкрософт выпускает hello новой пользовательского портала в бета-версии перед снятием с учета старая версия. Hello новой версии вызывается hello [«Предварительной версии портала Azure»](https://portal.azure.com/).

Как правило, достаточно долго оба портала работают одновременно. Хотя базовые службы присутствуют в обоих порталах, не все функциональные возможности могут быть реализованы в каждом из них. Новых служб может отображаться в hello новых портала первый и старше служб и функций могут существовать только в hello старая версия.  приветственное сообщение здесь — проверку, если вы не нашли что-нибудь в более старый портал hello, hello новой версии и наоборот.

## <a name="compute"></a>Среда выполнения приложений
Одним из наиболее основные возможности hello, облачная платформа не является выполнение приложений. Каждой из моделей hello вычислений Azure имеет свой собственный tooplay роли.

Можно использовать эти технологии отдельно или объедините их по мере необходимости foundation toocreate hello вправо для вашего приложения. Выбор зависит от способа Hello на какие проблемы вы пытаетесь toosolve.

### <a name="azure-virtual-machines"></a>Виртуальные машины Azure
![Виртуальные машины Azure ROBBCSIART_TEST](./media/fundamentals-introduction-to-azure/mscsiart_VirtualMachinesIntroNew_12345.png)   
*Рисунок: Виртуальные машины Azure позволяет полный контроль над экземпляров виртуальных машин в облаке hello.*

toocreate возможность Hello виртуальной машины по запросу, из стандартных образов или любого указывается, можно ли очень полезным. Именно такой подход, который обычно называют "инфраструктура как услуга" (IaaS), и предлагает модель виртуальных машин Azure. Рис. 2 показана сочетание, как выполняется виртуальной машины (VM) и как toocreate один из виртуального жесткого диска.  

toocreate виртуальную Машину, укажите размер какие VHD toouse и hello Виртуальной машины.  Затем оплачивается hello время приветствия, Виртуальная машина запущена. Оплачивается по минутам hello и только в том случае, пока она запущена, то, что минимальный хранилища плата за хранение hello виртуальный жесткий ДИСК доступен. Azure предлагает коллекцию виртуальных жестких дисков (называемых «images»), содержащие toostart загрузочную операционную систему из акций. Сюда входят варианты Майкрософт и партнеров, например Windows Server и Linux, SQL Server, Oracle и многие другие. Ваше свободного toocreate виртуальных жестких дисков и образов, а затем отправьте их самостоятельно. Вы можете даже отправлять виртуальные жесткие диски, которые содержат только данные, а потом получать к ним доступ из расположения, в котором запущены виртуальные машины.

Везде, где источником hello виртуального жесткого диска, постоянно можно сохранять любые изменения, внесенные во время работы виртуальной Машины. Hello следующий раз при создании виртуальной Машины из виртуального жесткого диска, что получают места. Hello виртуальные жесткие диски, обратно hello виртуальные машины хранятся в Azure хранилища больших двоичных объектов, которые мы говорим о более поздней версии.  Это означает, что вы получаете tooensure избыточности, виртуальные машины не удаляются из-за сбоя toohardware и диска. Его также можно toocopy hello изменения виртуального жесткого диска, за пределами Azure, затем запустить его локально.

Приложение запускается в пределах одного или нескольких виртуальных машин, в зависимости от того, как создать его перед или решить toocreate ее с нуля теперь.

Это вычисление можно использовать tooaddress многих проблем toocloud довольно общий подход.

**Сценарии виртуальных машин**

1. **Для разработки и тестирования** -можно использовать их toocreate недорогих платформы разработки и тестирования, можно завершить работу после завершения его использования. Вы также можете создать и запустить приложения, использующие любые необходимые вам языки и библиотеки. Эти приложения можно использовать любые параметры управления данными hello, предоставляемых Azure и вы также можете toouse SQL Server или другой СУБД, работающих в одной или нескольких виртуальных машин.
2. **Перемещение приложений tooAzure (точности прогнозов и сдвига)** -«Точности прогнозов и сдвига» относится toomoving приложения намного toomove погрузчика больших объектов могут использоваться как.  «Точность» hello VHD из локального центра обработки данных «shift» его tooAzure и выполнить его там.  Обычно имеется toodo некоторые зависимости tooremove работы в других системах. Если зависимостей слишком много, можно вместо этого выбрать вариант 3.  
3. **Расширение центра данных**. Виртуальные машины Azure можно использовать как расширение локального центра данных, в котором выполняются приложения, например SharePoint. toosupport, это возможно toocreate доменов Windows в облаке hello, выполнив Active Directory в виртуальных машинах Azure. Можно использовать tootie виртуальной сети Azure (упоминается в более поздней версии) в локальной сети и сети в Azure друг с другом.

### <a name="web-apps"></a>Веб-приложения
![Веб-приложения Azure ROBBCSIART_TEST](./media/fundamentals-introduction-to-azure/mscsiart_AzureWebsitesIntroNew_12345.png)   
 *Рисунок: Azure веб-приложения выполняется приложение веб-сайта в облаке hello без необходимости toomanage hello базовом веб-сервере.*

Одной из hello наиболее распространенных операций, выполнять людей в облаке hello выполняется веб-сайтов и веб-приложений. Позволяет виртуальным машинам Azure, но она по-прежнему оставляет hello несет ответственность за администрирование один или несколько виртуальных машин и hello базовой операционной системы. Это могут выполнять веб-роли облачных служб, но их развертывание и обслуживание все еще требует административной работы.  Что делать, если необходимо просто веб-сайт там, где кто отвечает за hello административной работы для вас?

Именно это и предоставляют веб-приложения. Эта модель вычислений предлагает управляемого веб-среду с помощью портала управления Azure hello, а также API-интерфейсы. Приложение веб-сайта можно переместить в веб-приложения без изменений, или можно создать новую непосредственно в облаке hello. После запуска веб-сайт можно добавить или удалить экземпляры динамически, полагаясь на веб-приложениях Azure tooload балансировать нагрузку по запросам между ними. Приложения Azure предлагает общий параметр, где веб-сайта выполняется в виртуальной машине с другими сайтами, и стандартный параметр, который позволяет toorun сайта в собственную виртуальную Машину. стандартный параметр Hello также позволяет увеличить размер hello (вычислительной мощности) в экземплярах, при необходимости.

В целях осуществления разработок веб-приложения поддерживают платформы и языки .NET, PHP, Node.js, Java, Python, SQL Database и MySQL (от компании ClearDB — партнера корпорации Майкрософт), применяемые в работе с реляционными хранилищами. Она также имеет встроенную поддержку нескольких популярных приложений, включая WordPress, Joomla и Drupal. Задача Hello — tooprovide больших затрат, масштабируемая и широком спектре платформу для создания веб-сайтов и веб-приложений в общедоступном облаке hello.

**Сценарии веб-приложений**

Веб-приложения является предполагаемым toobe полезно для организаций, разработчиков и учреждений разработки веб. Для корпораций это легко управляемое, масштабируемое, весьма доступное решение с высоким уровнем безопасности для работы веб-сайтов. При необходимости tooset веб-сайта он наиболее toostart с веб-приложения Azure и продолжить работу службы tooCloud после требуется компонент, который недоступен. В разделе hello конец hello «Вычисления» раздела Дополнительные ссылки, которые могут помочь toochoose между параметрами hello.

### <a name="cloud-services"></a>Облачные службы
![Облачная служба Azure](./media/fundamentals-introduction-to-azure/CloudServicesIntroNew.png)   
*Рисунок: Облачные службы Azure предоставляет месте toorun высокомасштабируемых настраиваемого кода на платформе как среде службы (PaaS)*

Предположим, что нужно toobuild облачного приложения, который может поддерживать большое количество одновременных пользователей не требует много администрирования и никогда не выходит из строя. Возможно установленное программное обеспечение поставщика, например, что решение tooembrace программное обеспечение как услуга (SaaS) путем создания версии одного из приложений в облаке hello. Или вы можете недавно открыть компанию и заниматься созданием потребительского приложения, рост числа пользователей которого по вашим оценкам обещает быть довольно бурным. Если вы создаете продукт в Azure, какую модель выполнения вам следует выбрать?

Модель веб-приложений Azure позволяет создать такое веб-приложение, однако есть некоторые ограничения. У вас нет административного доступа, то есть вы не можете, например, устанавливать произвольное программное обеспечение. Виртуальные машины Azure предоставляет гибкие возможности, включая административного доступа и его определенно можно использовать toobuild очень большие возможности масштабирования приложения, но у вас toohandle многие аспекты администрирования и стабильности самостоятельно. Хотелось бы — параметр, который позволяет hello управления требуется, но также обрабатывает большую часть работы hello, необходимые для администрирования и надежности.

Именно это и предлагает модель облачных служб Azure. Эта технология является разрабатывались специально toosupport масштабируемые и надежные и низким администрирования приложений и является примером так часто называемый платформа как услуга (PaaS). toouse, создать приложение с помощью технологии hello, таком как C#, Java, PHP, Python, Node.js или что-нибудь другое. Код выполняется на виртуальных машинах (экземплярах ссылка tooas) под управлением версии Windows Server.

Но эти виртуальные машины отличаются от hello из них, создаваемые вами виртуальные машины Azure. Прежде всего, Azure управляет ими самостоятельно, устанавливая исправления операционной системы и автоматически развертывая новые исправленные образы. Это означает, что приложение не следует сохраняют состояние в веб- и рабочих экземпляров ролей; Вместо этого он должен сохраняться в один из вариантов управления hello данных Azure, описанные в следующем разделе hello. Azure также отслеживает виртуальные машины, перезапуская их в случае сбоя. Можно задать облачных служб tooautomatically создания большее или меньшее число экземпляров в toodemand ответа. Это позволяет увеличить toohandle использования и масштабирование обратно, не обращая столько при использовании меньше.

У вас есть два toochoose ролей при создании экземпляра, и на основе Windows Server. Hello основное различие между двумя hello является то, что экземпляр веб-роли запущены службы IIS, а экземпляр рабочей роли нет. Как осуществляется в hello же образом, тем не менее и оно часто приложения toouse оба. Например экземпляре веб-роли может принимать запросы от пользователей, а затем передать их tooa экземпляра роли рабочего процесса для обработки. tooscale приложения вверх или вниз, можно запросить, Azure создать дополнительные экземпляры либо роли или завершить работу существующих экземпляров. И аналогичные tooAzure виртуальных машин вы оплачиваете только время hello, каждый рабочих и веб-экземпляр роли выполняется.

**Сценарии облачных служб**

Облачные службы являются идеальной toosupport значительных масштабирования при требуется больший контроль над hello платформы, чем предоставляемые веб-приложения Azure, но не требуется контроль над hello базовой операционной системы.

#### <a name="choosing-a-compute-model"></a>Выбор модели вычислений
Страница приветствия [веб-приложения Azure, облачных служб и виртуальных машин сравнения](app-service-web/choose-web-site-cloud-service-vm.md) предоставляет более подробные сведения о том, как toochoose модель вычислений.

## <a name="data-management"></a>Управление данными
Приложениям требуются данные, причем разным видам приложений требуются разные виды данных. По этой причине Azure предоставляет несколько различных способов toostore и управления данными. Azure предоставляет множество вариантов хранилища, но все они предназначены для долговременного хранения.  С помощью любого из этих вариантов всегда есть три копии данных синхронизируются во всех центра обработки данных Azure — 6, если разрешить Azure toouse геоизбыточность tooback копирование tooanother центра обработки данных по крайней мере 300 километров.     

### <a name="in-virtual-machines"></a>В виртуальных машинах
toorun Hello возможности SQL Server или другой СУБД на виртуальной машине, созданные с виртуальными машинами Azure уже были упомянуты. Учтите, что этот параметр, не ограниченной toorelational систем. Вы также бесплатно toorun технологий NoSQL, таких как MongoDB и Cassandra. Запуск систему базы данных является просто ИТ реплицирует мы находимся используется tooin собственных центрах обработки данных —, но также требует обработки администрирования hello, СУБД.  В других параметрах Azure обрабатывает несколько или все hello Администрирование за вас.

Опять же состояние hello hello виртуальной машины и любой дополнительный диск с данными создать или отправить, поддерживаемых хранилища больших двоичных объектов (который мы говорим о более поздней версии).  

### <a name="azure-sql-database"></a>База данных SQL Azure
![База данных SQL хранилища Azure](./media/fundamentals-introduction-to-azure/StorageAzureSQLDatabaseIntroNew.png)   

*Рисунок: База данных SQL Azure предоставляет службу управляемого реляционной базы данных в облаке hello.*

Для реляционного хранилища Azure предоставляет функции hello базы данных SQL. Не позволяйте hello именования ввести пользователя в заблуждение. Она отличается от обычной базы данных SQL, которую предоставляет SQL Server, запущенный в Windows Server.  

Называвшиеся SQL Azure, база данных SQL Azure предоставляет все hello основные возможности реляционной базы данных системы управления, включая атомарные транзакции, параллельный доступ к данным несколькими пользователями с целостность данных, ANSI SQL-запросы и знакомые программирование модель. Как и в случае с SQL Server, доступ к Базе данных SQL можно осуществить с использованием Entity Framework, ADO.NET, JDBC и других привычных технологий доступа к данным. Оно также поддерживает большинство hello языка T-SQL, вместе со средствами SQL Server, например SQL Server Management Studio. У любого человека, знакомого с SQL Server (или другой реляционной базой данных), использование базы данных SQL не вызовет никаких затруднений.

Но база данных SQL не просто СУБД в hello ИТ облако и службы PaaS. Вы по-прежнему управлять данных и доступ к его, но работы администратора grunt hello, например управление инфраструктурой оборудования hello и автоматически обновлениях hello базы данных и операционной системы по копии toodate берет на себя базы данных SQL. База данных SQL также предоставляет высокую доступность, автоматическое резервное копирование, возможности восстановления на момент времени и может реплицировать копии в географических регионах.  

**Сценарии для базы данных SQL**

При создании приложения Azure (с помощью любой модели hello вычислений), требуется реляционного хранилища базы данных SQL может быть хорошим вариантом. Приложения, запущенные вне hello облака можно также использовать этой службы, поэтому существует множество других сценариев. Например, доступ к данным, хранящимся в Базе данных SQL, может осуществляться из разных клиентских систем, включая настольные компьютеры, ноутбуки, планшетные ПК и телефоны. А благодаря наличию встроенной функции высокой доступности, реализуемой посредством репликации, использование базы данных SQL позволяет свести время простоя к минимуму.

### <a name="tables"></a>Таблицы
![Таблицы хранилища Azure](./media/fundamentals-introduction-to-azure/StorageTablesIntroNew.png)  

*Рисунок: Таблицы Azure предоставляет плоский вида toostore данных NoSQL.*

Эта функция иногда вызывается по-другому, поскольку она является частью более крупной функции, которая называется "Хранилище Azure". Если вы видите «таблицы», «Azure таблицы» или «хранения таблицы», его все же hello.  

И не следует путать с именем hello: Эта технология не обеспечивает реляционное хранилище. Фактически, это пример подхода NoSQL под названием "хранилище ключей/значений". Таблицы Azure позволяют приложению сохранять свойства различных типов, например строк, целых чисел и дат. Позднее приложение может извлечь группу свойств, предоставив для нее уникальный ключ. Хотя сложные операции, как соединения не поддерживаются, в таблицы обеспечивают быстрый доступ к данным tootyped. Они также очень большие возможности масштабирования, с одной таблицы может toohold сильнее, чем терабайт данных. И сопоставление их простоты, таблицы toouse обычно дешевле, чем реляционного хранилища базы данных SQL.

**Сценарии для таблиц**

Предположим, что нужно toocreate приложения Azure, которым нужен быстрый доступ к данным tootyped может быть большое количество его, но не требует tooperform сложных запросов SQL на основе этих данных. Например предположим, что вы создаете приложение-потребитель, который должен toostore сведения профиля клиента для каждого пользователя. Приложение будет toobe популярным, поэтому обязательно tooallow больших объемов данных, но не будет не очень много операций с данными за пределы хранения, извлечения простого способа. Это именно hello типа сценарии, где имеет смысл использовать таблицы Azure.

### <a name="blobs"></a>BLOB-объекты
![Большие двоичные объекты службы хранилища Azure](./media/fundamentals-introduction-to-azure/StorageBlobsIntroNew.png)    
*Рисунок. BLOB-объекты Azure предоставляют неструктурированные двоичные данные.*  

BLOB-объекты Azure (попытку «Хранилища больших двоичных объектов» и просто «хранилища больших двоичных объектов» являются hello одинаково) — спроектированный toostore неструктурированных двоичных данных. Как и таблицы, BLOB-объекты предоставляют экономичное хранилище, а в отдельном BLOB-объекте может храниться до одного 1 ТБ (одного терабайта) данных. Приложения Azure также могут использовать диски Azure, что позволяет BLOB-объектам обеспечивать постоянное хранение для файловой системы Windows, подключенной в экземпляре Azure. приложение Hello видит обычных файлов Windows, но содержимое hello хранятся в большом двоичном объекте.

Хранилище BLOB-объектов используется многими другими функциями Azure (включая виртуальные машины), поэтому оно также может обработать ваши рабочие нагрузки.

**Сценарии для BLOB-объектов**

Приложение, которое сохраняет видео, массовые файлы или другие двоичные сведения, может использовать BLOB-объекты в качестве простого и недорогого хранилища. BLOB-объекты также обычно используются совместно с другими службами, например с сетью доставки содержимого, которая будет описана далее.  

### <a name="import--export"></a>Импорт и экспорт
![Служба импорта и экспорта Azure](./media/fundamentals-introduction-to-azure/ImportExportIntroNew.png)  

*Рисунок: Azure импорта / экспорта предоставляет возможность tooship hello tooor физический жесткий диск из Azure для данных быстрее и дешевле массового импорта или экспорта.*  

Иногда требуется toomove большой объем данных в Azure. Это займет много времени, возможно, дни, и будет значительно использовать пропускную способность. В этих случаях можно использовать импорта и экспорта Azure, что позволяет вам tooship зашифрованные Bitlocker 3,5" жестких дисков SATA непосредственно центрах обработки данных tooAzure, где корпорация Майкрософт передаст hello данных в хранилище больших двоичных объектов для вас.  После окончания загрузки hello, корпорация Майкрософт предоставляет tooyou назад диски hello.  Можно также запросить экспорта на жесткие диски и отправляются назад tooyou по почте больших объемов данных из хранилища больших двоичных объектов.

**Сценарии для импорта и экспорта**

* **Крупной миграции данных** — каждый раз, когда у вас есть большое количество данных (терабайт), требуется tooupload tooAzure, hello службы импорта и экспорта чаще всего гораздо быстрее и может быть дешевле, чем помещается над hello internet. После загрузки данных hello в больших двоичных объектов, он может обрабатывать в других форм, таких как хранилище таблиц или базы данных SQL.
* **Архивирование восстановления данных** -можно использовать импорта и экспорта toohave Microsoft передачи больших объемов данных, хранящихся в хранилище больших двоичных объектов Azure tooa запоминающее устройство, отправки и затем у этого устройства доставляется задней tooa расположение при работе. Поскольку это занимает некоторое время, данный вариант не подходит для аварийного восстановления. Лучше всего архивировать данные, к которым не потребуется быстрый доступ.

### <a name="file-service"></a>Служба файлов
![Служба файлов Azure](./media/fundamentals-introduction-to-azure/FileServiceIntroNew.png)    
*Рисунок: Azure файловые службы предоставляют SMB \\ \\server\share пути для приложения, работающие в облаке hello.*

В локальной среде это общие toohave больших объемов хранения файлов, доступные через hello протокола Server Message Block (SMB) с использованием \\ \\Server\share формат. Azure теперь имеет службы, которая позволяет вам toouse этого протокола в облаке hello. Приложения, работающие в Azure можно использовать файлы tooshare между виртуальными машинами с помощью интерфейсов API, например ReadFile и WriteFile знакомы файловой системы. Кроме того, файлы hello можно получить в hello то же время через интерфейс REST, который позволяет общих папок hello tooaccess из локальной также настроить виртуальную сеть. Файлы Azure является надстройкой hello BLOB-объектов, поэтому он наследует hello же доступности, устойчивость, масштабируемость и географической избыточности, встроенные в хранилище Azure.

**Сценарии для файлов Azure**

* **Миграция существующих облачные приложения toohello** -его проще toomigrate локальных приложений toohello облако, используйте файл делится данными tooshare между частями приложения hello. Каждая виртуальная машина подключается toohello общей папки и затем мог считывать и записывать файлы так же, как он локального файла используют.
* **Общие параметры приложения** -шаблоном для распределенных приложений является toohave файлы конфигурации в центральном расположении, где они могли быть доступны из многих разных виртуальных машин. Эти файлы конфигурации можно хранить в общем ресурсе файлов Azure, и его могут читать все экземпляры приложений. Hello параметры также можно управлять с помощью интерфейса REST hello, позволяющий получить доступ по всему миру toohello файлы конфигурации.
* **Общий ресурс диагностики**. Вы можете сохранять и совместно использовать файлы диагностики, такие как журналы, метрики и аварийные дампы. Наличие этих файлов, доступных через hello SMB и интерфейс REST позволяет toouse приложений разнообразные средства анализа для обработки и анализа диагностических данных hello.
* **Разработки, тестирования и отладки** — когда разработчики и администраторы работают на виртуальных машинах в облаке hello, они часто требуется набор средств и служебных программ. Установка и распространение этих служебных программ на каждой виртуальной машине занимает много времени. В файлах, Azure разработчик или администратор можно хранить своих любимых средств на общем файловом ресурсе и подключение toothem из любой виртуальной машине.

## <a name="networking"></a>Сеть
Azure сегодня работает в распределяться Здравствуй, мир! многих центрах обработки данных. При запуске приложения или хранения данных, можно выбрать один или несколько из этих toouse центрах обработки данных. Можно также подключиться toothese центрах обработки данных различными способами, с помощью следующих служб hello.

### <a name="virtual-network"></a>Виртуальная сеть
![Виртуальная сеть](./media/fundamentals-introduction-to-azure/VirtualNetworkIntroNew.png)   

*Рисунок: Виртуальных сетей предоставляет частной сети в облаке hello, разных служб можно говорить tooeach другие или tooon локальные ресурсы при настройке VPN-подключения между организациями.*  

Один удобный способ toouse общедоступное облако — tootreat его как расширение собственного центра обработки данных.

Поскольку вы можете создавать виртуальные машины по запросу и затем удалять эти виртуальные машины (и прекращать платить за них), когда они больше не требуются, вы можете получать вычислительные мощности только в том случае, когда они действительно необходимы. А поскольку виртуальные машины Azure позволяет создавать виртуальные машины под управлением SharePoint, Active Directory и другие знакомые локального программного обеспечения, такой подход может работать с hello приложения, которые уже есть.

toomake это действительно полезным, пользователи с головой может tootreat toobe эти приложения как если бы они были запущены в собственном центре обработки данных. Именно это и предоставляет виртуальная сеть Azure. С помощью устройства шлюза VPN, администратор может настраивать виртуальной частной сети (VPN) между локальной сети и виртуальных машин, развернутых tooa виртуальной сети в Azure. Так как вы назначаете собственные IP версии 4 адреса toohello облака виртуальных машин, они отображаются toobe в своей сети. Пользователи в вашей организации использовать приложения hello этих виртуальных машин содержать, как если бы они были запущены локально.

Дополнительные сведения о планировании и создании виртуальной сети, подходящей вам, см. в разделе [Обзор виртуальной сети](virtual-network/virtual-networks-overview.md).

### <a name="express-route"></a>ExpressRoute
![ExpressRoute](./media/fundamentals-introduction-to-azure/ExpressRouteIntroNew.png)   

*Рисунок: ExpressRoute использует виртуальную сеть Azure, но маршруты соединения через быстрее выделенные линии вместо hello общедоступный Интернет.*  

Если вам нужна дополнительная пропускная способность или безопасность кроме той, которую предоставляет подключение виртуальной сети Azure, можно рассмотреть ExpressRoute. В некоторых случаях ExpressRoute может также сэкономить деньги. По-прежнему необходимо виртуальной сети в Azure, но hello связь между Azure и веб-узла использует выделенное соединение, которое не проходит через hello общедоступный Интернет. В заказ toouse этой службы, вам потребуется toohave соглашение с поставщика сетевых услуг или поставщика exchange.

Настройка подключения ExpressRoute требуется больше времени и планирования, поэтому вы можете toostart сеть сеть VPN-подключение, а затем перенести tooan подключение ExpressRoute.

Дополнительные сведения об ExpressRoute см. в разделе [Технический обзор ExpressRoute](expressroute/expressroute-introduction.md).

### <a name="traffic-manager"></a>Диспетчер трафика
![TrafficManager](./media/fundamentals-introduction-to-azure/TrafficManagerIntroNew.png)   

*Рисунок: Диспетчер трафика Azure позволяет вам tooroute глобальный трафик tooyour службе на основе правил интеллектуальной.*

Если приложение Azure работает в нескольких центрах обработки данных, можно использовать диспетчер трафика Azure tooroute запросы от пользователей интеллектуально экземпляров приложения hello. Также может осуществлять маршрутизацию трафика Здравствуйте, tooservices не работает в Azure, при условии, что они были доступны из Интернета.  

Приложения Azure с пользователями в одной части Здравствуй, мир! может выполняться в центре обработки данных Azure только один. Приложения с пользователями разбросанных Здравствуй, мир, тем не менее, — это скорее всего, toorun в нескольких центрах обработки данных, может быть даже все из них. В этом случае второй сталкиваются проблема: как это интеллектуально прямо экземпляров tooapplication пользователей? Большую часть времени hello, возможно, необходимо каждый пользователь tooaccess hello datacenter ближайший tooher, так как он скорее всего будет предоставлять ее hello наилучшее время отклика. Но что делать, если этот экземпляр приложения hello перегружен или недоступен? В этом случае он должен быть низким приоритетом toodirect ее запрос автоматически tooanother центра обработки данных. Именно это и делает Azure Traffic Manager.

Владелец приложения Hello определяет правила, которые можно указать, как запросы от пользователей должна быть расширенный toodatacenters, а затем использует toocarry диспетчера трафика, эти правила. Например пользователи обычно может быть направленной toohello ближайший центр обработки данных Azure, но передачу tooanother один превышение времени ответа hello в своем центре обработки данных по умолчанию hello время ответа из других центрах обработки данных. Глобально распределенных приложений с большим количеством пользователей возникают проблемы toohandle встроенную службу подобные полезно.

Диспетчер трафика использует конечные точки для tooservice пользователей tooroute каталога службы имен (DNS), но дополнительно трафик проходит через диспетчер трафика, после установления соединения. Такой подход позволяет избежать превращения Traffic Manager в узкое место, которое может снизить эффективность обмена информацией во время работы службы.

## <a name="developer-services"></a>Службы разработки
Azure предлагает ряд средств toohelp разработчикам и ИТ-специалистов, создания и настройки приложений в облаке hello.  

### <a name="azure-sdk"></a>Пакет SDK Azure
В 2008 hello самой первой предварительной версии Azure поддерживается только для разработки .NET. Однако сегодня можно создавать приложения Azure практически на любом языке. В настоящее время корпорация Майкрософт предоставляет языковые пакеты SDK для .NET, Java, PHP, Node.js, Ruby и Python. Имеется также общий пакет SDK Azure, который обеспечивает базовую поддержку для любого языка, например C++.  

Эти пакеты SDK помогают строить, разворачивать и управлять приложениями Azure. Они доступны на сайте [www.microsoftazure.com](https://azure.microsoft.com/downloads/) или на портале GitHub, и их можно использовать с Visual Studio и Eclipse. Azure также предлагает средства командной строки, которые разработчики могут использовать с любой редактор среды или среды разработки, включая средства для развертывания приложений tooAzure из систем Linux и Macintosh.

Помимо помощи в построении приложений Azure, эти пакеты SDK также предоставляют клиентские библиотеки, помогающие создавать программное обеспечение, которое использует службы Azure. Например можно создать приложение, которое выполняет чтение и запись больших двоичных объектов Azure или создать инструмент, который выполняет развертывание приложения Azure через интерфейс управления Azure hello.

### <a name="visual-studio-team-services"></a>Visual Studio Team Services
Visual Studio Team Services является именем маркетинга, охватывающие число служб, которые помогают приложениям toodevelop в hello Azure.

путаницы tooavoid - он не предоставляет размещенные или веб-версии Visual Studio. Вам все еще требуется локальная копия Visual Studio. Однако она предоставляет многие другие средства, которые могут оказаться очень полезными.

В нее не входит размещенная система управления версиями Team Foundation Service, которая позволяет управлять версиями и отслеживать рабочие элементы.  Вы даже можете использовать Git для управления версиями, если предпочитаете это. И может изменяться hello системы управления версиями, используемого в проекте. Можно создать неограниченное закрытый командных проектов доступны из в любом месте в Здравствуй, мир.  

Visual Studio Team Services предоставляют службу нагрузочного тестирования. Можно выполнять нагрузочные тесты, созданные в Visual Studio на виртуальных машинах в облаке hello. Укажите hello общее число пользователей, которые должны tooload теста с и Visual Studio Team Services автоматически определить, сколько агенты необходимы, запустит hello необходимые виртуальные машины и выполнение нагрузочных тестов. Если вы являетесь подписчиком MSDN, то получите тысячи бесплатных минут пользователей нагрузочного тестирования в месяц.

Visual Studio Team Services также предлагают поддержку динамической разработки с такими функциями, как сборки непрерывной интеграции, канбан-доски и виртуальные комнаты команды.

**Сценарии использования Visual Studio Team Services**

Visual Studio Team Services — это хороший вариант для компаний, которым требуется toocollaborate по всему миру и еще нет инфраструктуры hello в месте toodo таким образом. Вы можете выполнить настройку за минуты, выбрать систему управления версиями и начать написание кода и разработку в один день.  средства team Hello предоставляет место для координации совместной работы и дополнительные средства hello предоставлять и анализа hello нужен tootest быстро настраивать приложение.

Но организаций, которые уже есть в локальной системе можно протестировать новые проекты в Visual Studio Team Services toosee, если более эффективно.   

### <a name="application-insights"></a>Application Insights
![Application Insights](./media/fundamentals-introduction-to-azure/ApplicationInsights.png)  

*Рисунок: Application Insights позволяет отслеживать производительность и использование активного веб-приложения или приложения на устройстве.*

После публикации приложения независимо от того, выполняется ли оно в мобильном устройстве, на настольном компьютере или веб-браузере, Application Insights сообщает о том, как работает приложение и как с ним работают пользователи. Хранит количество сбоев и медленный ответ, предупреждения, если фигур hello cross неприемлемо пороговые значения и помогают диагностировать проблемы.

При разработке новых компонентов, планирование toomeasure его успешного завершения работы пользователей. Анализ шаблонов использования поможет понять, что лучше всего подходит для ваших клиентов, и совершенствовать приложение в каждом цикле разработки.

Несмотря на то, что служба Application Insights размещается в Azure, ее можно использовать для широкого и продолжающего расти диапазона приложений как в Azure, так и вне Azure. Этот диапазон включает веб-приложения J2EE и ASP.NET, а также приложения для iOS, Android, OS X и Windows. Данные телеметрии отправляется пакет SDK, созданного с помощью приложения hello toobe анализируются и отображаются в hello служба Application Insights в Azure.

Если требуется более специализированные analytics экспортируйте hello телеметрии поток tooa базы данных или tooPower бизнес-Аналитики или любые другие инструменты.

**Сценарии Application Insights**

Вы разрабатываете приложение. Это может быть веб-приложение, приложение для устройства или приложение для устройства с серверной частью.

* Настройки hello производительность вашего приложения после его публикации, или его во время тестирования нагрузки.  Application Insights агрегирует данные телеметрии из всех экземпляров hello установлен и предоставляет диаграммы времени отклика, запроса и число исключений, время ответа зависимостей и другие показатели производительности. Они помогают настроить производительность приложения. Можно также вставить код tooreport более конкретные данные, если это необходимо.
* Определяйте и решайте проблемы приложения в реальном времени. Вы можете получать оповещения по электронной почте, если индикаторы производительности превышают допустимые пределы. Можно выяснить сеансов для конкретного пользователя, например toosee hello запрос, который вызвал исключение.
* Отслеживание использования tooassess hello успешности каждой новой функции. При создании нового пользовательского описания функциональности, сколько используется toomeasure плана и ли пользователи достигнуть ожидаемого. Application Insights позволяет основные данные, как представления веб-страницы и взаимодействие с пользователем hello tootrack код можно вставить более подробно.

### <a name="automation"></a>Служба автоматизации
Не нравится toowaste времени на выполнение hello же вручную и снова обрабатывает. Служба автоматизации Azure предоставляет способ для toocreate, мониторинг, управление и развертывание ресурсов в вашей среде Azure.  

Автоматизация применяет «Runbook», которые использует рабочие процессы Windows PowerShell (и обычные PowerShell) на деле hello. Модули Runbook предназначены toobe выполнена без вмешательства пользователя. Рабочие процессы PowerShell позволяет hello состояние toobe скрипта, сохранен в контрольных точках вдоль hello способом. Если происходит сбой, не toostart сценарий с начала hello. Вы можете перезапустить в последней контрольной точке hello. Это сохраняет большой объем работы при попытке сценария дескриптор toomake hello всех возможных ошибок.

**Сценарии автоматизации**

Служба автоматизации Azure является ручной hello tooautomate хорошим выбором, длительные, вероятность ошибок и часто повторяющиеся задачи в Azure.

### <a name="api-management"></a>Управление API
Создание и публикация интерфейсов прикладного программирования (API) на hello Интернет-это общие tooapplications служб tooprovide способом. Если эти службы resellable (например, данных о погоде), организации можно разрешить третьих лиц tooaccess те же службы за плату. При масштабировании toomore партнеров, вам обычно требуется toooptimize и управления доступом.  Некоторые партнеры даже может понадобиться hello данных в другом формате.

Управления API Azure облегчает toopartners API-интерфейсы toopublish организаций, сотрудников и сторонними разработчиками безопасно и с масштабом. Он предоставляет другой конечной точке API и действует как toocall прокси hello реальная конечная точка службы, такие как кэширование преобразования, обеспечивая при этом регулирования, управления доступом и аналитика статистической обработки.

**Сценарии управления API**

Предположим, что ваша компания имеет набор устройств, что все требуется toocall задней tooa центральная служба tooget данных — например транспортной компании с устройств в каждой грузовик hello пути.  Наверняка hello компании может потребоваться tooset копирование tootrack системы она будет собственных грузовиках, поэтому можно надежно предсказать и обновить время доставки. Это позволит узнать количество грузовиков и планировать в соответствии с этими сведениями.  Каждый грузовик потребуется устройство, которое выполняет обратный вызов tooa центральное расположение с его расположение и скорость данных и может быть несколько.

Клиент hello доставки компании, скорее всего, будет также использовать преимущества получение этого размещения данных.  Hello клиент может использовать его tooknow, насколько далеко продукты имеют tootravel, где они возникли затруднения, сколько они оплаты вдоль определенных маршрутов (если вместе с том, что они платных tooship). Если hello доставки компании собирает эти данные уже, многие клиенты могут платить за его.  Но затем hello доставки компании tooprovide способом клиенты toogive hello данных. Как только они предоставляют доступ toocustomers, могут не иметь контролировать частоту hello запросе данных. Они будут иметь tooprovide правила, определяющие, кто имеет доступ к данных. Все эти правила бы toobe, встроенные в их внешних API. И в этом случае управление API может помочь.  

## <a name="identity-and-access"></a>Удостоверение и доступ
Работа с удостоверением характерна для большинства приложений. Сведения о том, кем является пользователь, позволяют приложению принять решение о разрешении взаимодействия с ним. Azure предоставляет службы toohelp отслеживания идентификаторов, а также интегрировать хранилищ удостоверений, который уже используется.

### <a name="active-directory"></a>Active Directory
Как и большинство служб каталогов Azure Active Directory хранит сведения о пользователям и организациям hello, которому они принадлежат. Он позволяет пользователям вход, а затем передает их с помощью токенов может быть представлен tooapplications tooprove свою личность. Кроме того, это позволяет синхронизировать информацию о пользователях с Windows Server Active Directory, выполняемой локально в вашей локальной сети. Хотя механизмы hello и форматов данных, используемых Azure Active Directory не отличается от тех, которые используются в Windows Server Active Directory, функций hello похожи.

Он является важным toounderstand, Azure Active Directory предназначен главным образом для использования с облачных приложений. Она может использоваться приложениями, выполняемыми в Azure или на других облачных платформах. Ее также могут использовать собственные облачные приложения корпорации Майкрософт, например входящие в состав Office 365. Если вы хотите tooextend центра обработки данных в облаке hello, с помощью виртуальных машин Azure и виртуальной сети Azure, Azure Active Directory не выбирайте hello. Вместо этого будет необходимо toorun Windows Server Active Directory на виртуальных машинах.

toolet приложений доступ к hello сведения, содержащиеся в нем, Azure Active Directory предоставляет называется Azure Active Directory Graph API RESTful. Этот API позволяет приложениям, выполняемым на любые объекты каталога доступа платформы и hello отношения между ними.  Например приложение авторизованным может использовать этот API toolearn о пользователь, hello групп, которым он принадлежит и другие сведения. Приложения можно также просмотреть отношения между их для пользователей социального графа информирующее их более рационально работа с подключениями hello между сотрудниками.

Еще одну функцию этой службы Azure Active Directory Access Control, облегчает приложение tooaccept данные идентификаторов от Facebook, Google, идентификатор Windows Live ID и других популярных поставщиков. Не требует от hello приложения toounderstand hello данные различных форматов и протоколов, используемых для каждого из этих поставщиков, управление доступом преобразует их все в один общий формат. Это также позволяет приложению принимать входы в систему из одного или нескольких доменов Active Directory . Например поставщика, предоставляющего приложения SaaS использовать пользователи toogive контроля доступа Azure Active Directory в каждом из его применение toohello клиентов.

Службы каталогов представляют собой базовую основу для локальной обработки. Не должно быть неожиданным, если они также важно в облаке hello.

### <a name="multi-factor-authentication"></a>Многофакторная идентификация
![Многофакторная идентификация Microsoft Azure;](./media/fundamentals-introduction-to-azure/MFAIntroNew.png)   

*Рисунок: Многофакторной проверки подлинности предоставляет функциональные возможности hello для вашего приложения tooverify более одного вида*

Вопросы безопасности всегда играют важную роль. Multi-Factor Authentication (MFA) гарантирует, что только сами пользователи могут получить доступ к своей учетной записи. MFA (также известная как двухфакторная проверка подлинности, или "2FA") требует от пользователей предоставить два из трех способов проверки данных для входа в систему и участия в транзакциях.

* Что-то, что известно вам (обычно это пароль).
* Что-то, что у вас есть (доверенное устройство, которое непросто дублируется, такое как телефон).
* Что-то, относящееся непосредственно к вам (биометрические данные).

Когда пользователь входит в систему, может потребоваться их tooalso подтвердить свою личность с помощью мобильного приложения, телефонный звонок или текстовое сообщение в сочетании с свой пароль. По умолчанию Azure Active Directory поддерживает hello использование паролей в качестве ее единственного способа проверки подлинности для входа в систему пользователя. Можно использовать многофакторную проверку Подлинности вместе с Azure AD или пользовательские приложения и каталоги с помощью hello SDK многофакторной проверки Подлинности. Вы также можете использовать этот способ с локальными приложениями с помощью сервера Multi-Factor Authentication.

**Сценарии MFA**

Защита входа в учетные записи с конфиденциальными сведениями, такими как реквизиты для входа в банковскую систему и доступ к исходному коду, когда неавторизованный вход может привести к высоким финансовым потерям или убыткам вследствие нарушения прав интеллектуальной собственности.   

## <a name="mobile"></a>Мобильные приложения
При создании приложения для мобильных устройств, Azure может помочь хранения данных в облаке hello, проверять подлинность пользователей и отправлять push-уведомления без необходимости toowrite значительные возможности пользовательского кода.

Хотя определенно можно построить hello серверной части для мобильного приложения с помощью виртуальных машин, облачных служб или веб-приложений, чтобы тратить базовые компоненты службы с помощью служб Azure записи hello гораздо меньше времени система.

### <a name="mobile-apps"></a>Мобильные приложения
![Мобильные приложения](./media/fundamentals-introduction-to-azure/MobileServicesIntroNew.png)

*Рисунок. Мобильные приложения предоставляют функции, обычно необходимые приложениям, предназначенным для мобильных устройств.*

Мобильные приложения Azure предоставляют множество полезных функций, которые могут сэкономить ваше время при создании серверной части для мобильного приложения. Оно позволяет toodo простая Подготовка и управление ими данные, хранящиеся в базе данных SQL. С серверным кодом вы можете легко использовать дополнительные варианты хранилища данных, такие как хранилище BLOB-объектов или MongoDB. Мобильные приложения поддерживают уведомления, но в некоторых случаях вместо них можно использовать центры уведомлений, как описано далее.  Hello service также имеет REST API, мобильного приложения можно вызвать tooget работы. Мобильные приложения также предоставляет возможность hello tooauthenticate пользователей с помощью Microsoft и Active Directory, а также других хорошо известными поставщиками удостоверений Facebook, Google и Twitter.   

Можно использовать другие службы Azure Service Bus и рабочих ролей и подключения tooon локальных систем. Можно даже использовать сторонних производителей надстройки из hello дополнительные функциональные возможности tooprovide хранилища Azure (например, SendGrid для электронной почты).

Собственные клиентские библиотеки для Android, iOS, HTML/JavaScript, Windows Phone и магазина Windows позволяют упростить toodevelop для приложений на всех основных мобильных платформ. REST API позволяет toouse мобильных служб данных и функциональных возможностей проверки подлинности с помощью приложений на разных платформах. Одна мобильная служба может поддерживать несколько клиентских приложений, так что вы можете предоставить пользователю согласованный интерфейс между устройствами.

Поскольку Azure уже поддерживает большом масштабе, может обрабатывать трафик hello как приложение становится более популярным.  Мониторинг и ведение журнала поддерживаются toohelp устранения неполадок и управление производительностью.

### <a name="notification-hubs"></a>Центры уведомлений
![NotificationHubs](./media/fundamentals-introduction-to-azure/NotificationHubsIntroNew.png)  

*Рисунок. Центры уведомлений предоставляют функции, обычно необходимые приложениям, предназначенным для мобильных устройств.*

Хотя можно писать код toodo уведомлений в мобильных приложениях Azure, концентраторы уведомлений — оптимизированный toobroadcast миллионов высокий уровень персонализации push-уведомлений в течение минут.  У вас нет tooworry о такие сведения, как оператор мобильной связи или изготовителю устройства. Вы можете указать одного или миллионы пользователей с помощью одного вызова API.

Концентраторы уведомлений — спроектированный toowork с любого сервера. Можно использовать мобильные приложения Azure, пользовательские базы данных в облаке hello под управлением любого поставщика или в локальной серверной части.

**Сценарии концентратора уведомлений** при создании мобильных игр, где проигрыватели найдено включает, может потребоваться проигрывателя toonotify 2 1 игрок завершения ее включить. Если это все, что нужно toodo, можно просто использовать мобильные приложения. Но если имеется 100 000 пользователей играть игру и время toosend tooeveryone конфиденциальных Бесплатное предложение, концентраторы уведомлений является лучшим выбором hello.

Вы можете отправлять новости, спортивные события и toomillions уведомления извещения продукта пользователей с низкой задержкой. Предприятия может уведомить сотрудников о новых время конфиденциальные обмен данными, например потенциальных покупателях, поэтому сотрудники не имеют tooconstantly Проверьте адрес электронной почты или других приложений, toostay проинформировать. Вы также можете отправлять одноразовые пароли, необходимые для Multi-Factor Authentication.

## <a name="back-up"></a>Резервное копирование
Каждый корпоративный должен toobackup и восстановления данных. Можно использовать Azure toobackup и восстановление приложения в облако hello или локальной. Azure предлагает toohelp различные варианты в зависимости от типа hello резервного копирования.

### <a name="site-recovery"></a>Site Recovery
Azure Site Recovery (прежнее название — диспетчер восстановления Hyper-V) может помочь защитить важные приложения, связавшись репликации hello и восстановления между сайтами. Site Recovery предоставляет возможность tooprotect приложений на основе Hyper-v, VMWare или SAN tooyour собственный дополнительный сайт, tooa поставщика услуг размещения сайта или tooAzure и избежать hello затрат и сложности построения и управления собственные вторичное расположение. Azure шифрует данные и связи и имеется параметр hello слишком включить шифрование для данных в rest.

Постоянно отслеживает hello работоспособности служб и помогает автоматизировать hello надлежащее восстановления служб hello для события сбоя сайта в основном центре обработки данных hello. Виртуальные машины можно быстро перевести в режим в службе восстановления toohelp направленные манере, даже для сложных многоуровневых рабочих нагрузок.

Site Recovery работает с существующими технологиями, такими как реплика Hyper-V, System Center и SQL Server AlwaysOn. Дополнительные сведения см. в статье [Что такое Site Recovery?](site-recovery/site-recovery-overview.md)

### <a name="azure-backup"></a>Служба архивации Azure
![Служба архивации Azure](./media/fundamentals-introduction-to-azure/AzureBackupIntroNew.png)  

*Рисунок: Резервного копирования Azure выполняет резервное копирование данных из локальных серверов Windows в облаке hello.*  

Служба архивации Azure выполняет резервное копирование данных из локальных серверов под управлением Windows Server в облаке hello. Вы можете управлять резервными копиями непосредственно из hello средств резервного копирования в Windows Server 2012, Windows Server 2012 Essentials или System Center 2012 — Data Protection Manager. Кроме того, вы можете использовать специальный агент резервного копирования.

Данные защищены, поскольку резервные копии шифруются перед передачей, хранятся зашифрованными в Azure и защищены отправленным сертификатом. Служба Hello использует hello же защиты высокой надежности и избыточности данных найден в хранилище Azure.  Вы можете выполнять резервное копирование файлов и папок регулярно или сразу, запустив полную или добавочную архивацию. После toohello облака резервное копирование данных, авторизованные пользователи могут легко восстановить резервные копии tooany сервера. Он также предлагает политики хранения настраиваемых данных, сжатие данных и передачи данных регулирования можно управлять hello стоимость toostore и передачи данных.

**Сценарии для службы архивации Azure**

Если вы уже используете Windows Server или System Center, служба архивации Azure — подходящее решение для архивации файловой системы сервера, виртуальных машин и баз данных SQL Server.  Она работает с зашифрованными, разреженными и сжатыми файлами. Существуют некоторые ограничения, поэтому необходимо [проверка предварительных требований для резервного копирования Azure hello](http://technet.microsoft.com/library/dn296608.aspx) первой.

## <a name="messaging-and-integration"></a>Обмен сообщениями и интеграция
Независимо от того, что он делает код должен часто toointeract с другим кодом.  В некоторых ситуациях достаточно обычного обмена сообщениями с организацией очереди. В других случаях требуются более сложные механизмы взаимодействия. Azure предоставляет несколько различных способов toosolve этих проблем. Рис. 5 показан порядок hello.

### <a name="queues"></a>Очереди
![Ретрансляция служебной шины Azure](./media/fundamentals-introduction-to-azure/QueuesIntroNew.png)

*Рис. Очереди позволяют ослабить связь между компонентами приложения и упростить масштабирование*  

Идея организации очереди довольно проста: одно приложение помещает в очередь сообщение, которое в конечном итоге считывается другим приложением. Если приложению только этой простой службы очередей Azure может быть лучшим вариантом hello.

Из-за hello способом приветствия Azure увеличился со временем очереди хранилища Azure и очереди Service Bus, обеспечивающие аналогичную очереди. в техническом документе hello рассматриваются Hello причин, почему нужно toouse один над hello других [очереди Azure и очереди шины обслуживания — сходство и отличия](http://msdn.microsoft.com/library/azure/hh767287.aspx).  В большинстве сценариев можно использовать обе службы.

**Сценарии очередей**

Одна распространенное применение очередей на сегодняшний день является toolet экземпляре веб-роли взаимодействуют экземпляр рабочей роли в рамках hello одного приложения облачных служб.

Например, предположим, что вы создаете приложение Azure для общего доступа к видео. приложение Hello состоит из кода PHP в веб-роли, в который загружен позволяет пользователям отправить и видеозаписи, а также рабочей роли, реализованную в C#, который преобразует видео в различных форматах.

Когда новое видео экземпляре веб-роли пользователя, его можно сохранить hello видео в большой двоичный объект, а затем отправить рабочей роли tooa сообщений через очереди, указав, где этот новый toofind видео. Рабочей роли экземпляра ИТ-не имеет значения, какое сообщение hello один будет затем чтения из очереди hello и выполнять необходимые hello видео переводов в фоновом режиме hello.

Структурирование приложения таким образом позволяет асинхронную обработку, а также делает hello приложения tooscale проще, поскольку hello число экземпляров веб-роли и экземпляры рабочих ролей, которые могут изменяться независимо друг от друга. Размер очереди hello также можно использовать как триггер tooscale hello количество рабочих ролей вверх и вниз. При слишком большом количестве добавляются дополнительные роли. Когда он получает ниже, можно уменьшить hello число ролей toosave money.  

Можно использовать один и тот же структурный элемент во множестве различных частей приложения, даже если они не используют веб-роли и рабочие роли.  Позволяет tooscale hello частей на обеих сторонах очереди hello вверх и вниз как спрос и требует времени обработки.

### <a name="service-bus"></a>Служебная шина
Является ли они выполняются в облаке hello, в вашем центре обработки данных на мобильном устройстве или в другом месте, приложения должны toointeract. Цель Hello шины обслуживания Azure — toolet запущенное практически в любом месте обмена данными.

В дополнение toohello очереди (один к одному) описано выше Service Bus также предоставляет tooother способы связи.

#### <a name="service-bus-relay"></a>Ретрансляция служебной шины
![Ретрансляция служебной шины Azure](./media/fundamentals-introduction-to-azure/ServiceBusRelayIntroNew.png)

*Рис. Ретранслятор служебной шины обеспечивает обмен данными между приложениями, которые находятся по разные стороны брандмауэра*

Service Bus обеспечивает прямую связь через свою службу ретрансляции, предоставляя toointeract безопасным способом через брандмауэры. Ретранслятор Service Bus включить toocommunicate приложений путем обмена сообщениями через конечную точку, размещенной в облаке hello, а не локально.

**Сценарии ретрансляции служебной шины**

Приложения, взаимодействующие через Service Bus, могут быть приложениями Azure или программным обеспечением, выполняемым на другой облачной платформе. Они также могут быть приложений, работающих за пределами облака hello, однако. Например, представьте авиакомпанию, службы резервирования которой реализованы на компьютерах, расположенных в ее центре обработки данных. Hello авиакомпании должен tooexpose toomany этих служб клиентов, включая киоски возврата в аэропортах, терминалы резервирование агента и может быть даже пользователи телефонов. Он может использовать Service Bus toodo это создание слабосвязанных взаимодействиями hello различных приложений.

#### <a name="service-bus-topics-and-subscriptions"></a>Разделы и подписки служебной шины
![Разделы служебной шины Azure](./media/fundamentals-introduction-to-azure/ServiceBusTopicsSubsIntroNew.png)   
 *Рисунок: Разделы шины обслуживания позволяет несколько сообщений toopost приложений и других сообщений tooreceive toosubscribe приложений, которые удовлетворяют определенным условиям.*

Служебная шина предоставляет механизм публикации и подписки, который называется "Разделы и подписки". Спектром приложение может отправлять tooa темы о сообщениях, пока подписки toothis тему можно создать другие приложения. Это позволяет один ко многим связи между набором приложений, позволяя hello же сообщение быть прочитаны несколько адресов электронной почты.

**Сценарии использования разделов и подписок служебной шины**

Каждый раз, когда вы настраиваете много сообщений, которые важны, когда различных подчиненных систем необходим только подмножества toodiffering toolisten обменом, шины обслуживания и подписок — это хороший вариант.

### <a name="biztalk-services"></a>Службы BizTalk
![Службы BizTalk](./media/fundamentals-introduction-to-azure/BizTalkServicesIntroNew.png)   
 *Рисунок: BizTalk Services предоставляет форматы сообщений XML tootransform возможность hello в облаке hello.*

Иногда требуется подключить системы, которые используют для взаимодействия разные форматы сообщений. Чаще всего для бизнеса toohave различные схемы баз данных и XML-сообщениями форматы, даже в том случае, если доступен общий стандарт. Чем писать пользовательский код, можно использовать toointegrate BizTalk Server в локальной среде различных систем.  Службы BizTalk Azure предоставляет hello одного типа службы, но в облаке hello. Оплатить только что можно использовать, не беспокоясь о шкалы как пришлось бы tooon организациями.

**Сценарии служб BizTalk**

Для взаимодействия между организациями (B2B) обычно требуется именно этот тип преобразования.  Например компания созданию потребностей самолетах tooorder частей из него — поставщики различных частей. Будет задействовано множество поставщиков частей.  Для этих заказов должно быть автоматизированные toogo непосредственно из систем построители hello "в самолете" в систему suppliers hello.  Ни одна из бизнеса хочет toochange их основных компонентов системы и форматы сообщений и крайне маловероятно, что эти форматы являются hello таким же. Службы BizTalk можно воспользоваться сообщений и преобразование между hello новые форматы обоими способами. Либо поставщик hello "в самолете" hello tootranslate рабочих или hello различные поставщики могут, в зависимости от того, кто хочет дополнительные hello и управления объем необходимые преобразования.     

## <a name="compute-assistance"></a>Помощь в вычислении
Azure предоставляет помощь для служб, которые не обязательно toorun все время hello.  

### <a name="scheduler"></a>Планировщик
![Планировщик Azure](./media/fundamentals-introduction-to-azure/SchedulerIntroNew.png)   
*Рисунок: Планировщик Azure предоставляет tooschedule способом задания в определенное время, в течение определенного периода времени.*

Иногда приложений достаточно toorun в определенное время. В Azure можно сэкономить с помощью такого приложения вместо просто продолжать работать 24 x 7 ожидание tooprocess данных приложения. Планировщик Azure позволяет tooschedule, когда приложение следует запускать на основании интервала времени и календаря. Он надежен и проверит, что процесс запускается даже при сбое сети, компьютера и центра обработки данных. Используйте API REST планировщика toomanage hello эти действия.

В случае запланированного сигнала планировщика отправляет HTTP или HTTPS сообщения tooa конкретной конечной точки, или можно поместить сообщение в очередь хранилища.  Поэтому требуется toohave приложения есть конечная точка доступа или его мониторинг очереди службы хранилища. Затем, когда он получает сообщение hello, он может выполнять любое действие, он запрограммирован на.

**Сценарии планировщика**

* Выполняемые регулярно действия приложений: например, служба периодически может получать данные из twitter и собрать данные hello в обычный веб-канал.
* Ежедневное обслуживание. Обработка или удаление журналов, создание резервных копий и другие периодические задачи планирования.
* Задачи, которые выполняются ночью.
* Задачи веб-приложения, такие как ежедневное удаление журналов, создание резервных копий и другие задачи обслуживания. Администратор может выбрать toobackup своей базе данных в 13: 00 каждый день в течение hello следующих 9 месяцев, например.

Hello API планировщика позволяет toocreate, обновить, удалить, просмотреть и программно управлять коллекций заданий и запланированные задания.

## <a name="performance"></a>Производительность
Производительность всегда важна в приложении. Приложения имеют тенденцию tooaccess hello одни и те же данные. Одним из способов tooimprove производительности tookeep копию данных ближе toohello приложения, минимизации hello время, необходимое tooretrieve его. Для этого Azure предоставляет разные службы.

### <a name="azure-caching"></a>Azure Caching
![Кэширование Azure](./media/fundamentals-introduction-to-azure/AzureCacheIntroNew.png)   
 **Рисунок. Приложение Azure может кэшировать данные в памяти и даже разделять их на несколько рабочих ролей.**

Доступ к данным, сохраненным в любой из служб управления данными Azure (база данных SQL, таблицы или BLOB-объекты), осуществляется довольно быстро. Однако доступ к данным, находящимся в памяти, осуществляется еще быстрее. В связи с этим наличие в памяти копии часто используемых данных может улучшить производительность приложения. Вы можно использовать toodo кэширование Azure в памяти.

Приложения облачных служб можно хранить данные в кэше, а затем извлечь его непосредственно, без необходимости постоянного хранения tooaccess. Hello кэша может поддерживаться исключительно toocaching выделенный внутри приложения, виртуальные машины или предоставляемые виртуальных машин. В любом случае hello кэша могут распространяться, с данными hello в ней распределенный между несколькими виртуальными машинами в центре обработки данных Azure.

В Azure существует несколько разных технологий кэша, которые изменились со временем. В порядке hello, они появились, имеется общий, роли, управляются и кэша Redis. кэширование общих Hello — устаревшая технология и с ним нельзя создавать новые реализации. Hello управляемый кэш имеет hello же возможности hello в роли кэша, так как службу вне hello портала управления Azure. Hello кэша Redis представляет собой предварительную версию. Реализация Redis Hello максимальным количеством функций hello и рекомендуется использовать при написании нового кода кэширования.

**Сценарии для кэша Azure**

Приложения, несколько раз считывает каталог продуктов может повыситься при использовании с помощью такого рода кэширования, например, с момента hello данных требуется сделает его доступным быстрее. Технология Hello также поддерживает блокировку, он будет использоваться для чтения и записи, а также данные, доступные только для чтения. И приложений ASP.NET могут использовать данные сеанса toostore hello службы с изменением конфигурации.

### <a name="content-delivery-network"></a>Сеть доставки содержимого
![Azure CDN](./media/fundamentals-introduction-to-azure/CDNIntroNew.png)   
 **Рисунок: Копии большого двоичного объекта может быть кэширован на сайтах вокруг Здравствуй, мир.**

Предположим, что необходимы данные большого двоичного объекта toostore, будут доступны для пользователей по всему Здравствуй, мир. Возможно, это видео hello последнюю мира совпадения, например, обновления драйверов или популярных электронных книг. Служит для хранения копии данных hello в нескольких центрах обработки данных Azure, но при наличии большого количества пользователей, это, скорее всего, не достаточно. Для еще более высокой производительностью можно использовать hello Azure CDN.

Hello CDN содержатся десятки сайтов вокруг Здравствуй, мир, каждый может хранить копии больших двоичных объектов Azure. Hello первый раз пользователь в некоторой части Здравствуй, мир! обращается к отдельного большого двоичного объекта hello сведения, содержащиеся в нем копируется из центра обработки данных Azure в локальное хранилище CDN в этом регионе. После этого доступ из этой части Здравствуй, мир! будет использоваться кэшированный в hello CDN копирования BLOB-объектов hello-их не нужно toogo все toohello способом hello ближайший центр обработки данных Azure. Hello результатом является более быстрый доступ к данным toofrequently доступ к пользователями в любом месте Здравствуй, мир.

**Сценарии CDN**

Это общие toouse CDN с помощью служб мультимедиа toodeliver видео по всему миру. Видео обычно большие и требуют большой пропускной способности.  Сведения о службах мультимедиа см. далее на этой странице.

## <a name="big-data-and-big-compute"></a>Большие данные и большие вычисления
### <a name="hdinsight-hadoop"></a>HDInsight (Hadoop)
![HDInsight](./media/fundamentals-introduction-to-azure/HDInsightIntroNew.png)   
 **Рисунок: HDInsight помогает обеспечивать hello массовой обработки большого объема данных**

В течение многих лет hello массового анализа данных выполняются на реляционных данных в хранилище данных, построенные с помощью реляционной СУБД. Такого рода бизнес-аналитика по-прежнему важен, и будет относиться к toocome много времени. Но что делать, если hello данные tooanalyze так велико, что реляционные базы данных просто не может обрабатывать его? И Предположим, что данные hello не реляционных? Это могут быть серверные журналы в центре обработки данных, полученные с датчиков данные об имевших место событиях или что-то еще. В таких случаях вы сталкиваетесь с так называемой проблемой большого объема данных. Вам требуется использовать иной подход.

Технология главным Hello сегодня для анализа большие наборы данных — Hadoop. Apache с открытым исходным кодом, эта технология занимают hello системы распределенных файла Hadoop (HDFS), после чего позволяет разработчикам создавать tooanalyze задания MapReduce этих данных. HDFS распределяет данные на нескольких серверах, то блоки запусков задания MapReduce hello на каждой из них, позволяя hello больших данных выполняться параллельно.

HDInsight — hello имя службы на основе Apache Hadoop hello Azure. HDInsight позволяет HDFS хранения данных в кластере hello и распределяют несколько виртуальных машин. Она также распространяет логику hello задания MapReduce в этих виртуальных машинах. Так же, как с Hadoop в локальной среде данных обработанные локально hello логика и hello данных предназначен для работы находятся в одной виртуальной Машины Здравствуйте,- и параллельно для повышения производительности. HDInsight также может хранить данные в хранилище Azure Storage Vault (ASV), которое использует BLOB-объекты.  Использование ASV позволяет toosave money так, как удалить кластер HDInsight не во время работы, но по-прежнему хранить данные в облаке hello.

HDinsight поддерживает hello экосистемы Hadoop, а также другие компоненты, включая Hive и Pig. Корпорация Майкрософт создала также компоненты, которые упрощают проще toowork с данными, полученных при HDInsight с помощью традиционных средств бизнес-Аналитики, такие как адаптер HiveODBC hello и обозреватель данных, работать с Excel.

### <a name="high-performance-computing-big-compute"></a>Высокопроизводительные вычислительные системы (большие вычисления)
Одним из наиболее привлекательный hello toouse способов облачной платформы является toorun высокопроизводительных вычислительных систем (HPC) и других приложений, «Больших вычислений». Примеры включают специализированные toouse проектирования приложений, построенных hello интерфейс передачи стандартных сообщений (MPI), а также так называемые неудобно параллельные приложения, такие модели финансовых рисков.

Hello суть больших вычислений выполнение кода на многих компьютерах в hello то же время. В Azure это означает одновременной много виртуальных машин все работы в параллельных toosolve проблему. Для этого потребуется некоторые способом tooresources и tooschedule приложения, т. е. toodistribute свою работу между этими экземплярами. Пакет HPC свободного корпорации Майкрософт и других решений вычислительного кластера можно выполнять в Azure, преимуществ Azure вычислений и служб инфраструктуры tooadd емкости по требованию tooan локальный вычислительный кластер или приложения для больших вычислений полностью в hello облако.

Azure предоставляет диапазон виртуальных Машин размеров экземпляров с различными конфигурациями количества ядер ЦП, памяти, емкости диска и другие характеристики toomeet hello требованиям различных приложений. Привет, недавно представленных A8 и A9 экземпляров хорошо подходят для многих вычислений интенсивных рабочих нагрузок и параллельных приложений MPI в частности, поскольку они имеют быстродействующие, многоядерные процессоры и большой объем памяти. В некоторых конфигурациях экземпляров hello воспользоваться преимуществами приложений с низкой задержкой и высокой пропускной способностью сети в облаке hello, включает в себя технологии удаленного памяти (RDMA) доступ для максимальной эффективности параллельных приложений MPI.

Azure также предлагает разработчикам приложений больших вычислений и партнерам полный набор возможностей вычислений, служб, вариантов архитектуры и средств разработки. Azure поддерживает настраиваемые больших вычислений рабочие процессы, включающие специализированные рабочие процессы данных и заданий и задач, шаблонов, которые можно масштабировать toothousands из планирования вычислительные ядра.

## <a name="media"></a>Служба мультимедиа
![Службы мультимедиа Azure](./media/fundamentals-introduction-to-azure/MediaServicesIntroNew.png)   
 **Рисунок: Media Services — это платформа для приложений, которые предоставляют видео и другие tooclients media вокруг Здравствуй, мир.**

Сегодня видео составляют большую часть интернет-трафика, а завтра этот процент будет еще больше. Еще предоставления видео на веб-hello не является простой. Существует множество переменных, например hello кодирования алгоритм и hello отображения разрешения экрана приветствия пользователя. Видео также обычно toohave всплесков запросу как пик ночь суббота при большое число пользователей решить, что хотелось бы toowatch online фильма.

Учитывая популярность видео, многие новые приложения наверняка будут создаваться с его использованием. Пока все из них потребуется toosolve некоторые из hello же проблемы и внесения каждый из этих проблем сам по себе не имеет никакого смысла. Оптимальный подход — это платформа, которая предоставляет общие решения для многих приложений toouse toocreate. И построение этой платформы в облаке hello имеет некоторые явные преимущества. Может быть широко доступны на основе по мере использования и также может обрабатывать hello разброса видеоприложения часто сталкиваются спроса.

Службы мультимедиа Azure устраняют эту проблему. Они предоставляет набор облачных компонентов, которые упрощают создание и запуск приложений, использующих видео и мультимедиа.

Как показано на рисунке hello, Media Services предоставляет набор компонентов для приложений, работающих с видео и другие файлы мультимедиа. Например, он включает носителей приема компонент tooupload видео в служб мультимедиа (где они хранятся в BLOB-объектов Azure), это кодирования компонент, который поддерживает различные форматы видео- и аудиопотоки, компонент защиты содержимого, который обеспечивает управление цифровыми правами компонент, для вставки рекламы в видео, компоненты для потоковой передачи и многое другое. Партнеров Майкрософт можно также предоставить компоненты для платформы hello, а затем имеют Microsoft распространять эти компоненты и выставления счетов от их имени.

Приложения, использующие эту платформу, можно запускать в Azure или в другом месте. Например приложением рабочего стола для house видеофайл могут допускать своим пользователям отправить видео tooMedia службы, затем обработать его различными способами. Кроме того служба управления содержимым на основе облака, работающих в Azure может полагаться на tooprocess служб мультимедиа и распространение видео. Везде, где он работает, и все, что это происходит, каждое приложение выбирает компоненты, которые он должен toouse, доступа к ним через интерфейсы RESTful.

toodistribute то, что он создает, приложение может использовать hello Azure CDN другой CDN или просто отправить непосредственно bits toousers. Независимо от способа доставки, видео, созданные с помощью служб мультимедиа, можно размещать на различных клиентских системах, включая Windows, Macintosh, HTML 5, iOS, Android, Windows Phone, Flash и Silverlight. Задача Hello — toomake его проще toocreate современных мультимедийных приложений.

**Справочные материалы**

Для более наглядный представление о принципах работы служб мультимедиа, загрузите hello [плакат службы мультимедиа Azure][Azure Media Services Poster].

## <a name="commerce"></a>Приложения для коммерции
Подъем Hello программное обеспечение как услуга преобразования, как мы создания приложений. Она также изменяет то, как мы продаем приложения. Поскольку приложения SaaS находятся в облаке hello, имеет смысл, его потенциальных заказчиков следует выполнить поиск решений в Интернете. И это изменение относится toodata, а также tooapplications. Почему не следует людей искать toohello облака коммерчески доступных наборов данных? Корпорация Майкрософт участвует оба эти вопросы, связанные с hello [Azure Marketplace](https://azure.microsoft.com/marketplace/).

![Коммерческие предложения Azure](./media/fundamentals-introduction-to-azure/CommerceIntroNew.png)   
 **Рисунок. В Azure Marketplace и магазине Azure можно найти и купить приложения Azure и коммерческие наборы данных, чтобы использовать их в рамках приложений Azure.**

Hello разницу между двумя hello — Marketplace выходит за пределы hello портала управления Azure, что хранилище hello может осуществляться из внутри hello портала. Потенциальным клиентам выполнять поиск toofind Azure приложениях, которые соответствуют их требованиям. Клиенты также могут искать коммерческие наборы данных, включая демографические, финансовые, географические данные и многое другое. При их найти то, что угодно, они могут получить доступ к его из поставщика hello напрямую через hello Marketplace или хранилище веб-сайтах или в некоторых случаях из hello портала управления. Приложения также могут использовать hello API поиска Bing через hello Marketplace, позволяя результаты toohello доступа к поиску в Интернете.

**Коммерческие сценарии**

SendGrid представляет собой приложение hello хранилища Azure, позволяющая toosend электронной почты. В нем предлагаются такие дополнительные функции, как надежная доставка и статистика.  Можно приобрести этого приложения и связанные службы вместо попробуйте toobuild такой инфраструктуры самостоятельно.  

## <a name="getting-started"></a>Приступая к работе
Теперь, когда имеется hello рисунками, hello следующим шагом является toowrite первого приложения Azure. Выберите свой язык [получить hello соответствующего пакета SDK](/downloads/)и повторите попытку. Облачные вычисления — новое и значение по умолчанию hello--начните прямо сейчас.

[Azure Media Services Poster]: http://azure.microsoft.com/documentation/infographics/media-services/
