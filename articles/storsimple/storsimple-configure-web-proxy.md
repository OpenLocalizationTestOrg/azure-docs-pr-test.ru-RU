---
title: "aaaSet копирование веб-прокси для устройства StorSimple | Документы Microsoft"
description: "Узнайте, как toouse Windows PowerShell для StorSimple tooconfigure веб-параметры прокси-сервера для устройства StorSimple."
services: storsimple
documentationcenter: 
author: alkohli
manager: carmonm
editor: 
ms.assetid: 6c2ca351-a7c6-4da6-ab5e-c081e6d08261
ms.service: storsimple
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: na
ms.date: 08/17/2016
ms.author: alkohli
ms.openlocfilehash: c0213eb2a1902ff994147bb16593f0ff300eb70c
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="configure-web-proxy-for-your-storsimple-device"></a>Настройка веб-прокси для устройства StorSimple
## <a name="overview"></a>Обзор
В данном учебнике как toouse Windows PowerShell для StorSimple tooconfigure и представление веб-параметры прокси-сервера для устройства StorSimple. При взаимодействии с облаком hello Hello параметры веб-прокси используются hello устройства StorSimple. Прокси-сервера веб-сервере — используется tooadd еще один уровень безопасности, фильтрации содержимого, кэшировать tooease пропускной способности или даже справки с помощью анализа.

Веб-прокси представляет собой вариант конфигурации устройства StorSimple. Настроить веб-прокси можно только через Windows PowerShell для StorSimple. Hello конфигурации состоит из двух этапов следующим образом:

1. Сначала настроить параметры веб-прокси приветствия мастера установки или Windows PowerShell для StorSimple командлетов.
2. Включите hello настроены параметры веб-прокси через Windows PowerShell для StorSimple командлетов.

После завершения настройки веб-прокси hello можно просмотреть параметры веб-прокси настроен hello в службы Microsoft Azure StorSimple Manager hello и hello Windows PowerShell для StorSimple. 

После изучения этого учебника вы сможете сделать следующее.

* настраивать веб-прокси с помощью мастера установки и командлетов;
* включать веб-прокси с помощью командлетов;
* Просмотр параметров веб-прокси в hello классический портал Azure
* устранять ошибки во время конфигурации веб-прокси;

## <a name="configure-web-proxy-via-windows-powershell-for-storsimple"></a>настраивать веб-прокси с помощью Windows PowerShell для StorSimple.
Можно использовать любой из hello следующие параметры tooconfigure веб-прокси:

* Настройка tooguide мастера можно выполнить с помощью действия по настройке hello.
* Командлеты Windows PowerShell для StorSimple.

Каждый из этих методов рассматривается в следующих разделах hello.

## <a name="configure-web-proxy-via-hello-setup-wizard"></a>Настройка веб-прокси через приветствия мастера установки
Можно использовать tooguide мастер установки hello, можно выполнить с помощью hello шаги настройки веб-прокси. Выполните следующие шаги tooconfigure веб-прокси на устройстве hello.

#### <a name="tooconfigure-web-proxy-via-hello-setup-wizard"></a>веб-прокси tooconfigure через приветствия мастера установки
1. В меню последовательной консоли hello, выберите параметр 1, **войти с полным доступом** и предоставляют hello **пароль администратора устройства**. Тип hello следующая команда toostart сеанс мастера установки:
   
    `Invoke-HcsSetupWizard`
2. Если это hello впервые, использовался приветствия мастера установки для регистрации устройств, необходимо будет tooconfigure все параметры сети требуется hello пока не достигнете hello конфигурацию веб-прокси. Если устройство уже зарегистрировано, может принимать все hello настроены параметры сети, пока не достигнете hello конфигурацию веб-прокси. При tooconfigure запрос веб-параметры прокси-сервера, введите в приветствия мастера установки, **Да**.
3. Для hello **URL-адрес прокси-сервера**, укажите hello IP-адрес или полное доменное имя (FQDN) вашего веб прокси сервера и hello номер порта TCP, желательно toouse вашего устройства, при взаимодействии с облаком hello hello. Hello используйте следующий формат:
   
    `http://<IP address or FQDN of hello web proxy server>:<TCP port number>`
   
    По умолчанию указан номер TCP-порта 8080.
4. Выберите тип проверки подлинности hello как **NTLM**, **основные**, или **нет**. Basic hello наименее безопасная проверка подлинности для hello конфигурацию прокси-сервера. NT LAN Manager (NTLM) является наиболее безопасным и комплексным проверки подлинности протоколом, который используется трехстороннее система обмена сообщениями (иногда четырехстороннюю, если требуется повышенная целостность) tooauthenticate пользователя. Проверка подлинности по умолчанию Hello — NTLM. Дополнительные сведения см. в следующих ресурсах, посвященных [базовой аутентификации](http://hc.apache.org/httpclient-3.x/authentication.html) и [аутентификации NTLM](http://hc.apache.org/httpclient-3.x/authentication.html). 
   
   > [!IMPORTANT]
   > **В hello в службе StorSimple Manager hello диаграммы мониторинга устройства не работают, если базовый или проверка подлинности NTLM включена в hello конфигурацию прокси-сервера для hello устройства. Для наблюдения за toowork диаграммы hello, вам потребуется tooensure подлинности должна задаваться tooNONE.**
   > 
   > 
5. При использовании аутентификации укажите **имя пользователя веб-прокси** и **пароль веб-прокси**. Также потребуется пароль tooconfirm hello.
   
    ![Настройка веб-прокси на устройстве StorSimple 1](./media/storsimple-configure-web-proxy/IC751830.png)

Если вы регистрируете устройство для hello первый раз, продолжите hello регистрации. Если устройство уже зарегистрировано, hello мастер завершит работу. Hello настроены параметры будут сохранены.

Веб-прокси также будет включен. Вы можете пропустить hello [Включение веб-прокси](#enable-web-proxy) шаг и перейти непосредственно слишком[просматривать параметры веб-прокси в классический портал Azure hello](#view-web-proxy-settings-in-the-azure-classic-portal).

## <a name="configure-web-proxy-via-windows-powershell-for-storsimple-cmdlets"></a>Настройка веб-прокси с помощью Windows PowerShell для StorSimple
Параметры веб-прокси альтернативный способ tooconfigure осуществляется через hello Windows PowerShell для StorSimple командлетов. Выполните следующие шаги tooconfigure веб-прокси hello.

#### <a name="tooconfigure-web-proxy-via-cmdlets"></a>веб-прокси tooconfigure через командлеты
1. В меню последовательной консоли hello, выберите параметр 1, **войти с полным доступом**. При появлении запроса укажите hello **пароль администратора устройства**. пароль по умолчанию Hello — `Password1`.
2. Hello командной строки введите следующую команду:
   
    `Set-HcsWebProxy -Authentication NTLM -ConnectionURI "<http://<IP address or FQDN of web proxy server>:<TCP port number>" -Username "<Username for web proxy server>"`
   
    Укажите и подтвердите пароль hello в ответ на запрос, как показано ниже.
   
    ![Настройка веб-прокси на устройстве StorSimple 3](./media/storsimple-configure-web-proxy/IC751831.png)

веб-прокси Hello теперь настроен и должен toobe включена.

## <a name="enable-web-proxy"></a>Включение веб-прокси
По умолчанию веб-прокси отключен. После настройки параметров hello веб-прокси на устройстве StorSimple, требуется hello toouse Windows PowerShell для параметров StorSimple tooenable hello веб-прокси.

> [!NOTE]
> **Этот шаг не потребуется, если вы использовали hello tooconfigure мастер настройки веб-прокси. По умолчанию веб-прокси автоматически включается после завершения работы мастера.**
> 
> 

Выполните hello, выполните действия в Windows PowerShell для StorSimple tooenable веб-прокси на устройстве.

#### <a name="tooenable-web-proxy"></a>tooenable веб-прокси
1. В меню последовательной консоли hello, выберите параметр 1, **войти с полным доступом**. При появлении запроса укажите hello **пароль администратора устройства**. пароль по умолчанию Hello — `Password1`.
2. Hello командной строки введите следующую команду:
   
    `Enable-HcsWebProxy`
   
    Теперь вы включили hello конфигурацию веб-прокси на устройстве StorSimple.
   
    ![Настройка веб-прокси на устройстве StorSimple 4](./media/storsimple-configure-web-proxy/IC751832.png)

## <a name="view-web-proxy-settings-in-hello-azure-classic-portal"></a>Просмотр параметров веб-прокси в hello классический портал Azure
параметры прокси-сервера веб-Hello настраиваются через интерфейс Windows PowerShell hello и нельзя изменять в классический портал hello. Тем не менее, вы можете просмотреть эти настроенные параметры hello классического портала. Выполните следующие шаги tooview веб-прокси hello.

#### <a name="tooview-web-proxy-settings"></a>Параметры tooview веб-прокси
1. Перейдите в слишком**в службе StorSimple Manager > устройства**. Выберите и щелкните устройство и перейдите слишком**Настройка**.
2. Прокрутите вниз hello **Настройка** страницы слишком**параметры прокси-сервера веб-** раздела. Можно просмотреть hello настроены параметры веб-прокси на устройстве StorSimple, как показано ниже.
   
    ![Просмотр веб-прокси на портале управления](./media/storsimple-configure-web-proxy/ViewWebProxyPortal_M.png)

## <a name="errors-during-web-proxy-configuration"></a>Ошибки во время конфигурации веб-прокси
Если параметры hello веб-прокси настроены неправильно, сообщения об ошибках будут отображаемых toohello пользователя в Windows PowerShell для StorSimple. Привет, в следующей таблице описаны некоторые эти сообщения об ошибках, возможные причины и рекомендуемые действия.

| Номер | Код ошибки HRESULT | Возможная основная причина | Рекомендуемое действие |
|:--- |:--- |:--- |:--- |
| 1. |0x80070001 |Команда выполняется из пассивного контроллера hello и не может toocommunicate с активным контроллером hello. |Выполните команду hello на активном контроллере hello. toorun hello команду из пассивного контроллера hello, вам потребуется подключения hello toofix tooactive пассивный контроллер. Если подключение будет прервано, потребуется tooengage технической поддержки Майкрософт. |
| 2. |0x800710dd — идентификатор операции hello не является допустимым |Параметры прокси не поддерживаются на виртуальном устройстве StorSimple. |Параметры прокси не поддерживаются на виртуальном устройстве StorSimple. Их можно задать только на физическом устройстве StorSimple. |
| 3. |0x80070057 — неверный параметр |Один из параметров hello, указанных для hello параметры прокси-сервера является недопустимым. |Hello URI не указан в неправильном формате. Hello используйте следующий формат:`http://<IP address or FQDN of hello web proxy server>:<TCP port number>` |
| 4. |0x800706ba — сервер RPC недоступен |Hello основной причиной является одна из следующих hello:</br></br>Кластер не функционирует.</br></br>Служба пути к данным не запущена.</br></br>Hello команду из пассивного контроллера, и он не может toocommunicate с активным контроллером hello. |Проверьте охват tooensure технической поддержки Майкрософт, hello кластера запущен и работает служба пути к данным.</br></br>Выполните команду hello из активного контроллера hello. Если требуется, чтобы toorun hello команду из пассивного контроллера hello, вам потребуется tooensure hello пассивный контроллер может взаимодействовать с активным контроллером hello. Если подключение будет прервано, потребуется tooengage технической поддержки Майкрософт. |
| 5. |0x800706be — не удалось выполнить вызов RPC |Кластер не работает. |Обратитесь в tooensure, hello кластер работает технической поддержки Майкрософт. |
| 6. |0x8007138f — ресурс кластера не найден. |Ресурс кластера службы платформы не найден. Это может произойти, если установка hello была не правильной. |Может потребоваться tooperform к заводским настройкам на устройстве. Может потребоваться toocreate ресурса платформы. Обратитесь в службу поддержки Майкрософт, чтобы узнать, что делать дальше. |
| 7. |0x8007138c — ресурс кластера отключен от сети. |Ресурсы платформы или кластера пути данных отключены от сети. |Toohelp обратитесь в службу поддержки Майкрософт убедитесь, что hello пути к данным и платформы службы находятся в сети. |

> [!NOTE]
> * Hello над списком сообщений об ошибках не является исчерпывающим. 
> * Параметры прокси-сервера tooweb связанных ошибок не будет отображаться в hello классический портал Azure в службе StorSimple Manager. Если после завершения настройки hello имеется проблема с веб-прокси, состояние устройства hello изменится слишком**Offline** классическом портале hello. |
> 
> 

## <a name="next-steps"></a>Дальнейшие действия
* Если возникают проблемы при развертывании устройства или Настройка параметров веб-прокси, см. слишком[Устранение неполадок развертывания устройства StorSimple](storsimple-troubleshoot-deployment.md).
* как toouse hello в службе StorSimple Manager перейдите слишком toolearn[используйте hello tooadminister службы диспетчера StorSimple устройство StorSimple](storsimple-manager-service-administration.md).

