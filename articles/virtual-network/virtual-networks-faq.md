---
title: "часто задаваемые вопросы о виртуальной сети aaaAzure | Документы Microsoft"
description: "Ответы toohello наиболее часто задаваемые вопросы о виртуальных сетей Microsoft Azure."
services: virtual-network
documentationcenter: na
author: jimdial
manager: timlt
editor: tysonn
ms.assetid: 54bee086-a8a5-4312-9866-19a1fba913d0
ms.service: virtual-network
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: infrastructure-services
ms.date: 01/18/2017
ms.author: jdial
ms.openlocfilehash: c2f9faee41b9c45e8bb196c58282d597ae732e54
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="azure-virtual-network-frequently-asked-questions-faq"></a>Виртуальная сеть Azure: часто задаваемые вопросы

## <a name="virtual-network-basics"></a>Основные сведения о виртуальных сетях

### <a name="what-is-an-azure-virtual-network-vnet"></a>Что представляет собой виртуальная сеть Azure?
Виртуальная сеть Azure (VNet) — это представление сети в облаке hello. Это логическое изоляции hello подписки tooyour выделенное облако Azure. Можно использовать tooprovision виртуальных сетей и обслуживать виртуальные частные сети (VPN) в Azure и при необходимости hello связь виртуальных сетей с других виртуальных сетей в Azure или в локальной решения ИТ-инфраструктурой toocreate гибридный или между организациями. Каждой виртуальной сети, созданные блок CIDR и может быть связанного tooother виртуальных сетей и в локальной сети, при условии, что блоки CIDR hello не перекрываются. Также имеется контроль над параметры DNS-сервера для виртуальных сетей и сегментацию hello виртуальной сети на подсети.

Вы можете использовать виртуальные сети для указанных ниже целей.

* Создание выделенной частной облачной виртуальной сети. Иногда для решения не нужна распределенная конфигурация. При создании виртуальной сети, служб и виртуальных машин в виртуальной сети могут взаимодействовать напрямую и безопасно друг с другом в облаке hello. Это безопасно хранить трафик в hello виртуальной сети, но по-прежнему допускает tooconfigure конечной точки подключения для hello виртуальных машин и служб, которые требуется подключение к Интернету в рамках решения.
* Безопасным образом расширить центр обработки данных с помощью виртуальных сетей, можно построить традиционных сеть сеть VPN (S2S) toosecurely шкалы емкости центра обработки данных. VPN S2S с помощью IPSEC tooprovide безопасного соединения между корпоративным шлюзом VPN и Azure.
* Включение гибридных облачных средах, виртуальные сети обеспечивают hello гибкость toosupport диапазон гибридных облачных средах. Вы можете надежно подключать облачные приложения, тип tooany в локальной системе в том числе большим ЭВМ и системам Unix.

### <a name="how-do-i-know-if-i-need-a-vnet"></a>Как узнать, требуется ли виртуальная сеть?
Hello [Обзор виртуальной сети](virtual-networks-overview.md) также приводятся таблицей решений, которая поможет вам подобрать hello наилучший вариант сети для вас.

### <a name="how-do-i-get-started"></a>Как начать работу?
Посетите hello [виртуальной сети документации](https://docs.microsoft.com/azure/virtual-network/) tooget работы. Здесь представлена информация Обзор и развертывания для всех функций hello виртуальной сети.

### <a name="can-i-use-vnets-without-cross-premises-connectivity"></a>Можно ли использовать виртуальные сети без распределенных подключений?
Да. Вы можете использовать виртуальную сеть без гибридных подключений. Это особенно полезно в том случае, если требуется toorun контроллеры домена Microsoft Windows Server Active Directory и фермы SharePoint в Azure.

### <a name="can-i-perform-wan-optimization-between-vnets-or-a-vnet-and-my-on-premises-data-center"></a>Можно ли выполнять оптимизацию глобальной сети между виртуальными сетями или между виртуальной сетью и локальным центром обработки данных?

Да. Можно развернуть [ГЛОБАЛЬНОЙ оптимизации сети виртуальной машины](https://azure.microsoft.com/marketplace/?term=wan+optimization) из нескольких поставщиков через hello Azure Marketplace.

## <a name="configuration"></a>Конфигурация

### <a name="what-tools-do-i-use-toocreate-a-vnet"></a>Средства следует использовать toocreate виртуальной сети?
Можно использовать следующие средства toocreate hello или настройки виртуальной сети.

* Портал Azure (для классических виртуальных сетей и виртуальных сетей Resource Manager).
* Файл конфигурации сети (netcfg — только для классических виртуальных сетей). В разделе hello [настройки виртуальной сети с помощью файла конфигурации сети](virtual-networks-using-network-configuration-file.md) статьи.
* PowerShell (для классических виртуальных сетей и виртуальных сетей Resource Manager).
* Azure CLI (для классических виртуальных сетей и виртуальных сетей Resource Manager).

### <a name="what-address-ranges-can-i-use-in-my-vnets"></a>Какие диапазоны адресов можно использовать в виртуальных сетях?
Любой диапазон IP-адресов, определенный в [RFC 1918](http://tools.ietf.org/html/rfc1918). Например, 10.0.0.0/16.

### <a name="can-i-have-public-ip-addresses-in-my-vnets"></a>Можно ли в виртуальных сетях использовать общедоступные IP-адреса?
Да. Дополнительные сведения об общедоступных IP-адресах см. в разделе hello [адресное пространство общедоступный IP-адрес в виртуальной сети](virtual-networks-public-ip-within-vnet.md) статьи. Открытый IP-адреса не будут доступны непосредственно из Интернета hello.

### <a name="is-there-a-limit-toohello-number-of-subnets-in-my-vnet"></a>Существует предел toohello количество подсетей в моей виртуальной сети?
Да. Чтение hello [Azure ограничивает](../azure-subscription-service-limits.md#networking-limits) Дополнительные сведения см. Адресные пространства подсетей не должны пересекаться друг с другом.

### <a name="are-there-any-restrictions-on-using-ip-addresses-within-these-subnets"></a>Существуют ли ограничения на использование IP-адресов в пределах этих подсетей?
Да. Azure резервирует некоторые IP-адреса в каждой подсети. Hello первый и последний IP-адреса подсетей hello зарезервированы для соответствия протоколу, а также 3 дополнительные адреса, используемые для служб Azure.

### <a name="how-small-and-how-large-can-vnets-and-subnets-be"></a>Насколько большими или малыми могут быть виртуальные сети и подсети?
поддерживаемая Hello наименьшее подсеть — / 29, а наибольшее hello/8 (по определениям подсети CIDR).

### <a name="can-i-bring-my-vlans-tooazure-using-vnets"></a>Можно ли перенести Мои виртуальные локальные сети tooAzure, с помощью виртуальных сетей?
Нет. Виртуальные сети представляют собой наложения Layer-3. Azure не поддерживает семантику Layer-2.

### <a name="can-i-specify-custom-routing-policies-on-my-vnets-and-subnets"></a>Можно ли в виртуальных сетях и подсетях задавать настраиваемые политики маршрутизации?
Да. Вы можете использовать пользовательскую маршрутизацию. Дополнительные сведения о пользовательской маршрутизации см. в статье [Что такое определяемые пользователем маршруты и IP-пересылка?](virtual-networks-udr-overview.md)

### <a name="do-vnets-support-multicast-or-broadcast"></a>Поддерживают ли виртуальные сети технологии многоадресной или широковещательной рассылки?
Нет. Технологии многоадресной или широковещательной рассылки не поддерживаются.

### <a name="what-protocols-can-i-use-within-vnets"></a>Какие протоколы можно использовать в рамках виртуальных сетей?
Вы можете использовать в виртуальных сетях протоколы TCP, UDP и ICMP, относящиеся к стеку TCP/IP. В виртуальных сетях блокируются многоадресные и широковещательные рассылки, инкапсулированные пакеты IP in IP и пакеты, передаваемые по протоколу GRE (Generic Routing Encapsulation). 

### <a name="can-i-ping-my-default-routers-within-a-vnet"></a>Можно ли в виртуальной сети проверять связь с маршрутизаторами по умолчанию?
Нет.

### <a name="can-i-use-tracert-toodiagnose-connectivity"></a>Можно ли использовать tracert toodiagnose?
Нет.

### <a name="can-i-add-subnets-after-hello-vnet-is-created"></a>Можно ли добавлять подсети после создания виртуальной сети hello
Да. Подсети можно добавлять tooVNets в любое время при условии, что адрес подсети hello не входит в другую подсеть hello виртуальной сети.

### <a name="can-i-modify-hello-size-of-my-subnet-after-i-create-it"></a>Можно ли изменять размер hello моей подсети, после его создания?
Да. Вы можете добавлять, удалять, расширять или уменьшать подсеть (если в ней не развернуты виртуальные машины и службы).

### <a name="can-i-modify-subnets-after-i-created-them"></a>Можно ли изменять подсети после их создания?
Да. Существует возможность добавления, удаления и изменения блоки CIDR hello, используемые виртуальной сети.

### <a name="can-i-connect-toohello-internet-if-i-am-running-my-services-in-a-vnet"></a>Можно ли подключить toohello Интернета, если я использую мой служб в виртуальной сети?
Да. Все службы, развернутые в виртуальной сети могут подключаться toohello Интернета. Каждая облачная служба развернута в Azure есть tooit общедоступный VIP. Будет иметь toodefine входные конечные точки для ролей PaaS и конечные точки для виртуальных машин tooenable подключения tooaccept эти службы из hello Интернета.

### <a name="do-vnets-support-ipv6"></a>Поддерживают ли виртуальные сети протокол IPv6?
Нет. В настоящее время невозможно использовать протокол IPv6 в виртуальных сетях.

### <a name="can-a-vnet-span-regions"></a>Может ли виртуальная сеть охватывать несколько регионов?
Нет. Виртуальная сеть — один регион ограниченного tooa.

### <a name="can-i-connect-a-vnet-tooanother-vnet-in-azure"></a>Можно ли подключить виртуальной сети tooanother виртуальной сети в Azure?
Да. Можно подключиться при помощи виртуальной сети tooanother с одной виртуальной сети:
- VPN-шлюз Azure. Чтение hello [Настройка подключения VNet-VNet](../vpn-gateway/vpn-gateway-howto-vnet-vnet-resource-manager-portal.md) Дополнительные сведения см. 
- Пиринговая связь между виртуальными сетями. Чтение hello [пиринга Обзор виртуальной сети](virtual-network-peering-overview.md) Дополнительные сведения см.

## <a name="name-resolution-dns"></a>Разрешение имен (DNS)

### <a name="what-are-my-dns-options-for-vnets"></a>Каковы параметры DNS для виртуальных сетей?
Используйте таблицу решений hello на hello [разрешение имен для виртуальных машин и экземпляров ролей](virtual-networks-name-resolution-for-vms-and-role-instances.md) tooguide страницы по hello DNS параметры недоступны.

### <a name="can-i-specify-dns-servers-for-a-vnet"></a>Можно ли указать DNS-серверы для виртуальной сети?
Да. IP-адреса DNS-сервера можно указать в параметрах виртуальной сети hello. Это будет применяться в качестве DNS-серверов по умолчанию hello для всех виртуальных машин в виртуальной сети hello.

### <a name="how-many-dns-servers-can-i-specify"></a>Сколько DNS-серверов можно указать?
Справочник по hello [Azure ограничивает](../azure-subscription-service-limits.md#networking-limits) Дополнительные сведения см.

### <a name="can-i-modify-my-dns-servers-after-i-have-created-hello-network"></a>Можно ли изменять DNS-серверы, после создания hello сети?
Да. Hello список DNS-серверов для виртуальной сети можно изменить в любое время. При изменении в список DNS-серверов, необходимо будет toorestart каждого hello виртуальных машин в виртуальной сети для них toopick копирование hello новый DNS-сервер.

### <a name="what-is-azure-provided-dns-and-does-it-work-with-vnets"></a>Что такое DNS, предоставленная Azure, и работает ли она с виртуальными сетями?
DNS, предоставленная Azure, — это мультитенантная служба DNS, предлагаемая корпорацией Майкрософт. Azure регистрирует в этой службе все ваши виртуальные машины и экземпляры ролей облачной службы. Эта служба обеспечивает разрешение имен по имени узла для виртуальных машин и экземпляров ролей, содержащихся в hello облачной службы и полное доменное имя для виртуальных машин и экземпляров ролей в hello же виртуальной сети. Чтение hello [разрешение имен для виртуальных машин и экземпляров ролей](virtual-networks-name-resolution-for-vms-and-role-instances.md) toolearn статьи, Дополнительные сведения о DNS.

> [!NOTE]
> Имеется ограничение в это время toohello первых 100 облачных служб в виртуальной сети для разрешения имен с помощью Azure DNS. Если вы используете собственный DNS-сервер, то это ограничение не применяется.
> 
> 

### <a name="can-i-override-my-dns-settings-on-a-per-vm--service-basis"></a>Можно ли переопределять параметры DNS для каждой виртуальной машины или службы?
Да. DNS-серверов можно задать на уровне облачной службы основы toooverride hello сетевые параметры по умолчанию. Тем не менее, мы рекомендуем максимально широко использовать DNS уровня сети.

### <a name="can-i-bring-my-own-dns-suffix"></a>Можно использовать собственный суффикс DNS?
Нет. Невозможно указать пользовательский суффикс DNS для виртуальных сетей.

## <a name="connecting-virtual-machines"></a>Подключение виртуальных машин

### <a name="can-i-deploy-vms-tooa-vnet"></a>Можно развернуть виртуальные машины tooa виртуальной сети
Да. Все tooa интерфейсов (NIC), подключенных сети, развернутые ВМ с помощью модели развертывания диспетчера ресурсов hello должен быть подключенным tooa виртуальной сети. Виртуальные машины, развернутые с помощью hello классической модели развертывания может быть подключенным tooa виртуальной сети.

### <a name="what-are-hello-different-types-of-ip-addresses-i-can-assign-toovms"></a>Каковы различные типы hello IP-адресов можно назначить tooVMs?
* **Личный:** назначен tooeach сетевого Адаптера в каждой виртуальной Машины. Hello адрес назначается с помощью метода hello статическим или динамическим. Частные IP-адреса назначаются из диапазона hello, указанного в параметрах hello подсети виртуальной сети. Ресурсы, развертываемые через hello классической модели развертывания назначенных частных IP-адресов, даже если они не подключены tooa виртуальной сети. Частный IP-адрес, назначенный с hello динамический метод остается назначенных ресурсов tooa до удаления ресурсов hello (виртуальные машины или облачной службы слоты развертывания). Частный IP-адрес, назначенный с hello динамический метод может измениться после перезапуска виртуальной Машины, после в hello остановлена (освобождена) состояние. Частный IP-адрес, назначенный с ресурсом tooa остается назначен статический метод hello пока hello ресурс будет удален. Если вам требуется tooensure, hello частный IP-адрес для ресурса никогда не изменяется, пока hello ресурс будет удален, назначение частного IP-адреса с помощью статического метода hello.
* **Общедоступный:** при необходимости назначенного tooNICs присоединенного tooVMs, развернутые с помощью модели развертывания диспетчера ресурсов Azure hello. Hello адрес можно назначить с помощью метода выделения статической или динамической hello. Экземпляры роли всех виртуальных машин и облачных служб, развернутые с помощью hello классической модели развертывания существует в облачной службе, которая назначается *динамическое*, общедоступный адрес виртуального IP-адресов (VIP). В качестве виртуального IP-адреса может быть назначен общедоступный *статический* IP-адрес, который также называется [зарезервированным IP-адресом](virtual-networks-reserved-public-ip.md). Можно назначить открытый tooindividual IP адресов, развернутые экземпляры роли ВМ или облачных служб с помощью hello классической модели развертывания. Эти адреса называются [общедоступными IP-адресами уровня экземпляра (ILPIP)](virtual-networks-instance-level-public-ip.md) и могут назначаться динамически.

### <a name="can-i-reserve-a-private-ip-address-for-a-vm-that-i-will-create-at-a-later-time"></a>Можно ли зарезервировать частный IP-адрес для виртуальной машины, которую я планирую создать позже?
Нет. Зарезервировать частный IP-адрес невозможно. При наличии частный IP-адрес ему назначается tooa ВМ или экземпляру роли DHCP-сервером hello. Что виртуальная машина может или не может быть hello, требуется toobe IP адрес hello для частного назначен. Тем не менее, можно изменить hello частный IP-адрес уже созданной виртуальной Машины tooany доступных частный IP-адрес.

### <a name="do-private-ip-addresses-change-for-vms-in-a-vnet"></a>Изменяются ли частные IP-адреса виртуальных машин в виртуальной сети?
Здесь возможны варианты в зависимости от обстоятельств. Динамические частные IP-адреса сохраняются за виртуальной машиной, пока она не будет остановлена (освобождена) или удалена. Статические частные IP-адреса не освобождаются, пока виртуальная машина не будет удалена.

### <a name="can-i-manually-assign-ip-addresses-toonics-within-hello-vm-operating-system"></a>Можно ли вручную назначать IP адресов tooNICs в операционной системе виртуальной Машины hello?
Да, но делать это не рекомендуется. Вручную изменять hello IP-адрес для сетевого Адаптера в операционной системе Виртуальной машины может привести toohello toolosing подключения к виртуальной Машине, будто toochange hello IP-адрес, назначенный tooa сетевых Адаптеров в виртуальной Машине Azure hello.

### <a name="what-happens-toomy-ip-addresses-if-i-stop-a-cloud-service-deployment-slot-or-shutdown-a-vm-from-within-hello-operating-system"></a>Что произойдет toomy IP-адресов, если остановить слота развертывания облачной службы или завершение работы виртуальной Машины из hello операционной системы?
Ничего. Hello IP-адреса (общедоступный VIP, открытый и закрытый) остаются слот развертывания назначенный toohello облачной службы или виртуальной Машине. Динамические адреса освобождаются только в том случае, когда виртуальная машина остановлена (освобождена) или удалена либо удален слот развертывания облачной службы. Выбрав hello **остановить** кнопку для ВМ в hello портал Azure задает ее состояние, tooStopped (освобождена). В этом случае hello виртуальной Машины будут потеряны IP-адресов.

### <a name="can-i-move-vms-from-one-subnet-tooanother-subnet-in-a-vnet-without-re-deploying"></a>Можно ли переместить виртуальные машины из одной подсети tooanother подсети в виртуальной сети без необходимости повторного развертывания?
Да. Дополнительные сведения можно найти в hello [как toomove виртуальной Машины или роли экземпляра другой подсети tooa](virtual-networks-move-vm-role-to-subnet.md) статьи.

### <a name="can-i-configure-a-static-mac-address-for-my-vm"></a>Можно ли настроить статический MAC-адрес для виртуальной машины?
Нет. Настроить MAC-адрес статически невозможно.

### <a name="will-hello-mac-address-remain-hello-same-for-my-vm-once-it-has-been-created"></a>Hello MAC-адрес останется hello одинаково для моей виртуальной Машины после ее создания?
Да, hello MAC-адрес остается hello одинаковы для виртуальной Машины развертывания через hello диспетчера ресурсов и классическое развертывание модели, пока будет удален. Ранее hello MAC-адреса была снята, если hello виртуальная машина остановлена (освобождена), но теперь hello MAC-адрес сохраняется даже в том случае, когда hello виртуальная машина находится в hello освобождена состояния.

### <a name="can-i-connect-toohello-internet-from-a-vm-in-a-vnet"></a>Можно ли подключить toohello Интернету из виртуальной Машины в виртуальной сети?
Да. Экземпляры роли всех виртуальных машин и облачных служб, развернутых в виртуальной сети можно подключить toohello Интернета.

## <a name="azure-services-that-connect-toovnets"></a>Службы Azure, которые подключаются tooVNets

### <a name="can-i-use-azure-app-service-web-apps-with-a-vnet"></a>Могу ли я использовать веб-приложения службы приложений Azure с виртуальной сетью?
Да. Вы можете развернуть веб-приложения внутри виртуальной сети, используя среду службы приложений (ASE). Все веб-приложения могут безопасно подключаться к виртуальной сети Azure и получать доступ к ее ресурсам, если для нее настроено подключение типа "точка — сеть". Дополнительные сведения см. в разделе hello в следующих статьях:

* [Создание веб-приложений в среде службы приложений](../app-service-web/app-service-web-how-to-create-a-web-app-in-an-ase.md)
* [Интеграция приложения с виртуальной сетью Azure](../app-service-web/web-sites-integrate-with-vnet.md)
* [Использование интеграции виртуальной сети и гибридных подключений с веб-приложениями](../app-service-web/web-sites-integrate-with-vnet.md#hybrid-connections-and-app-service-environments)

### <a name="can-i-deploy-cloud-services-with-web-and-worker-roles-paas-in-a-vnet"></a>Можно ли развернуть в виртуальной сети облачные службы с веб-ролями и рабочими ролями (платформа как услуга)?
Да. Вы можете (по своему усмотрению) развертывать экземпляры ролей облачных служб в виртуальных сетях. toodo таким образом, укажите имя виртуальной сети hello и сопоставления ролей и подсеть hello в разделе конфигурации сети hello конфигурации службы. Вы не обязательно tooupdate двоичные файлы.

### <a name="can-i-connect-a-virtual-machine-scale-set-vmss-tooa-vnet"></a>Можно подключить набор масштабирования виртуальной машины (VMSS) tooa виртуальной сети?
Да. VMSS tooa виртуальной сети, необходимо подключиться.

### <a name="can-i-move-my-services-in-and-out-of-vnets"></a>Можно ли перемещать службы в виртуальные сети и из них?
Нет. Невозможно перемещать службы в виртуальные сети и из них. Будет иметь toodelete и повторное развертывание службы toomove hello его tooanother виртуальной сети.

## <a name="security"></a>Безопасность

### <a name="what-is-hello-security-model-for-vnets"></a>Что такое hello модель безопасности для виртуальных сетей?
Виртуальные сети полностью изолированы друг от друга и другие службы, размещенные в hello инфраструктуры Azure. Виртуальная сеть — это граница доверия.

### <a name="can-i-restrict-inbound-or-outbound-traffic-flow-toovnet-connected-resources"></a>Можно ограничить ресурсы подключены tooVNet потока входящего и исходящего трафика?
Да. Можно применить [сетевых групп безопасности](virtual-networks-nsg.md) tooindividual подсетей в виртуальной сети, сетевых адаптеров присоединения tooa виртуальной сети или оба.

### <a name="can-i-implement-a-firewall-between-vnet-connected-resources"></a>Можно ли развернуть брандмауэр между ресурсами, подключенными к виртуальной сети?
Да. Можно развернуть [брандмауэра сети виртуальной машины](https://azure.microsoft.com/en-us/marketplace/?term=firewall) из нескольких поставщиков через hello Azure Marketplace.

### <a name="is-there-information-available-about-securing-vnets"></a>Могу ли я получить информацию о защите виртуальных сетей?
Да. В разделе hello [Обзор безопасности сети Azure](../security/security-network-overview.md) Дополнительные сведения см.

## <a name="apis-schemas-and-tools"></a>API-интерфейсы, схемы и инструменты

### <a name="can-i-manage-vnets-from-code"></a>Можно ли управлять виртуальными сетями из кода?
Да. Можно использовать API REST для виртуальных сетей в hello [диспетчера ресурсов Azure](https://msdn.microsoft.com/library/mt163658.aspx) и [классический (Service Management)](http://go.microsoft.com/fwlink/?LinkId=296833)) модели развертывания.

### <a name="is-there-tooling-support-for-vnets"></a>Существуют ли специальные программные средства для работы с виртуальными сетями?
Да. Дополнительные сведения об использовании средств:
- Hello Azure портала toodeploy виртуальных сетей hello [диспетчера ресурсов Azure](virtual-networks-create-vnet-arm-pportal.md) и [классический](virtual-networks-create-vnet-classic-pportal.md) модели развертывания.
- Toomanage PowerShell при развертывании виртуальных сетей с помощью hello [диспетчера ресурсов](/powershell/resourcemanager/azurerm.network/v3.1.0/azurerm.network.md) и [классический](/powershell/module/azure/?view=azuresmps-3.7.0) модели развертывания.
- Hello [Azure командной строки (CLI)](../virtual-machines/azure-cli-arm-commands.md#azure-network-commands-to-manage-network-resources) toomanage виртуальных сетей, развернутые с помощью обе модели развертывания.  
