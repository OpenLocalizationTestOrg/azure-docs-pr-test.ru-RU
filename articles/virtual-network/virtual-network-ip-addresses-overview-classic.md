---
title: "типы адресов aaaIP в Azure (классические) | Документы Microsoft"
description: "Сведения об использовании частных и общедоступных IP-адресов (классическая модель) в Azure."
services: virtual-network
documentationcenter: na
author: jimdial
manager: carmonm
editor: tysonn
tags: azure-service-management
ms.assetid: 2f8664ab-2daf-43fa-bbeb-be9773efc978
ms.service: virtual-network
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: infrastructure-services
ms.date: 02/11/2016
ms.author: jdial
ms.openlocfilehash: 7e09a5ad5b5f2d55329152b5d843de3c4455d1b2
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="ip-address-types-and-allocation-methods-classic-in-azure"></a>Типы IP-адресов и методы распределения (классическая модель) в Azure
Можно назначить toocommunicate IP адресов tooAzure ресурсов с другим ресурсам Azure в локальной сети и hello Интернета. В Azure можно использовать два типа IP-адресов: общедоступные и частные.

Общедоступные IP-адреса используются для связи с hello Интернета, включая Azure публичный веб-службы.

Частные IP-адреса используются для связи внутри виртуальной сети (VNet) Azure облачной службы и в локальной сети, при использовании шлюза VPN или tooextend цепь ExpressRoute tooAzure вашей сети.

> [!IMPORTANT]
> В Azure предлагаются две модели развертывания для создания ресурсов и работы с ними: [модель Resource Manager и классическая модель](../resource-manager-deployment-model.md).  В этой статье описан с помощью hello классической модели развертывания. Для большинства новых развертываний мы рекомендуем использовать модель развертывания с помощью Azure Resource Manager. Дополнительные сведения об IP-адресов в диспетчере ресурсов при чтении hello [IP-адреса](virtual-network-ip-addresses-overview-arm.md) статьи.

## <a name="public-ip-addresses"></a>Общедоступные IP-адреса
Общедоступные IP-адреса разрешить toocommunicate ресурсы Azure с публичный веб-службы Интернета и Azure, такие как [кэш Azure Redis](https://azure.microsoft.com/services/cache/), [концентраторов событий Azure](https://azure.microsoft.com/services/event-hubs/), [баз данных SQL](../sql-database/sql-database-technical-overview.md), и [хранилища Azure](../storage/common/storage-introduction.md).

Общедоступный IP-адрес связан с hello следующие типы ресурсов:

* Облачные службы
* виртуальные машины IaaS;
* экземпляры роли PaaS;
* VPN-шлюзы
* Шлюзы приложений

### <a name="allocation-method"></a>Способ выделения
Если общедоступный IP-адрес должен toobe, назначенный tooan ресурсов Azure, это *динамически* выделенный из пула доступных общедоступных IP-адрес в пределах ресурс hello расположения hello создается. Этот IP-адрес освобождается при остановке hello ресурсов. В случае, если для облачной службы, это происходит, когда останавливаются все экземпляры роли hello, который можно избежать с помощью *статических* (зарезервировано) IP-адрес (в разделе [облачные службы](#Cloud-services)).

> [!NOTE]
> Список диапазонов IP-адресов, из которых tooAzure ресурсы выделяются общих IP-адресов Hello опубликованы по адресу [диапазоны IP-центра обработки данных Azure](https://www.microsoft.com/download/details.aspx?id=41653).
> 
> 

### <a name="dns-hostname-resolution"></a>Разрешение DNS-имен узлов
При создании облачной службы или Виртуальной машине IaaS, необходимо tooprovide DNS-имя облачной службы, который является уникальным для всех ресурсов в Azure. При этом создается сопоставление в hello Azure под управлением DNS-серверы для *dnsname*. cloudapp.net toohello общедоступный IP-адрес ресурса hello. Например, при создании облачной службы с DNS-имя облачной службы из **contoso**, hello полное доменное имя (FQDN) **contoso.cloudapp.net** разрешит tooa общедоступный IP-адрес (VIP) Hello облачной службы. Можно использовать этот toocreate полное доменное имя пользовательского домена записей CNAME, указывающих toohello общедоступный IP-адрес в Azure.

### <a name="cloud-services"></a>Облачные службы
Облачная служба всегда имеет открытый IP-адрес связанной tooas виртуальный IP-адрес (VIP). Можно создать конечные точки в облачной службе tooassociate разные порты порты toointernal hello виртуальных IP-адресов для виртуальных машин и экземпляров ролей в облачной службе hello. 

Облачная служба может содержать несколько виртуальных машин в IaaS или PaaS экземпляров роли, все они доступны через hello виртуальных IP-адресов для одной облачной службы. Можно также назначить [несколько виртуальных IP-адресов tooa облачной службы](../load-balancer/load-balancer-multivip.md), позволяющий multi-VIP сценариев, таких как многопользовательской среде с веб-сайтов на основе протокола SSL.

Вы можете обеспечить hello общедоступный IP-адрес облачной службе остается hello остановился таким же, даже в том случае, если все hello экземпляров роли, с помощью *статических* общедоступный IP-адрес, который ссылается tooas [зарезервированный IP-адрес](virtual-networks-reserved-public-ip.md). Можно создать статический ресурс IP (зарезервировано) в определенном расположении и назначьте его tooany облачной службы в этом расположении. Невозможно указать hello фактический IP-адрес для hello зарезервированные IP-адреса, выделяется из пула доступных IP-адресов в расположении hello ее создания. Этот IP-адрес не освобождается, пока не будет явно удален.

Статические (зарезервировано) общедоступных IP-адресов обычно используются в сценариях hello, где облачной службы:

* требуется настройка toobe правила брандмауэра конечным пользователем.
* облачная служба зависит от разрешения внешних DNS-имен, и для динамического IP-адреса нужно обновить записи A;
* облачная служба использует внешние веб-службы, в которых применяется модель безопасности на основе IP-адресов;
* использует SSL сертификаты связанного tooan IP-адрес.

> [!NOTE]
> При создании классической виртуальной машины в Azure создается *облачная служба* -контейнер с виртуальным IP-адресом. После завершения создания hello через портал, значение по умолчанию RDP или SSH *конечная точка* настроено порталом hello. Поэтому toohello виртуальной Машины могут подключаться через hello облачной службы VIP. Можно зарезервировать виртуальный IP-адрес этой облачной службы, который фактически предоставляет зарезервированные tooconnect toohello IP адрес виртуальной Машины. Настраивая дополнительные конечные точки, вы можете открыть дополнительные порты.
> 
> 

### <a name="iaas-vms-and-paas-role-instances"></a>Виртуальные машины IaaS и экземпляры роли PaaS
Можно назначить общедоступный IP-адрес непосредственно tooan IaaS [ВМ](../virtual-machines/virtual-machines-linux-about.md?toc=%2fazure%2fvirtual-machines%2flinux%2ftoc.json) или экземпляра роли PaaS в облачной службе. Это ссылка tooas общедоступный IP-адрес уровня экземпляра ([ILPIP](virtual-networks-instance-level-public-ip.md)). и он может быть только динамическим.

> [!NOTE]
> Это поведение отличается от hello VIP hello облачной службы, который представляет собой контейнер для экземпляров роли ВМ IaaS или PaaS, так как облачная служба может содержать несколько ВМ IaaS или PaaS экземпляров роли, все они доступны через hello же облака виртуальных IP-адресов службы.
> 
> 

### <a name="vpn-gateways"></a>VPN-шлюзы
Объект [VPN-шлюз](../vpn-gateway/vpn-gateway-about-vpngateways.md) может быть используется tooconnect tooother виртуальной сети Azure виртуальные сети Azure или в локальной сети. VPN-шлюза назначен общедоступный IP-адрес *динамически*, который поддерживает связь с удаленной сети hello.

### <a name="application-gateways"></a>Шлюзы приложений
Azure [шлюз приложений](../application-gateway/application-gateway-introduction.md) может использоваться для балансировки нагрузки tooroute сетевого трафика на основании HTTP Layer7. Шлюз приложения будет назначен общедоступный IP-адрес *динамически*, который выступает в качестве hello VIP с балансировкой нагрузки.

### <a name="at-a-glance"></a>Вкратце
в следующей таблице Hello показывает каждого типа ресурсов с методами возможных выделения hello (динамический или статический) и возможность tooassign несколько общих IP-адресов.

| Ресурс | Динамический | Статическое | Несколько IP-адресов |
| --- | --- | --- | --- |
| облачная служба |Да |Да |Да |
| Виртуальная машина IaaS или экземпляр роли PaaS |Да |Нет |Нет |
| VPN-шлюз |Да |Нет |Нет |
| Шлюз приложений |Да |Нет |Нет |

## <a name="private-ip-addresses"></a>Частные IP-адреса
Частные IP-адреса позволяют toocommunicate ресурсы Azure с другими ресурсами в облачной службе или [виртуальной сети](virtual-networks-overview.md)(VNet) или tooon локальную сеть (через шлюз виртуальной частной сети или канал ExpressRoute), без использования Интернет доступный IP-адрес.

В Azure классической модели развертывания частный IP-адрес можно назначить следующие ресурсы Azure toohello:

* Виртуальные машины IaaS и экземпляры роли PaaS
* внутреннему балансировщику нагрузки;
* Шлюз приложений

### <a name="iaas-vms-and-paas-role-instances"></a>Виртуальные машины IaaS и экземпляры роли PaaS
Виртуальные машины (VM), созданные с hello классической модели развертывания они всегда размещаются в облачной службе, как экземпляры роли tooPaaS. поведение Hello частных IP-адресов, таким образом, как для этих ресурсов.

Это важные toonote облачной службы можно развернуть двумя способами:

* служба не входит в виртуальную сеть и является *автономной* ;
* служба является частью виртуальной сети.

#### <a name="allocation-method"></a>Способ выделения
В случае возникновения *автономный* облачная служба, get ресурсы, выделенные частный IP-адрес *динамически* с частных IP-адреса hello центра обработки данных Azure диапазона адресов. Он может использоваться только для обмена данными с другими виртуальными машинами в hello же облачной службе. Этот IP-адрес можно изменить, если ресурс hello остановлена и запущена.

В случае, если для облачной службы, развернутые в виртуальной сети, ресурсы получить закрытый IP-адреса, выделенные из диапазона адресов hello hello связанные подсетями (как указано в конфигурации сети). Этот закрытый IP-адреса можно использовать для обмена данными между всеми виртуальными машинами в виртуальной сети hello.

Кроме того, если облачная служба развернута в виртуальной сети, частный IP-адрес выделяется *динамически* по умолчанию (с помощью DHCP). Его можно изменить, если ресурс hello остановлена и запущена. Здравствуйте tooensure hello IP адрес остается таким же, вы должны способ распределения hello tooset слишком*статических*и укажите допустимый IP-адрес в диапазоне адресов соответствующего hello.

Вот для чего обычно используются статические частные IP-адреса:

* для виртуальных машин, работающих в качестве контроллеров домена или DNS-серверов;
* для виртуальных машин, на которых в правилах брандмауэра нужно использовать IP-адреса;
* для виртуальных машин со службами, к которым другие приложения обращаются по IP-адресу.

#### <a name="internal-dns-hostname-resolution"></a>Разрешение внутренних DNS-имен узлов
По умолчанию все виртуальные машины Azure и экземпляры роли PaaS используют [DNS-серверы, которыми управляет система Azure](virtual-networks-name-resolution-for-vms-and-role-instances.md#azure-provided-name-resolution) (кроме случаев, когда вы настроили другие DNS-серверы). Эти DNS-серверы обеспечивают внутреннее разрешение имен для виртуальных машин и экземпляров ролей, которые находятся в пределах hello одной виртуальной сети или облачной службе.

При создании виртуальной Машины, сопоставление для hello hostname tooits частный IP-адрес добавляется toohello Azure под управлением DNS-серверов. В случае виртуальной Машины multi-NIC, сопоставленного имени узла hello toohello частный IP-адрес основного hello сетевого адаптера. Тем не менее, сведения о сопоставлении является ограниченным tooresources внутри hello же облачной службы или виртуальной сети.

В случае возникновения *автономный* облачной службы будет имена узлов может tooresolve всех экземпляров виртуальных машин или роли в рамках hello же облачную службу в только. В случае облачной службы в виртуальной сети будет имена узлов может tooresolve всех экземпляров виртуальных машин и роль hello в hello виртуальной сети.

### <a name="internal-load-balancers-ilb--application-gateways"></a>Внутренние балансировщики нагрузки и шлюзы приложений
Можно назначить закрытый toohello адрес IP **внешнего интерфейса** конфигурация [внутренняя Подсистема балансировки нагрузки Azure](../load-balancer/load-balancer-internal-overview.md) (ILB) или [шлюза приложения Azure](../application-gateway/application-gateway-introduction.md). Это частный IP-адрес служит внутреннюю конечную точку, доступны только ресурсы toohello в своем виртуальной сети (VNet) и удаленные сети hello подключен toohello виртуальной сети. Можно назначить либо динамический или статический закрытый конфигурацию IP-адресов toohello переднего плана. Можно также назначить несколько частных IP адреса tooenable multi-vip сценариев.

### <a name="at-a-glance"></a>Вкратце
в следующей таблице Hello показывает каждого типа ресурсов с методами возможных выделения hello (динамический или статический) и возможность tooassign несколько частных IP-адресов.

| Ресурс | Динамический | Статическое | Несколько IP-адресов |
| --- | --- | --- | --- |
| Виртуальная машина (в *автономной* облачной службе) |Да |Да |Да |
| Экземпляр роли PaaS (в *автономной* облачной службе) |Да |Нет |Да |
| Виртуальная машина или экземпляр роли PaaS (в виртуальной сети) |Да |Да |Да |
| Внешний интерфейс внутреннего балансировщика нагрузки |Да |Да |Да |
| Интерфейсная часть шлюза приложений |Да |Да |Да |

## <a name="limits"></a>Ограничения
в следующей таблице Hello показывает hello ограничения, налагаемые на IP-адресов адресации в Azure на подписку. Вы можете [обратитесь в службу поддержки](https://portal.azure.com/#blade/Microsoft_Azure_Support/HelpAndSupportBlade) ограничения по умолчанию hello tooincrease копирование toohello максимальные ограничения, зависимости от потребностей бизнеса.

|  | Ограничение по умолчанию | Максимальное ограничение |
| --- | --- | --- |
| Общедоступные IP-адреса (динамические) |5 |обратиться в службу поддержки |
| Зарезервированные общедоступные IP-адреса |20 |обратиться в службу поддержки |
| Общедоступный виртуальный IP-адрес на развертывание (облачная служба) |5 |обратиться в службу поддержки |
| Частный виртуальный IP-адрес на развертывание (облачная служба) |1 |1 |

Убедитесь, что чтение hello полный набор [ограничивает сетей](../azure-subscription-service-limits.md#networking-limits) в Azure.

## <a name="pricing"></a>Цены
В большинстве случаев общедоступные IP-адреса бесплатны. Нет номинальный платы toouse дополнительные и/или статические общедоступные IP-адреса. Убедитесь, что вы понимаете hello [структуре ценообразования для общедоступных IP-адресов](https://azure.microsoft.com/pricing/details/ip-addresses/).

## <a name="differences-between-resource-manager-and-classic-deployments"></a>Различие развертываний диспетчера ресурсов и классических развертываний
Ниже приведено сравнение функций адресации IP в диспетчере ресурсов и hello классической модели развертывания.

|  | Ресурс | Классический | Диспетчер ресурсов |
| --- | --- | --- | --- |
| **Общедоступный IP-адрес** |***Виртуальная машина*** |Ссылка tooas ILPIP (динамическая) |Название tooas общедоступный IP-адрес (динамический или статический) |
|  ||Назначенные tooan ВМ IaaS или PaaS экземпляра роли |Сетевой Адаптер связан toohello виртуальной Машины | |
|  |***Балансировщик нагрузки для Интернета*** |Название tooas виртуальных IP-адресов (динамически) или зарезервированный IP-адрес (статический) |Название tooas общедоступный IP-адрес (динамический или статический) | |
|  ||Назначенные tooa облачной службы |Конфигурации переднего плана подсистемы балансировки нагрузки связанных toohello | |
|  | | | |
| **Частные IP-адреса** |***Виртуальная машина*** |Ссылка tooas a DIP |Название tooas частный IP-адрес |
|  ||Назначенные tooan ВМ IaaS или PaaS экземпляра роли |Назначенные toohello ВМ сетевого Адаптера | |
|  |***Внутренний балансировщик нагрузки (ILB)*** |Назначенные toohello ILB (динамический или статический) |Назначенные toohello ILB конфигурации внешнего интерфейса (динамический или статический) | |

## <a name="next-steps"></a>Дальнейшие действия
* [Развертывание виртуальной Машины статический частный IP-адрес](virtual-networks-static-private-ip-classic-pportal.md) с помощью портала Azure "hello".

