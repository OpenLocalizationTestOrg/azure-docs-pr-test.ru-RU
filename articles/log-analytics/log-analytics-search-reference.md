---
title: "Справочник по поиску aaaAzure анализа журналов | Документы Microsoft"
description: "Справочник по поиска аналитики журналов Hello описывает язык поиска hello и предоставляет hello общие варианты синтаксиса запросов, используемые при поиске данных и фильтрации выражений toohelp сужение области поиска."
services: log-analytics
documentationcenter: 
author: bwren
manager: carmonm
editor: 
ms.assetid: 402615a2-bed0-4831-ba69-53be49059718
ms.service: log-analytics
ms.workload: na
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 07/13/2017
ms.author: bwren
ms.custom: H1Hack27Feb2017
ms.openlocfilehash: 7478a1139b88a1ce76ebb7b76027a6ccd66f4f27
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="log-analytics-search-reference"></a>Справочник по поиску в Log Analytics

>[!NOTE]
> В этой статье описывается использование hello текущий язык запроса в службе анализа журналов запросов поиска журналов.  Если обновленный toohello рабочей области [языка запросов новый журнал аналитики](log-analytics-log-search-upgrade.md), то вы должны обращаться слишком[Здравствуйте, справочник по языку для hello новый язык](https://go.microsoft.com/fwlink/?linkid=856079).

Hello следующем справочном разделе о языке поиска описываются hello общие варианты синтаксиса запросов, используемые при поиске данных и фильтрации выражений toohelp сужение области поиска. Здесь также описываются команды, которые можно использовать действие tootake на извлеченные данные hello.

Вы найдете сведения о полях hello, возвращаемых при поиске и hello аспекты, которые помогают поиска дополнительных сведений о подобные категории данных в hello [полю и аспекту поиска справочнике](#search-field-and-facet-reference).

## <a name="general-query-syntax"></a>Общий синтаксис запросов
синтаксис Hello для общих запросов выглядит следующим образом:

```
filterExpression | command1 | command2 …
```

Здравствуйте, критерий фильтра (`filterExpression`) определяет hello Здравствуйте, «where» условие для запроса. команды Hello применяются toohello результатов, возвращаемых запросом hello. Несколько команд должны быть разделены hello вертикальной черты (|).

### <a name="general-syntax-examples"></a>Примеры общего синтаксиса
Примеры:

```
system
```

Этот запрос возвращает результаты, содержащие слово hello *системы* в любом поле, которое было проиндексировано для полнотекстовый или условия поиска.

> [!NOTE]
> Не все поля индексируются таким образом, но являются наиболее распространенные текстовые поля (например, описания и имена), обычно hello.
>
>

```
system error
```

Этот запрос возвращает результаты, содержащие слова hello *системы* и *ошибка*.

```
system error | sort ManagementGroupName, TimeGenerated desc | top 10
```

Этот запрос возвращает результаты, содержащие слова hello *системы* и *ошибка*. Hello результаты сортируются по hello *ManagementGroupName* (в порядке возрастания), а затем по hello *TimeGenerated* (в порядке убывания). Он принимает только hello первые 10 результатов.

> [!IMPORTANT]
> Здравствуйте, все имена полей и значения hello hello строковых и текстовых полей учитывают регистр.
>
>

## <a name="filter-expressions"></a>Выражения фильтра
Привет, следующие подразделы содержат пояснения по выражениям фильтра hello.

### <a name="string-literals"></a>Строковые литералы
Строковый литерал — это любая строка, которая не распознается средством синтаксического анализа hello как ключевое слово или Предопределенный тип данных (например, числа или даты).

Примеры:

```
These all are string literals
```

Этот запрос выполняет поиск результатов, содержащих вхождения всех пяти слов. tooperform поиска в сложных строк заключите hello строковый литерал в кавычки. Например:

```
"Windows Server"
```

Этот запрос возвращает только результаты с точным совпадением по строке *Windows Server*.

### <a name="numbers"></a>Числа
средство синтаксического анализа Hello поддерживает hello десятичное целое число с плавающей запятой синтаксис и чисел для числовых полей.

Примеры:

```
Type:Perf 0.5
```

```
HTTP 500
```

### <a name="dates-and-times"></a>Даты и время
Каждый фрагмент данных в системе hello имеет *TimeGenerated* свойства, которое представляет hello исходная дата и время hello записи. Некоторые типы данных могут также иметь дополнительные поля даты и времени (например, *LastModified*).

Здравствуйте, временная шкала **диаграммы и времени** селектор в службе анализа журналов Azure показывает распределение результатов во времени (соответствующим toohello текущий запрос выполняется). Следующий пример основан на hello *TimeGenerated* поля. Поля даты и времени имеют определенный строковый формат, который может использоваться в запросах toorestrict hello запроса tooa определенным периодом времени. Также можно использовать синтаксис toorefer toorelative временных интервалов (например, «между 3 днями назад и 2 часами назад»).

Hello ниже приведены допустимые формы синтаксиса для значения даты и времени.

```
yyyy-mm-ddThh:mm:ss.dddZ
```

```
yyyy-mm-ddThh:mm:ss.ddd
```

```
yyyy-mm-ddThh:mm:ss
```

```
yyyy-mm-ddThh:mm:ss
```

```
yyyy-mm-ddThh:mm
```

```
yyyy-mm-dd
```


Например:

```
TimeGenerated:2013-10-01T12:20
```

Hello предыдущая команда возвращает только записи с *TimeGenerated* значение точности соответствует 12:20 1 октября 2013 г.

Hello синтаксического анализа также поддерживает мнемоническое значение даты и времени hello, ТЕПЕРЬ. (Это маловероятно, что это даст никаких результатов, так как данные не проходят через систему с такой скоростью hello.)

В этих примерах показаны стандартные блоки toouse для относительных и абсолютных дат. В Здравствуйте трех следующих подразделах, вы увидите, как toouse их в более сложных фильтров с примерами, где применяются Относительные временные диапазоны.

### <a name="datetime-math"></a>Математические операторы даты и времени
Используйте математические операторы hello даты и времени toooffset или округления значения даты и времени hello, с помощью простых вычислений даты и времени.

Синтаксис:

```
datetime/unit
```

```
datetime[+|-]count unit
```

| Оператор | Описание |
| --- | --- |
| / |Округляет число toohello даты и времени указаны единицы. Например, ТЕПЕРЬ / DAY округляет toomidnight текущей даты и времени hello объекта hello текущего дня. |
| + или - |Смещение даты и времени, hello указанное число единиц. Например, ТЕПЕРЬ + 1 HOUR смещает текущее hello даты и времени на час вперед. 2013-10-01T12:00-10 дней смещает hello значение даты на 10 дней назад. |

Математические операторы даты и времени hello можно цепочку. Например:

```
NOW+1HOUR-10MONTHS/MINUTE
```

Hello следующей таблице перечислены поддерживаемые hello единицы даты и времени.

| Единица измерения даты и времени | Описание |
| --- | --- |
| YEAR, YEARS |Округляет число toocurrent год или смещения, hello заданное число лет. |
| MONTH, MONTHS |Округляет число месяца toocurrent или смещения, hello заданное число месяцев. |
| DAY, DAYS, DATE |Округляет число toocurrent день месяца hello или смещения, hello заданное число дней. |
| HOUR, HOURS |Округляет число toocurrent час или смещения, hello заданное число часов. |
| MINUTE, MINUTES |Округляет число toocurrent минуту или смещения, hello заданное число минут. |
| SECOND, SECONDS |Округляет toocurrent секунды или смещает hello указанное число секунд. |
| MILLISECOND, MILLISECONDS, MILLI, MILLIS |Округляет число toocurrent миллисекунды или смещения, hello заданное число миллисекунд. |

### <a name="field-facets"></a>Аспекты поля
С помощью аспектов поля можно указать условие поиска hello для конкретных полей и их точные значения. Это отличается от написания запросов «произвольный текст» для различных терминов по всему индексу hello. Вы уже видели этот метод в некоторых предыдущих примерах. Здесь представлены Hello более сложные примеры.

**Синтаксис**

```
field:value
```

```
field=value
```

**Описание**

Поиск hello поля для конкретного значения hello. Hello значение может быть строковым литералом, числом, или дата и время.

Например:

```
TimeGenerated:NOW
```

```
ObjectDisplayName:"server01.contoso.com"
```

```
SampleValue:0.3
```

**Синтаксис**

*поле>значение*

*поле<значение*

*поле>=значение*

*поле<=значение*

*поле!=значение*

**Описание**

Предоставляет сравнения.

Например:

```
TimeGenerated>NOW+2HOURS
```

**Синтаксис**

```
field:[from..to]
```

**Описание**

Предоставляет аспекты диапазона.

Например:

```
TimeGenerated:[NOW..NOW+1DAY]
```

```
SampleValue:[0..2]
```

### <a name="in"></a>IN
Hello **в** ключевое слово позволяет tooselect из списка значений. В зависимости от hello синтаксисом, используемым это может быть простой список заданные значения, или список значений в статистической функции.

Синтаксис 1:

```
field IN {value1,value2,value3,...}
```

Этот синтаксис позволяет tooinclude все значения в простом списке.



Примеры:

```
EventID IN {1201,1204,1210}
```

```
Computer IN {"srv01.contoso.com","srv02.contoso.com"}
```

Синтаксис 2:

```
(Outer query) (Field toouse with inner query results) IN {Inner query | measure count() by (Field toosend tooouter query)} (rest  of outer query)  
```

Этот синтаксис позволяет toocreate агрегата. Затем можно направить hello список значений из этого агрегата в другой внешний (основной) поиск, который будет искать события с этим значением. Это делается путем hello внутренний поиск заключается в фигурные скобки, а его результаты передаются in. как возможные значения для поля во внешнем поиске hello с помощью оператора IN hello.

Пример внутреннего запроса: *компьютеров, на которых сейчас отсутствуют обновления для системы безопасности* с hello статистической обработки запроса:

```
Type:Update Classification="Security Updates"  UpdateState=needed TimeGenerated>NOW-25HOURS | measure count() by Computer
```    

Hello последний запрос, который находит *все события Windows для компьютеров, на которых сейчас отсутствуют обновления для системы безопасности* аналогичные hello ниже:

```
Type=Event Computer IN {Type:Update Classification="Security Updates"  UpdateState=needed TimeGenerated>NOW-25HOURS | measure count() by Computer}
```

### <a name="contains"></a>Содержит
Hello **Contains** ключевое слово позволяет toofilter для записей с полем, которое содержит указанную строку. Такие запросы чувствительны к регистру, работают только со строковыми полями и не могут содержать escape-символы.

Синтаксис:

```
field:contains("string")
```

Пример:

```
Type:contains("Event")
```

Это возвращает записи с типом, который содержит строку hello, «События». Примеры включают **Event**, **SecurityEvent** и **ServiceFabricOperationEvent**.



### <a name="regular-expressions"></a>Регулярные выражения
Можно указать условие поиска для поля с регулярным выражением, используя hello **Regex** ключевое слово. Полное описание синтаксиса hello, можно использовать в регулярных выражениях см. в разделе [с помощью регулярных выражений запросов поиска журналов toofilter в службе анализа журналов](log-analytics-log-searches-regex.md).

Синтаксис:

```
field:Regex("Regular Expression")
```

Пример:

```
Computer:Regex("^C.*")
```

### <a name="logical-operators"></a>Логические операторы
запрос Hello языки поддерживают логические операторы hello (*AND*, *или*, и *не*) и их псевдонимы в стиле языка C (*&&*,  *||* , и *!*соответственно). Эти операторы можно использовать toogroup круглые скобки.

Примеры:

```
system OR error

```

```
Type:Alert AND NOT(Severity:1 OR ObjectId:"8066bbc0-9ec8-ca83-1edc-6f30d4779bcb8066bbc0-9ec8-ca83-1edc-6f30d4779bcb")
```

Можно опустить для аргументов фильтра верхнего уровня hello hello логический оператор. В этом случае подразумевается hello оператор и.

| Выражения фильтра | Эквивалент слишком|
| --- | --- |
| system error |system AND error |
| system "Windows Server" OR Severity:1 |system AND ("Windows Server" OR Severity:1) |

### <a name="wildcarding"></a>Подстановочные знаки
Hello язык запросов поддерживает использование hello ( \* ) символ слишком представляют один или несколько символов для значения в запросе.

Пример:

 Найти все компьютеры с «SQL» в имени hello, например «Redmond-SQL».

```
Type=Event Computer=*SQL*
```

> [!NOTE]
> Подстановочные знаки пока нельзя использовать в кавычках. Например, сообщение hello `"*This text*"` рассматривает hello (\*) использовать в качестве литерала (\*) символов.


## <a name="commands"></a>Команды


команды Hello применяются toohello результатов, возвращаемых запросом hello. Используйте hello вертикальной черты (|) tooapply toohello команды получить результаты. Несколько команд должны быть разделены вертикальной чертой hello.

> [!NOTE]
> Имена команд можно вводить в верхнем или нижнем регистре, в отличие от имен полей hello и hello данных.
>
>

### <a name="sort"></a>Сортировать
Синтаксис:

    sort field1 asc|desc, field2 asc|desc, …

Сортирует hello результаты по определенным полям. Hello asc или desc суффикс toosort hello результаты в порядке возрастания или убывания является необязательным. Здравствуйте, если он опущен, *asc* порядок сортировки. Для hello **TimeGenerated** поля, *desc* порядок сортировки используется, чтобы он возвращал последние результаты hello сначала по умолчанию.

### <a name="toplimit"></a>Top/Limit
Синтаксис:

    top number


    limit number
Ограничения hello ответа toohello первых N результатов.

Пример:

    Type:Alert errors detected | top 10

Возвращает hello первые 10 результатов сопоставления.

### <a name="skip"></a>Skip
Синтаксис:

    skip number

Пропускает hello число результатов.

Пример:

    Type:Alert errors detected | top 10 | skip 200

Возвращает 10 лучших соответствующих результатов, начиная с результата под номером 200.

### <a name="select"></a>Выберите пункт
Синтаксис:

    select field1, field2, ...

Ограничивает результаты выбранными полями toohello.

Пример:

    Type:Alert errors detected | select Name, Severity

Ограничения hello возвращаемые результаты полями слишком*имя* и *серьезность*.

### <a name="measure"></a>Measure
Hello *мер* команда является используется tooapply статистические функции toohello необработанным результатам поиска. Это очень полезно tooget *сгруппированном* представления данных hello. При использовании команды measure hello поиска журналов аналитики отображается таблица с агрегированными результатами.

**Синтаксис**

    measure aggregateFunction1([aggregatedField]) [as fieldAlias1] [, aggregateFunction2([aggregatedField2]) [as fieldAlias2] [, ...]] by groupField1 [, groupField2 [, groupField3]]  [interval interval]


    measure aggregateFunction1([aggregatedField]) [as fieldAlias1] [, aggregateFunction2([aggregatedField2]) [as fieldAlias2] [, ...]]  interval interval



Объединяет результаты hello, *groupField*и вычисляет значения hello статистическая обработка проводится с помощью *aggregatedField*.

| Статистическая функция для measure | Описание |
| --- | --- |
| *aggregateFunction* |Имя Hello hello агрегатной функции (без учета регистра). поддерживаются следующие агрегатные функции Hello: COUNT, MAX, MIN, SUM, AVG, STDDEV, COUNTDISTINCT, ПРОЦЕНТИЛЯ ## или PCT ## (## — любое число от 1 до 99). |
| *aggregatedField* |поле Hello, выполняется статистическая обработка. Это поле является необязательным для агрегатной функции COUNT hello, но имеет toobe числового поля для SUM, MAX, MIN, AVG, STDDEV, ПРОЦЕНТИЛЯ ## или PCT ## (## — любое число от 1 до 99). Hello aggregatedField также может быть hello **расширить** поддерживаемые функции. |
| *fieldAlias* |псевдоним (необязательный) Hello для hello рассчитывается статистическое значение. Если не указан, именем поля hello — **AggregatedValue**. |
| *groupField* |Имя Hello hello поля, которое hello результирующего набора, сгруппированные по. |
| *Интервал* |время в формате hello Hello:**nnnNAME**. **nnn**— hello положительным целым числом. **ИМЯ** — имя интервала hello. Поддерживаемые имена интервалов (с учетом регистра): MILLISECOND[S], SECOND[S], MINUTE[S], HOUR[S], DAY[S], MONTH[S] и YEAR[S]. |

параметр interval Hello может использоваться только в полях групп даты и времени (например, *TimeGenerated* и *TimeCreated*). В настоящее время это не является обязательным для hello службы, но поле без даты и времени, который передается toohello серверной части вызывает ошибку во время выполнения. При реализации проверки схемы hello hello API службы отклоняет запросы, использующие поля без даты и времени для статистической обработки по интервалу. Hello текущей *мер* реализация поддерживает группирование интервала для агрегатных функций.

Здравствуйте, если hello BY опущено, однако интервал задан (в качестве второго синтаксиса), *TimeGenerated* используется по умолчанию.

Примеры:

**Пример 1**

    Type:Alert | measure count() as Count by ObjectId

Группы hello оповещения по *ObjectID*и вычисляет количество оповещений для каждой группы в hello. Hello агрегированное значение возвращается как hello *число* (псевдоним).

**Пример 2**

    Type:Alert | measure count() interval 1HOUR

Группы с помощью hello hello оповещения с интервалами в 1 час *TimeGenerated* поле и возвращает hello количество оповещений в каждом интервале.

**Пример 3**

    Type:Alert | measure count() as AlertsPerHour interval 1HOUR

В предыдущем примере hello только с псевдонимом агрегированного поля (*AlertsPerHour*).

**Пример 4**

    * | measure count() by TimeCreated interval 5DAYS

Группирует hello результаты с интервалами в 5 дней, используя hello *TimeCreated* поле и возвращает hello количество результатов в каждом интервале.

**Пример 5**

    Type:Alert | measure max(Severity) by WorkflowName

Группы hello оповещения по имени рабочей нагрузки и возвращает hello значение максимального уровня серьезности оповещений для каждого рабочего процесса.

**Пример 6**

    Type:Alert | measure min(Severity) by WorkflowName

В предыдущем примере hello только с hello *min* агрегированной функцией.

**Пример 7**

    Type:Perf | measure avg(CounterValue) by Computer

Группирует производительности на компьютере и вычисляет среднее значение hello (avg).

**Пример 8**

    Type:Perf | measure sum(CounterValue) by Computer

Аналогичен предыдущему примеру hello, но использует *сумма*.

**Пример 9**

    Type:Perf | measure stddev(CounterValue) by Computer

Аналогичен предыдущему примеру hello, но использует *stddev*.

**Пример 10**

    Type:Perf | measure percentile70(CounterValue) by Computer

Аналогичен предыдущему примеру hello, но использует *percentile70*.

**Пример 11**

    Type:Perf | measure pct70(CounterValue) by Computer

Аналогичен предыдущему примеру hello, но использует *pct70*. Обратите внимание, что *PCT##* — это только псевдоним для функции *PERCENTILE##*.

**Пример 12**

    Type:Perf | measure avg(CounterValue) by Computer, CounterName

Сначала группирует производительности в компьютер и затем CounterName и вычисляет среднее значение hello (avg).

**Пример 13**

    Type:Alert | measure count() as Count by WorkflowName | sort Count desc | top 5

Получает пять первых рабочих процессов с максимальным числом оповещений hello hello.

**Пример 14**

    * | measure countdistinct(Computer) by Type

Подсчитывает количество уникальных компьютеров, создание отчетов для каждого типа в hello.

**Пример 15**

    * | measure countdistinct(Computer) Interval 1HOUR

Подсчитывает количество уникальных компьютеров, передающих каждый час в hello.

**Пример 16**

```
Type:Perf CounterName=”% Processor Time” InstanceName=”_Total” | measure avg(CounterValue) by Computer Interval 1HOUR
```

Группирует % загруженности процессора на компьютере и возвращает среднее значение hello за каждый час.

**Пример 17**

    Type:W3CIISLog | measure max(TimeTaken) by csMethod Interval 5MINUTES

Группирует W3CIISLog методом и возвращает максимальное hello каждые 5 минут.

**Пример 18**

```
Type:Perf CounterName=”% Processor Time” InstanceName=”_Total”  | measure min(CounterValue) as MIN, avg(CounterValue) as AVG, percentile75(CounterValue) as PCT75, max(CounterValue) as MAX by Computer Interval 1HOUR
```

Группы % загруженности процессора компьютера и возвращает минимальный hello, среднее, 75-процентиля и максимальное значение для каждого часа.

**Пример 19**

```
Type:Perf CounterName=”% Processor Time”  | measure min(CounterValue) as MIN, avg(CounterValue) as AVG, percentile75(CounterValue) as PCT75, max(CounterValue) as MAX by Computer, InstanceName Interval 1HOUR
```

Группы % загруженности процессора сначала на компьютер, а затем по имени и возвращает минимум hello, среднее, 75-й процентиль экземпляра и максимальное значение для каждого часа.

**Пример 20**

```
Type= Perf CounterName="Disk Writes/sec" Computer="BaconDC01.BaconLand.com" | measure max(product(CounterValue,60)) as MaxDWPerMin by InstanceName Interval 1HOUR
```

Вычисляет максимальное hello к дискам на запись в минуту для каждого диска на компьютере.

### <a name="where"></a>Where
Синтаксис:

```
**where** AggregatedValue>20
```

Можно использовать только после *мер* команда toofurther фильтра hello статистическую обработку результатов, hello *мер* созданных статистической функции.

Примеры:

    Type:Perf CounterName:"% Total Run Time" | Measure max(CounterValue) as MAXCPU by Computer

    Type:Perf CounterName:"% Total Run Time" | Measure max(CounterValue) by Computer | where (AggregatedValue>50 and AggregatedValue<90)



### <a name="dedup"></a>Dedup
Синтаксис:

    Dedup FieldName

Возвращает hello первый документ, найденный для каждого уникального значения заданного поля hello.

Пример:

    Type=Event | Dedup EventID | sort TimeGenerated DESC

Этот пример возвращает одно событие (hello последнее событие) каждого EventID.

### <a name="join"></a>Объединение
Соединения hello результаты двух запросов tooform один результирующий набор.  Поддерживает несколько типов соединений, описанных в hello выполните таблицы.

| Тип соединения | Описание |
|:--|:--|
| Внутреннее | Возвращает только записи с совпадающим значением в обоих запросах. |
| Внешнее | Возвращает все записи из обоих запросов.  |
| Левое  | Возвращает все записи из левого запроса и совпадающие записи из правого. |


- Соединения в настоящее время не поддерживает запросы, включающие hello **в** ключевое слово, hello **мер** команды или hello **расширить** команды, если он предназначен поле из hello правого запроса.
- Пока в соединение можно включить всего одно поле.
- Один поиск не может содержать больше одного соединения.

**Синтаксис**

```
<left-query> | JOIN <join-type> <left-query-field-name> (<right-query>) <right-query-field-name>
```

**Примеры**

типы соединения tooillustrate hello, рассмотрите возможность присоединения тип данных собираются из пользовательский журнал с именем MyBackup_CL hello пакет пульса для каждого компьютера.  Эти типы данных имеют hello следующие данные.

`Type = MyBackup_CL`

| TimeGenerated | Компьютер | LastBackupStatus |
|:---|:---|:---|
| 4/20/2017 01:26:32.137 AM | Srv01.contoso.com | Успешно |
| 4/20/2017 02:13:12.381 AM | SRV02.contoso.com | Успешно |
| 4/20/2017 02:13:12.381 AM | srv03.contoso.com | Сбой |

`Type = Hearbeat` (Показано лишь подмножество полей)

| TimeGenerated | Компьютер | ComputerIP |
|:---|:---|:---|
| 4/21/2017 12:01:34.482 PM | Srv01.contoso.com | 10.10.100.1 |
| 4/21/2017 12:02:21.916 PM | SRV02.contoso.com | 10.10.100.2 |
| 4/21/2017 12:01:47.373 PM | srv04.contoso.com | 10.10.100.4 |

#### <a name="inner-join"></a>Внутреннее соединение

`Type=MyBackup_CL | join inner Computer (Type=Heartbeat) Computer`

Возвращает hello следующие записи, в которых соответствует поле hello компьютера для обоих типов данных.

| Компьютер| TimeGenerated | LastBackupStatus | TimeGenerated_joined | ComputerIP_joined | Type_joined |
|:---|:---|:---|:---|:---|:---|
| Srv01.contoso.com | 4/20/2017 01:26:32.137 AM | Успешно | 4/21/2017 12:01:34.482 PM | 10.10.100.1 | Пульс |
| SRV02.contoso.com | 4/20/2017 02:13:12.381 AM | Успешно | 4/21/2017 12:02:21.916 PM | 10.10.100.2 | Пульс |


#### <a name="outer-join"></a>Внешнее соединение

`Type=MyBackup_CL | join outer Computer (Type=Heartbeat) Computer`

Возвращает hello следующие записи для обоих типов данных.

| Компьютер| TimeGenerated | LastBackupStatus | TimeGenerated_joined | ComputerIP_joined | Type_joined |
|:---|:---|:---|:---|:---|:---|
| Srv01.contoso.com | 4/20/2017 01:26:32.137 AM | Успешно  | 4/21/2017 12:01:34.482 PM | 10.10.100.1 | Пульс |
| SRV02.contoso.com | 4/20/2017 02:14:12.381 AM | Успешно  | 4/21/2017 12:02:21.916 PM | 10.10.100.2 | Пульс |
| srv03.contoso.com | 4/20/2017 01:33:35.974 AM | Сбой  | 4/21/2017 12:01:47.373 PM | | |
| srv04.contoso.com |                           |          | 4/21/2017 12:01:47.373 PM | 10.10.100.2 | Пульс |



#### <a name="left-join"></a>Левое соединение

`Type=MyBackup_CL | join left Computer (Type=Heartbeat) Computer`

Возвращает следующие записи из MyBackup_CL с полями сопоставления из пульса hello.

| Компьютер| TimeGenerated | LastBackupStatus | TimeGenerated_joined | ComputerIP_joined | Type_joined |
|:---|:---|:---|:---|:---|:---|
| Srv01.contoso.com | 4/20/2017 01:26:32.137 AM | Успешно | 4/21/2017 12:01:34.482 PM | 10.10.100.1 | Пульс |
| SRV02.contoso.com | 4/20/2017 02:13:12.381 AM | Успешно | 4/21/2017 12:02:21.916 PM | 10.10.100.2 | Пульс |
| srv03.contoso.com | 4/20/2017 02:13:12.381 AM | Сбой | | | |


### <a name="extend"></a>Extend
Позволяет toocreate поля времени выполнения в запросах. Обратите внимание, что поля времени выполнения не может использоваться с hello мер команда tooperform агрегата.

**Пример 1**

    Type=SQLAssessmentRecommendation | Extend product(RecommendationScore, RecommendationWeight) AS RecommendationWeightedScore
Показать взвешенный балл рекомендаций для рекомендаций оценки SQL.

**Пример 2**

    Type=Perf CounterName="Private Bytes" | EXTEND div(CounterValue,1024) AS KBs | Select CounterValue,Computer,KBs
Показать значение счетчика в килобайтах, а не в байтах.

**Пример 3**

    Type=WireData | EXTEND scale(TotalBytes,0,100) AS ScaledTotalBytes | Select ScaledTotalBytes,TotalBytes | SORT TotalBytes DESC
Масштабирование значение WireData TotalBytes, Здравствуйте, таким образом, что все результаты находятся в диапазоне от 0 до 100.

**Пример 4**

```
Type=Perf CounterName="% Processor Time" | EXTEND if(map(CounterValue,0,50,0,1),"HIGH","LOW") as UTILIZATION
```
Обозначить все значения счетчика производительности в диапазоне до 50 % как низкие (LOW), а все остальные — как высокие (HIGH).

**Пример 5**

```
Type= Perf CounterName="Disk Writes/sec" Computer="BaconDC01.BaconLand.com" | Extend product(CounterValue,60) as DWPerMin| measure max(DWPerMin) by InstanceName Interval 1HOUR
```
Вычисляет максимальное hello к дискам на запись в минуту для каждого диска на компьютере.

**Поддерживаемые функции**

| Функция | Описание | Примеры синтаксиса |
| --- | --- | --- |
| abs |Возвращает hello абсолютное значение hello указанного значения или функции. |`abs(x)` <br> `abs(-5)` |
| acos |Возвращает арккосинус значения или функции. |`acos(x)` |
| и |Возвращает значение true только в том случае, если все его операнды принимают tootrue. |`and(not(exists(popularity)),exists(price))` |
| asin |Возвращает арксинус значения или функции. |`asin(x)` |
| atan |Возвращает арктангенс значения или функции. |`atan(x)` |
| atan2 |Возвращает угол hello, возникающие в результате преобразования hello hello прямоугольных координат x, y toopolar координат. |`atan2(x,y)` |
| cbrt |Кубический корень. |`cbrt(x)` |
| ceil |Округляет число вверх tooan целое число со знаком. |`ceil(x)`  <br> `ceil(5.6)` — возвращает 6. |
| cos |Возвращает косинус угла. |`cos(x)` |
| cosh |Возвращает гиперболический косинус угла. |`cosh(x)` |
| def |Сокращенная форма по умолчанию. Возвращает hello значение поля «field». Если поле hello не существует, возвращает заданное значение по умолчанию hello и возвращает первое значение hello где: `exists()==true`. |`def(rating,5)`. Эта функция def() возвращает оценку hello или если указано без оценки в документе hello 5. <br> `def(myfield, 1.0)`является эквивалентом`if(exists(myfield),myfield,1.0)`. |
| deg |Преобразует радианы toodegrees. |`deg(x)` |
| div |`div(x,y)` делит x на y. |`div(1,y)` <br> `div(sum(x,100),max(y,1))` |
| dist |Возвращает hello расстояние между двумя векторами, (точками) в n мерном пространстве. Принимает hello питания, а также два или несколько экземпляров ValueSource и вычисляет hello расстояния между двумя векторами hello. Каждый экземпляр ValueSource должен иметь числовое значение. Должно быть четное число экземпляров ValueSource и hello метод предполагает, что hello Первая половина представляет первый вектор hello и hello Вторая половина — второй вектор hello. |`dist(2, x, y, 0, 0)`Вычисляет hello Евклидово расстояние между (0,0) и (x, y) для каждого документа. <br> `dist(1, x, y, 0, 0)`Вычисляет hello Манхэттенское расстояние между (0,0) и (x, y) для каждого документа. <br> `dist(2,,x,y,z,0,0,0)` — евклидово расстояние между (0,0,0) и (x,y,z) для каждого документа.<br>`dist(1,x,y,z,e,f,g)` — манхэттенское расстояние между (x,y,z) и (e,f,g), где каждая буква обозначает имя поля. |
| exists (существует) |Возвращает значение TRUE, если все поля hello существует элемент. |`exists(author)`Возвращает значение TRUE для любого документа, имеющего значение в поле «Автор» hello.<br>`exists(query(price:5.00))` — возвращает значение true, если поле price имеет значение 5,00. |
| exp |Число Эйлера возвращает возникает toopower x. |`exp(x)` |
| floor |Округляет число вниз tooan целое число со знаком. |`floor(x)`  <br> `floor(5.6)` — возвращает 5. |
| hypo |Возвращает значение sqrt(sum(pow(x,2),pow(y,2))) без промежуточного переполнения или потери значимости. |`hypo(x,y)`  <br> ` |
| if |Позволяет выполнять запросы с условиями. В `if(test,value1,value2)`, тест или ссылается tooa логическое значение или выражение, возвращающее логическое значение (TRUE или FALSE). `value1`hello значение возвращается функцией hello Если test возвращает значение TRUE. `value2`hello значение возвращается функцией hello Если test возвращает значение FALSE. В качестве выражения принимается любая функция, которая возвращает логические значения или даже числовые значения (тогда 0 будет интерпретироваться как false) либо строки (в этом случае пустая строка будет интерпретироваться как false). |`if(termfreq(cat,'electronics'),popularity,42)`Эта функция проверяет каждый документ toosee, если он содержит hello термина «electronics» в поле cat hello. Если его затем hello возвращается значение поля popularity hello. В противном случае возвращается значение 42 hello. |
| linear |Выполняет расчет `m*x+c`, где m и c — константы, а x — произвольная функция. Это значение эквивалентно слишком`sum(product(m,x),c)`, но несколько более эффективна, так как она реализована в виде одной функции. |`linear(x,m,c) linear(x,2,4)` возвращает `2*x+4` |
| ln |Возвращает hello натуральному логарифму hello указана функция. |`ln(x)` |
| log |Возвращает hello журнала функция указанное основание 10 hello. |`log(x)   log(sum(x,100))` |
| map |Сопоставляет все значения входной функции x, которые попадают в min и max, включительно toohello указанный целевой объект. Hello аргументы min и max должны быть константами. Hello аргументы target и default могут быть константами или функциями. Если hello значение x не входит в диапазон между min и max, возвращается значение x либо hello или значение по умолчанию возвращается в том случае, если оно указано как 5-й аргумент. |`map(x,min,max,target) map(x,0,0,1)`Изменяет все значения 0 too1. Может использоваться для обработки значений 0 по умолчанию.<br> `map(x,min,max,target,default)    map(x,0,100,1,-1)`Изменяет все значения между 0 и 100 too1 и все другие значения слишком-1.<br>  `map(x,0,100,sum(x,599),docfreq(text,solr))`Изменяет все значения в диапазоне от 0 до 100 toox + 599 и все другие значения toofrequency hello термина «solr» в поле текста hello. |
| max |Здравствуйте, возвращает максимальное числовое значение нескольких вложенных функций или констант, которые указаны в качестве аргументов: `max(x,y,...)`. функция max Hello также можно использовать для «получения нижней границы» другой функции или с помощью заданной константы.  Используйте hello `field(myfield,max)` синтаксис для выбора максимального значения одного многозначного поля hello. |`max(myfield,myotherfield,0)` |
| Min |Здравствуйте, возвращает минимальное числовое значение нескольких вложенных функций или констант, которые указаны в качестве аргументов: `min(x,y,...)`. функция min Hello также может быть полезна для предоставления функции с помощью константы «верхнюю границу». Используйте hello `field(myfield,min)` синтаксис для выбора hello минимального значения одного многозначного поля. |`min(myfield,myotherfield,0)` |
| mod (модуль) |Вычисляет модуль hello функции hello x на y функции hello. |`mod(1,x)` <br> `mod(sum(x,100), max(y,1))` |
| ms |Возвращает разницу в миллисекундах между заданными аргументами. Диапазон дат включает относительный toohello Unix or POSIX времени эпохи, полуночи 1 января 1970 г. UTC. Аргументы могут быть hello имя индексированного TrieDateField или математическими датами на основе константы даты или NOW. `ms()`является эквивалентом`ms(NOW)`, число миллисекунд с начала эпохи hello. `ms(a)`Возвращает hello число миллисекунд с начала эпохи hello, представляющий аргумент hello. `ms(a,b)`Возвращает hello число миллисекунд, за которые b предшествует, который является `a - b`. |`ms(NOW/DAY)`<br>`ms(2000-01-01T00:00:00Z)`<br>`ms(mydatefield)`<br>`ms(NOW,mydatefield)`<br>`ms(mydatefield,2000-01-01T00:00:00Z)`<br>`ms(datefield1,datefield2)` |
| not |значение Hello отрицание hello в оболочку функция. |`not(exists(author))` равно true, только если `exists(author)` имеет значение false. |
| или |Логическая дизъюнкция. |`or(value1,value2)` возвращает TRUE, если хотя бы одно из значений value1 и value2 равно TRUE. |
| pow |Вызывает hello указан базовый toohello указан питания. `pow(x,y)`вызывает x toohello степень y. |`pow(x,y)`<br>`pow(x,log(y))`<br>`pow(x,0.5)`Здравствуйте таким же, что и sqrt. |
| product |Возвращает hello произведение нескольких значений или функций, которые указаны в список с разделителями запятыми. `mul(...)` . |`product(x,y,...)`<br>`product(x,2)`<br>`product(x,y)`<br>`mul(x,y)` |
| recip |Выполняет обратную функцию, то есть `recip(x,m,a,b)` соответствует `a/(m*x+b)`, где m, a и b — константы, а x — функция любого уровня сложности. Если a = b и x >= 0, эта функция имеет максимальное значение 1, которое уменьшается по мере увеличения x. Увеличение значения hello и b приведет к перемещению всей функции tooa hello плоское часть кривой hello. Эти свойства позволяют использовать функцию для повышения рейтинга более свежих документов, указав в качестве x функцию `rord(datefield)`. |`recip(myfield,m,a,b)`<br>`recip(rord(creationDate),1,1000,1000)` |
| rad |Преобразует tooradians градусов. |`rad(x)` |
| rint |Округляет число toohello ближайшего целого. |`rint(x)`  <br> `rint(5.6)` — возвращает 6. |
| sin |Возвращает синус угла. |`sin(x)` |
| sinh |Возвращает гиперболический синус угла. |`sinh(x)` |
| scale |Масштабирует значения функции hello x, таким образом, что они входят в диапазон между hello указан minTarget и maxTarget включительно. Hello текущая реализация проходит через все hello функция значения tooobtain hello min и max, чтобы выбрать правильный масштаб hello. Текущая реализация Hello не может различать удаленные документы или документы, которые не имеют значения. Для обоих этих случаев используется значение 0,0. Это означает, что если обычно все больше 0,0 значения, можно по-прежнему получиться 0,0 как hello toomap значение min из. В этих случаях соответствующей `map()` функция может использоваться как значение tooa toochange 0,0 обходной путь в реальном диапазоне hello, как показано ниже:`scale(map(x,0,0,5),1,2)` |`scale(x,minTarget,maxTarget)`<br>`scale(x,1,2)`Масштабирует значения x, Здравствуйте, таким образом, все значения между 1 и 2 включительно. |
| sqrt |Возвращает hello квадратный корень из hello указанного значения или функции. |`sqrt(x)`<br>`sqrt(100)`<br>`sqrt(sum(x,100))` |
| strdist |Вычисляет hello расстояния между двумя строками. Использует интерфейса StringDistance средства проверки орфографии Lucene hello и поддерживает все hello реализаций, доступных в этом пакете. Также позволяет приложениям tooplug в своих собственных через ресурсов Solr возможности загрузки. strdist принимает аргументы `(string1, string2, distance measure)`. Возможные значения для меры расстояния:<ul><li>jw: расстояние Джаро — Винклера;</li><li>edit: расстояние Левенштейна или редакторское расстояние;</li><li>ngram: hello NGramDistance, если указано, можно также передавать размер ngram hello слишком. По умолчанию используется значение 2.</li><li>FQN: Полное доменное имя для реализации интерфейса StringDistance hello. Должен использоваться конструктор без аргументов.</li></ul> |`strdist("SOLR",id,edit)` |
| sub |Возвращает разность значений x-y из `sub(x,y)`. |`sub(myfield,myfield2)`<br>`sub(100,sqrt(myfield))` |
| Sum |Возвращает hello сумму нескольких значений или функций, которые указаны в список с разделителями запятыми. `add(...)` . |`sum(x,y,...)`<br>`sum(x,1)`<br>`sum(x,y)`<br>`sum(sqrt(x),log(y),z,0.5)`<br>`add(x,y)` |
| termfreq |Возвращает hello количество появлений термина hello поля «hello» для этого документа. |termfreq(text,'memory') |
| tan |Возвращает тангенс угла. |`tan(x)` |
| tanh |Возвращает гиперболический тангенс угла. |`tanh(x)` |

## <a name="search-field-and-facet-reference"></a>Справочник по полю и аспекту поиска
При использовании поиска журналов toofind данных в результатах отображаются различные поля и аспекты. Некоторые hello сведений могут показаться непонятными. Используйте следующие сведения toohelp понять результаты hello hello.

| Поле | Тип поиска | Описание |
| --- | --- | --- |
| TenantId |Все |Использовать toopartition данных. |
| TimeGenerated |Все |Использовать toodrive управления hello временной шкалой и селекторами времени (в поиске и на других экранах). Оно представляет, когда hello фрагмент данных был создан (обычно в агенте hello). Hello время выражено в формате ISO и всегда имеет значение UTC. В случае hello типов, основанных на существующем инструментировании (т. е. событий в журнале) обычно это реальное время, hello запись/строки/запись в журнале hello. Для некоторых hello других типов, которые создаются через пакеты управления или в облаке hello (например, рекомендации или предупреждения), hello представляет время другим. Это время hello новый фрагмент данных с моментальным снимком конфигурации определенного рода были собраны, или был создан на основе его рекомендация/оповещение. |
| EventID |Событие |Код события в журнале событий Windows hello. |
| EventLog |Событие |Журнал событий, где hello событие было зарегистрировано операционной системой Windows. |
| EventLevelName |Событие |Критический/предупреждение/информационный/успешное выполнение. |
| EventLevel |Событие |Числовое значение для уровня критический/ предупреждение/информационный/успешное выполнение (используйте вместо него EventLevelName, чтобы получить более простые и удобные для чтения запросы). |
| SourceSystem |Все |Источник данных hello (на основе присоединения служба toohello режиме). Например, System Center Operations Manager и служба хранилища Azure. |
| ObjectName |PerfHourly |Имя объекта производительности Windows. |
| InstanceName |PerfHourly |Имя экземпляра счетчика производительности Windows. |
| CounteName |PerfHourly |Имя счетчика производительности Windows. |
| ObjectDisplayName |PerfHourly, ConfigurationAlert, ConfigurationObject, ConfigurationObjectProperty |Отображаемое имя hello мишенью для правила сбора данных производительности в Operations Manager. Может также быть отображаемое имя hello hello объекта, обнаруженного службой оперативной аналитики или к какой hello было выдано оповещение. |
| RootObjectName |PerfHourly, ConfigurationAlert, ConfigurationObject, ConfigurationObjectProperty |Отображаемое имя родителя hello hello родительского элемента (в отношении двойного размещения) hello объекта распространяется правило сбора данных производительности в Operations Manager. Может также быть отображаемое имя hello hello объекта, обнаруженного службой оперативной аналитики или к какой hello было выдано оповещение. |
| Компьютер |Большинство типов |Имя компьютера, к которому принадлежит hello данных. |
| DeviceName |ProtectionStatus |Слишком относятся данные hello. имя компьютера (то же, что «Компьютер»). |
| DetectionId |ProtectionStatus | |
| ThreatStatusRank |ProtectionStatus |Ранг состояния угрозы является числовым представлением состояния угрозы hello. Аналогичные коды ответа tooHTTP, ранжирование hello имеет промежутки между числами hello (поэтому число угроз составляет 150, а не 100 или 0), оставляя места tooadd новые состояния. Для свертки состояния угрозы и состояния защиты hello намерение будет tooshow hello Наихудшее состояние hello компьютер находился за выбранный период времени hello. номера Hello ранжирования различных состояний hello, чтобы можно искать hello запись с наибольшим номером hello. |
| ThreatStatus |ProtectionStatus |Описание для ThreatStatus полностью соответствует описанию для ThreatStatusRank. |
| TypeofProtection |ProtectionStatus |Продукт защиты от вредоносных программ, обнаруженных в компьютере hello: none, средство удаления вредоносных программ Майкрософт, Forefront и т. д. |
| ScanDate |ProtectionStatus | |
| SourceHealthServiceId |ProtectionStatus, RequiredUpdate |Идентификатор службы работоспособности для агента этого компьютера. |
| HealthServiceId |Большинство типов |Идентификатор службы работоспособности для агента этого компьютера. |
| ManagementGroupName |Большинство типов |Имя группы управления для подключенных к Operations Manager агентов. В противном случае имеет пустое значение или значение NULL. |
| ObjectType |ConfigurationObject |Тип (как в типе/классе пакета управления Operations Manager) для этого объекта, обнаруженный функцией оценки конфигурации Log Analytics. |
| UpdateTitle |RequiredUpdate |Имя обновления hello, которое не найдено установленной. |
| PublishDate |RequiredUpdate |При публикации hello обновления в центре обновления Майкрософт. |
| сервер; |RequiredUpdate |Слишком относятся данные hello. имя компьютера (то же, что «Компьютер»). |
| Продукт |RequiredUpdate |Применяет hello обновления продукта. |
| UpdateClassification |RequiredUpdate |Тип обновления (например, накопительный пакет обновления или пакет обновления). |
| KBID |RequiredUpdate |Идентификатор статьи в базе знаний с описанием этой рекомендации или этого обновления. |
| WorkflowName |ConfigurationAlert |Имя hello правила или монитора, вызвавшего предупреждение hello. |
| Severity |ConfigurationAlert |Серьезность предупреждения hello. |
| Приоритет |ConfigurationAlert |Приоритет предупреждения hello. |
| IsMonitorAlert |ConfigurationAlert |Это предупреждение создается монитором (true) или правилом (false)? |
| AlertParameters |ConfigurationAlert |XML-ФАЙЛ с параметрами hello hello анализа журналов оповещения. |
| Context |ConfigurationAlert |XML с контекстом hello hello предупреждения анализа журналов. |
| Рабочая нагрузка |ConfigurationAlert |Технология или рабочая нагрузка, hello предупреждение ссылается. |
| AdvisorWorkload |Рекомендации |Технология или рабочая нагрузка, hello рекомендация относится. |
| Описание |ConfigurationAlert |Описание оповещения (краткое). |
| DaysSinceLastUpdate |UpdateAgent |Сколько дней назад (относительный tooTimeGenerated этой записи) этот агент устанавливал любое обновление из службы обновления Windows Server (WSUS) или Центра обновления Майкрософт? |
| DaysSinceLastUpdateBucket |UpdateAgent |Основанная на значении DaysSinceLastUpdate классификация того, как давно на компьютере в последний раз было установлено обновление из служб обновления Windows Server или Центра обновления Майкрософт, по заданным периодам. |
| AutomaticUpdateEnabled |UpdateAgent |Включена ли в данном агенте автоматическая проверка обновления? |
| AutomaticUpdateValue |UpdateAgent |— Автоматическое обновление проверки набора tooautomatically загрузки и установки, загружать только или только на проверку? |
| WindowsUpdateAgentVersion |UpdateAgent |Номер версии агента Центра обновления Майкрософт hello. |
| WSUSServer |UpdateAgent |На какой сервер WSUS нацелен этот агент обновления? |
| OSVersion |UpdateAgent |Версия hello операционной системы, в которой выполняется этот агент обновления. |
| Имя |Recommendation, ConfigurationObjectProperty |Имя или название рекомендации hello, или имя hello свойства из оценки конфигурации службы анализа журналов. |
| Значение |ConfigurationObjectProperty |Значение свойства из функции оценки конфигурации Log Analytics. |
| KBLink |Рекомендации |Статья toohello КБ URL-адрес с описанием этой рекомендации или обновления. |
| RecommendationStatus |Рекомендации |Рекомендации относятся hello немногим типам, записи которых получить обновленный, не только что добавленную toohello индекс поиска. Это состояние изменяется ли hello рекомендация активной или открытой, или служба аналитики журналов обнаруживает, что она была разрешена. |
| RenderedDescription |Событие |Готовое для просмотра описание события Windows (повторно используемый текст с заполненными параметрами). |
| ParameterXml |Событие |XML-ФАЙЛ с параметрами hello hello раздела данных события Windows (как показано в средстве просмотра событий). |
| EventData |Событие |XML-ФАЙЛ с раздела данных в целом hello события Windows (как показано в средстве просмотра событий). |
| Источник |Событие |Источник журнала событий, который создал событие hello. |
| EventCategory |Событие |Категория события hello, непосредственно из журнала событий Windows hello. |
| UserName |Событие |Имя пользователя события Windows hello (обычно это NT AUTHORITY\LOCALSYSTEM). |
| SampleValue |PerfHourly |Среднее значение для почасовой статистической обработки счетчика производительности hello. |
| Min |PerfHourly |Минимальное значение в hello почасовом интервале для почасовой статистической обработки счетчика производительности. |
| Max |PerfHourly |Максимальное значение в hello почасовом интервале для почасовой статистической обработки счетчика производительности. |
| Percentile95 |PerfHourly |Hello значение процентиля 95 для hello почасовом интервале для почасовой статистической обработки счетчика производительности. |
| SampleCount |PerfHourly |Количество выборок счетчиков производительности неформатированной были используется tooproduce этом почасовой статистической обработки записи. |
| Threat |ProtectionStatus |Имя обнаруженной вредоносной программы. |
| StorageAccount |W3CIISLog |Обнаружен Azure журнала hello учетной записи хранения. |
| AzureDeploymentID |W3CIISLog |Идентификатор развертывания Azure журнал hello hello облачной службы принадлежит. |
| Роль |W3CIISLog |Принадлежит роль журнал hello hello Azure облачной службы. |
| RoleInstance |W3CIISLog |Значение RoleInstance роли Azure, hello журнала hello принадлежит. |
| sSiteName |W3CIISLog |Веб-сайт IIS, hello журнала принадлежит too(metabase notation); поле s-sitename Hello в исходном журнале hello. |
| sComputerName |W3CIISLog |поле s-computername Hello в исходном журнале hello. |
| sIP |W3CIISLog |Был направлен запрос HTTP hello адрес IP-адреса сервера. Hello поле s-ip в исходном журнале hello. |
| csMethod |W3CIISLog |Используемая клиентом hello в запросе hello HTTP метод HTTP (например, GET или POST). Здравствуйте, cs-method в исходном журнале hello. |
| cIP |W3CIISLog |Запрос HTTP hello адрес IP-адреса клиента, откуда. поле c-ip Hello в исходном журнале hello. |
| csUserAgent |W3CIISLog |Агент пользователя HTTP, объявленный клиентом hello (браузер или другой). Здравствуйте, cs-user-agent в исходном журнале hello. |
| scStatus |W3CIISLog |Код состояния HTTP (например, 200/403/500) toohello клиентом hello server. Здравствуйте, cs-status в исходном журнале hello. |
| TimeTaken |W3CIISLog |Как долго (в миллисекундах), hello запрос обрабатывался toocomplete. Hello timetaken в исходном журнале hello. |
| csUriStem |W3CIISLog |Относительный URI (без адреса узла, например /search), который был запрошен. Hello cs-uristem в исходном журнале hello. |
| csUriQuery |W3CIISLog |Запрос URI. Запросы URI необходимы только для динамических страниц, например ASP-страниц, поэтому для статических страниц это поле обычно содержит дефис. |
| sPort |W3CIISLog |Порт сервера, который hello HTTP-запрос был отправлен слишком (и, который прослушивает IIS, так как он взял). |
| csUserName |W3CIISLog |Если запрос hello прошедшего проверку подлинности и не анонимный проверку подлинности имени пользователя. |
| csVersion |W3CIISLog |Версия протокола HTTP, используемого в запросе hello (например, HTTP/1.1). |
| csCookie |W3CIISLog |Сведения о файле cookie. |
| csReferer |W3CIISLog |Сайт последнего посещения пользователя hello. Этот сайт предоставил ссылку toohello текущего сайта. |
| csHost |W3CIISLog |Заголовок узла (например, www.mysite.com), который был запрошен. |
| scSubStatus |W3CIISLog |Код ошибки подсостояния . |
| scWin32Status |W3CIISLog |Код состояния Windows. |
| csBytes |W3CIISLog |Байты, отправленные в запросе hello hello клиент toohello сервер. |
| scBytes |W3CIISLog |Байт, возвращенных в hello ответа от клиента toohello server hello. |
| ConfigChangeType |ConfigurationChange |Тип изменения (например, службы Windows и программное обеспечение). |
| ChangeCategory |ConfigurationChange |Категория изменения hello (Added/изменено или удалено). |
| SoftwareType |ConfigurationChange |Тип программного обеспечения (обновление, приложение). |
| SoftwareName |ConfigurationChange |Название программы hello (только изменения toosoftware применимо). |
| Издатель |ConfigurationChange |Поставщик, опубликовавший программное обеспечение hello (только изменения toosoftware применимо). |
| SvcChangeType |ConfigurationChange |Тип изменения, примененного к службе Windows (состояние, тип запуска, путь, учетная запись службы). Это только применимые tooWindows изменения в службе. |
| SvcDisplayName |ConfigurationChange |Отображаемое имя измененной службы hello. |
| SvcName |ConfigurationChange |Имя измененной службы hello. |
| SvcState |ConfigurationChange |Новое (текущее) состояние службы hello. |
| SvcPreviousState |ConfigurationChange |Предыдущее известное состояние hello службы (применимо только в случае изменения состояния службы). |
| SvcStartupType |ConfigurationChange |Тип запуска службы. |
| SvcPreviousStartupType |ConfigurationChange |Предыдущий тип запуска службы (применимо только в случае изменения типа запуска службы). |
| SvcAccount |ConfigurationChange |Учетная запись службы. |
| SvcPreviousAccount |ConfigurationChange |Предыдущая учетная запись службы (применимо только в случае изменения учетной записи службы). |
| SvcPath |ConfigurationChange |Путь toohello исполняемому файлу службы Windows hello. |
| SvcPreviousPath |ConfigurationChange |Предыдущий путь hello исполняемый файл для hello службы Windows (применимо, только если он изменен). |
| SvcDescription |ConfigurationChange |Описание службы hello. |
| Предыдущий |ConfigurationChange |Предыдущее состояние данного программного обеспечения (установлено, не установлено, предыдущая версия). |
| Текущее значение |ConfigurationChange |Последнее состояние данного программного обеспечения (установлено, не установлено, текущая версия). |

## <a name="next-steps"></a>Дальнейшие действия
Дополнительные сведения о расширенных базах данных.

* Ознакомьтесь с [входа выполняет](log-analytics-log-searches.md) tooview подробные данные, собранные решения.
* Используйте [пользовательские поля в службе анализа журналов](log-analytics-custom-fields.md) tooextend запросов поиска журналов.
