---
title: "aaaDebug модели в машинном обучении Azure | Документы Microsoft"
description: "Как toodebug ошибки, возникшие по Обучение модели и оценка модели модулей в машинном обучении Azure."
services: machine-learning
documentationcenter: 
author: garyericson
manager: jhubbard
editor: cgronlun
ms.assetid: 629dc45e-ac1e-4b7d-b120-08813dc448be
ms.service: machine-learning
ms.workload: data-services
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 03/14/2017
ms.author: bradsev;garye
ms.openlocfilehash: ee38ca8ce38d4fc7add5ba70c80ab9bb2ceaf1d4
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="debug-your-model-in-azure-machine-learning"></a>Отладка машинного обучения модели в Azure

В этой статье описываются hello потенциальных причин, почему либо после двух отказов hello может возникать при работе модели:

* Hello [Обучение модели] [ train-model] модуля приводит к ошибке 
* Hello [модель оценки] [ score-model] модуль выдает неверные результаты 

[!INCLUDE [machine-learning-free-trial](../../includes/machine-learning-free-trial.md)]

## <a name="train-model-module-produces-an-error"></a>Модуль "Обучение модели" выдает ошибку

![рисунок 1](./media/machine-learning-debug-models/train_model-1.png)

Hello [Обучение модели] [ train-model] модуль ожидает два входа:

1. Тип Hello модели машинного обучения из коллекции hello моделях, предоставляемых машинного обучения Azure.
2. Hello обучающие данные с указанного столбца метки, который указывает hello переменной toopredict (hello других столбцов, считаются toobe функции).

Этот модуль может вызвать ошибку в hello в следующих случаях:

1. столбец метки Hello указан неправильно. Это может произойти, если более чем один столбец выбран в качестве метки hello или выбран неправильный столбца индекса. Например второй вариант hello будет применяться, если индекс столбца 30 используется с входного набора данных, содержащей только 25 столбцов.

2. Hello набора данных не содержит какие-либо столбцы компонентов. Например если hello входной набор данных содержит только один столбец, который помечен как столбец метки hello, будет нет функций с какую модель toobuild hello. В этом случае hello [Обучение модели] [ train-model] модуль выдает ошибку.

3. Hello входного набора данных (функций или метку) содержит значение бесконечности.

## <a name="score-model-module-produces-incorrect-results"></a>Модуль "Score Model" (Оценка модели) выдает неправильные результаты

![рисунок 2](./media/machine-learning-debug-models/train_test-2.png)

В обычной обучение и тестирование эксперименте для контролируемого обучения, hello [разбиение данных] [ split] модуль hello исходный набор данных разделяется на две части: одной части модели используется tootrain hello и используется одна часть tooscore, насколько хорошо выполняется hello обученной модели. Hello обученной модели является используется tooscore hello проверочных данных, после чего результаты hello, вычисленное toodetermine hello точность модели hello.

Hello [модель оценки] [ score-model] модуль требует наличия двух входных значений:

1. Вывод hello обученной модели [Обучение модели] [ train-model] модуля.
2. Оценки набора данных, отличный от hello набора данных используется модель tootrain hello.

Он, возможно, что даже если эксперимент hello завершится успешно, hello [модель оценки] [ score-model] модуль выдает неверные результаты. Несколько сценариев возможно возникновение этого toohappen:

1. Если hello указанная категориальные метки и модель регрессии проводится на данных hello, неверные выходные данные будут получены с hello [модель оценки] [ score-model] модуля. Это обусловлено тем, что для регрессии требуется непрерывная переменная ответа. В этом случае было бы более подходящими toouse модели классификации. 

2. Аналогично Если для набора данных наличие чисел с плавающей запятой в столбец метки hello является обученной модели классификации, может привести к нежелательным результатам. Это обусловлено тем, что для классификации требуется дискретная переменная отклика, с которой можно использовать только значения, соответствующие конечному и обычно несколько малому набору классов.

3. Здравствуйте, если hello оценки набора данных не содержит все модели hello tootrain hello используемых функций, [модель оценки] [ score-model] приводит к ошибке.

4. Здравствуйте, если строка hello оценки набор данных содержит отсутствующее значение или бесконечное значение для любой из его компонентов, [модель оценки] [ score-model] не приводят к соответствующей toothat строки все выходные данные.

5. Hello [модель оценки] [ score-model] может привести к одинаковых выходов для всех строк в hello оценки набора данных. Это может произойти, например, при попытке классификации с помощью леса принятия решений, если вы выбрали toobe больше, чем hello количеству обучающих примеров доступных hello минимальное число выборок для конечного узла.

<!-- Module References -->
[score-model]: https://msdn.microsoft.com/library/azure/401b4f92-e724-4d5a-be81-d5b0ff9bdb33/
[split]: https://msdn.microsoft.com/library/azure/70530644-c97a-4ab6-85f7-88bf30a8be5f/
[train-model]: https://msdn.microsoft.com/library/azure/5cc7053e-aa30-450d-96c0-dae4be720977/

