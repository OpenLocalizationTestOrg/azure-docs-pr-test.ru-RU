---
title: "руководство по aaaGet работы для разработчиков в Azure | Документы Microsoft"
description: "В этом разделе содержатся важные сведения для разработчиков, tooget работы с использованием платформы Microsoft Azure hello для своих нужд."
services: 
cloud: 
documentationcenter: 
author: ggailey777
manager: erikre
ms.assetid: 
ms.service: na
ms.workload: na
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 06/05/2017
ms.author: glenga
ms.openlocfilehash: 72dc2678db7738923d4bc7783e297fea6fcded83
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="get-started-guide-for-azure-developers"></a>Руководство по началу работы для разработчиков Azure

## <a name="what-is-azure"></a>Что такое Azure?

Azure является полный облачная платформа, которая размещения существующие приложения, упрощающих разработку hello новых приложений и даже расширения локальных приложений. Azure интегрируется hello облачные службы, необходимые toodevelop, тестирования, развертывания и управления приложениями — при одновременном использовании преимуществ hello эффективности облачных вычислений.

Разместив свои приложения в Azure, можно начать с малого и незначительно масштабировать приложения по мере роста потребностей клиентов. Azure также предлагает надежность hello, которые необходимы для приложений, высокого уровня доступности, даже в том числе отработку отказа между разных регионах. Hello [портал Azure](https://portal.azure.com) позволяет легко управлять всем службам Azure. Для программного управления службами можно также использовать интерфейсы API служб и шаблоны.

**Для кого предназначен этот**: в этом руководстве является введение toohello платформы Azure для разработчиков приложений. Он предоставляет советы и необходимость создания приложений в Azure или перенос существующих приложений tooAzure toostart направление.

## <a name="where-do-i-start"></a>С чего начать?

Со всеми службами hello, предоставляемые Azure может быть сложной toofigure задача ожидания службы, которые необходимо toosupport архитектуры решения. Этот раздел выделяет hello Azure службы обычно используют разработчики. Список всех служб Azure см. в разделе hello [документации Azure](../../index.md).

Во-первых, необходимо решить, как toohost вашего приложения в Azure. Нужна toomanage всей инфраструктуры как виртуальные машины (VM). Можно использовать средства управления платформы hello, предоставляемых Azure? Возможно, требуется без сервера framework toohost выполнение кода только?

Приложению требуется облачное хранилище, и Azure предоставляет несколько вариантов такого хранилища. Можно воспользоваться преимуществами корпоративной аутентификации Azure. Существуют также инструменты для облачной разработки и мониторинга, и большинство служб размещения обеспечивает интеграцию DevOps.

Теперь рассмотрим некоторые hello конкретных служб, которые мы рекомендуем исследование для приложения.

### <a name="application-hosting"></a>Размещение приложений

Azure предоставляет несколько облачных вычислительных предложений toorun приложения, чтобы не нужно tooworry о подробностями инфраструктуры hello. Можно легко масштабировать ресурсы по мере расширения использования приложения.

Azure предлагает службы, обеспечивающие потребности в разработке и размещении приложений. Azure предоставляет инфраструктуру как услуга (IaaS) toogive полный контроль над размещения приложений. Предложения (PaaS) платформа как служба Azure предоставляют hello управляемые toopower необходимые службы приложений. Существует это верно даже без сервера размещения в Azure все, что нужно toodo где писать код.

![Варианты размещения приложений в Azure](./media/azure-developer-guide/azure-developer-hosting-options.png)


#### <a name="azure-app-service"></a>Служба приложений Azure 

Получить быстрый путь toopublish hello веб-проектов, рассмотрим службу приложений Azure. Службы приложений позволяет легко tooextend вашего веб-приложения toosupport мобильным клиентам и опубликовать простой в использовании интерфейсов API REST. Эта платформа обеспечивает аутентификацию посредством поставщиков социальных сетей, автомасштабирование на основе трафика, тестирование в рабочей среде, а также непрерывное развертывание и развертывание на основе контейнера.

При создании приложения в службе приложений выберите один из hello следующие типы:

- [Веб-приложения](../../app-service-web/app-service-web-overview.md). Позволяют размещать веб-сайты и веб-приложения, созданные на языке .NET, Java, PHP, Node.js или Python.

- [Мобильные приложения](../../app-service-mobile/app-service-mobile-value-prop.md): расширяет веб-приложений toosupport доступа с мобильных устройств. Это позволяет использовать аутентификацию с помощью поставщиков социальных сетей и Azure Active Directory (Azure AD), а также обеспечивает серверное хранилище и интеграцию с [центрами уведомлений Azure](../../notification-hubs/notification-hubs-push-notification-overview.md) для использования push-уведомлений.

- [Приложения API](../../app-service-api/app-service-api-apps-why-best-platform.md): позволяет безопасно предоставлять собственные интерфейсы API в облаке hello метаданных Swagger, что позволяет клиентам легко обрабатывать их.

Поскольку все три приложения типы используют Здравствуйте, среда выполнения приложения службы, размещения веб-сайта, поддержка мобильных клиентов и предоставлять собственные интерфейсы API в Azure, все из hello того же проекта или решения. toolearn Дополнительные сведения о службе приложения в разделе [как приложение службы работает](../../app-service/app-service-how-works-readme.md).

Служба приложений была разработана с ориентацией на DevOps. Она поддерживает различные инструменты для публикации и развертываний с непрерывной интеграцией, включая веб-перехватчики GitHub, Jenkins, Visual Studio Team Services, TeamCity и пр.

Ваш существующий tooApp приложений службы можно перенести с помощью hello [online миграция](https://www.migratetoazure.net/).

>**Когда toouse**: использования службы приложений при миграции существующих tooAzure веб-приложений, и при необходимости полностью управляемая платформа размещения веб-приложения. Службы приложений также можно использовать при необходимости toosupport мобильных клиентов или предоставляют интерфейсы API REST с вашим приложением.

>**Приступая к работе**: службы приложений, позволяет легко toocreate и развертывание первого [веб-приложения](../../app-service-web/web-sites-dotnet-get-started.md), [мобильного приложения](../../app-service-mobile/app-service-mobile-ios-get-started.md), или [приложения API](../../app-service-api/app-service-api-dotnet-get-started.md).

>**Попробуйте прямо сейчас**: приложение службы можно подготовить платформы hello tootry кратковременных приложения без необходимости toosign для использования учетной записи Azure. Повторите платформы hello и [создать приложение службы приложений Azure](https://tryappservice.azure.com/).

#### <a name="azure-virtual-machines"></a>Виртуальные машины Azure

Как поставщик инфраструктуры как услуга (IaaS) Azure позволяет развертывать tooor перенести tooeither вашего приложения Windows или виртуальных машин Linux. Вместе с виртуальной сети Azure виртуальные машины Azure поддерживает развертывание hello tooAzure Windows или виртуальных машин Linux. С виртуальными машинами у вас есть полный контроль над hello конфигурации машины hello. При использовании виртуальных машин вы отвечаете за установку, настройку и обслуживание всего серверного программного обеспечения, а также за установку исправлений для операционной системы.

Из-за уровня hello элемента управления, у вас есть с виртуальными машинами можно запустить широкий диапазон серверных рабочих нагрузок в Azure, которая не помещаются в модели PaaS. К этим рабочим нагрузкам относятся серверы базы данных, Windows Server Active Directory и Microsoft SharePoint. Дополнительные сведения см. в разделе Документация по виртуальным машинам hello в любом [Linux](/azure/virtual-machines/linux/) или [Windows](/azure/virtual-machines/windows/).

>**Когда toouse**: используйте виртуальные машины, если требуется полный контроль над вашего приложения инфраструктуры или toomigrate локальных приложений рабочих нагрузок tooAzure без необходимости изменения toomake.

>**Приступая к работе**: создание [ВМ Linux](../../virtual-machines/virtual-machines-linux-quick-create-portal.md) или [виртуальной Машины Windows](../../virtual-machines/virtual-machines-windows-hero-tutorial.md) из hello портал Azure.

#### <a name="azure-functions-serverless"></a>Функции Azure (бессерверные)

А не беспокоясь о построении и управлении всей инфраструктуры toorun приложения или hello кода. Что делать, если можно просто написать собственный код и открыть его в ответ tooevents или по расписанию?  [Функции Azure](../../azure-functions/functions-overview.md) — «без сервера» a-предложения, что позволяет просто создавать hello код, требуемый стиль. При использовании службы "Функции Azure" выполнение кода активируется HTTP-запросами, веб-перехватчиками, событиями облачной службы или активируется по расписанию. Вы можете программировать на выбранном вами языке разработки, включая C\#, F\#, Node.js, Python и PHP. С выставления счетов на основе использования вы платите только за hello время выполнения кода и Azure масштабируется по мере необходимости.

>**Когда toouse**: используйте Azure функции, если у вас есть код, который вызывается с другими службами Azure, веб-событий, либо по расписанию. Также можно использовать функции, если не требуется hello издержки проекте размещенного или когда требуется только toopay для hello запуске кода. toolearn более, в разделе [Общие сведения о функциях Azure](../../azure-functions/functions-overview.md).

>**Приступая к работе**: выполните учебника функции hello слишком[создания вашего первого функции](../../azure-functions/functions-create-first-azure-function.md) из портала hello.

>**Попробуйте прямо сейчас**: функции Azure позволяет выполнять код без необходимости toosign для использования учетной записи Azure. Опробуйте ее возможности прямо сейчас и [создайте свою первую функцию Azure](https://tryappservice.azure.com/).

#### <a name="azure-service-fabric"></a>Azure Service Fabric

Azure Service Fabric — это платформа распределенных систем, который позволяет легко toobuild упаковки, развертывания и управления масштабируемыми и надежными микрослужбами. Кроме того, эта платформа содержит полный набор функций для управления приложениями, который позволяет подготавливать, развертывать, отслеживать, обновлять и исправлять приложения, а также удалять развернутые приложения. Приложения, которые выполняются на общий пул машин, можно приступить к работе и масштабировать toohundreds или тысяч компьютеров при необходимости.

Service Fabric поддерживает веб-API благодаря использованию Open Web Interface для .NET (OWIN) и ASP.NET Core. Эта служба предоставляет пакеты SDK для создания служб в среде Linux с помощью .NET Core и Java. toolearn Дополнительные сведения о Service Fabric. в разделе hello [план обучения Service Fabric](https://azure.microsoft.com/documentation/learning-paths/service-fabric/).

>**Когда toouse:** Service Fabric удобна при создании приложения или перезаписи существующего приложения toouse архитектура микрослужбы. Используйте Service Fabric, если требуются дополнительные контроль над или прямой доступ к базовой инфраструктурой hello.

>**Приступая к работе**. [Создайте свое первое приложение Azure Service Fabric](../../service-fabric/service-fabric-create-your-first-application-in-visual-studio.md).

### <a name="enhance-your-applications-with-azure-services"></a>Расширение возможностей приложений с помощью служб Azure

Кроме tooapplication размещение, Azure предоставляет предложения услуг, которые могут расширить функциональность hello, разработки и обслуживания приложений, как в облаке hello и локальных.

#### <a name="hosted-storage-and-data-access"></a>Размещенное хранилище и доступ к данным

Большинство приложений должны хранения данных, поэтому независимо от того как вы решите toohost приложения в Azure, рекомендуется один или несколько hello следующие службы хранения и обработки данных.

-   **База данных SQL Azure**: Azure под управлением версии подсистемы hello Microsoft SQL Server для хранения реляционных табличных данных в облаке hello. База данных SQL обеспечивает прогнозируемую производительность, масштабируемость без простоев, непрерывность бизнес-процессов и защиту данных.

    >**Когда toouse**: Если приложению требуется хранилище данных с ссылочной целостности, поддержка транзакций и поддержка для запросов на языке TSQL.

    >**Приступая к работе**: [создать базу данных SQL в минутах, с помощью портала Azure hello](../../sql-database/sql-database-get-started.md).

-   **Служба хранилища Azure**. Обеспечивает надежное высокодоступное хранилище для больших двоичных объектов, очередей, файлов и других типов нереляционных данных. Хранилище обеспечивает основу hello хранилища для виртуальных машин.

    >**Когда toouse**: Если приложение хранит нереляционных данных, таких как пары "ключ значение" (таблицы), большие двоичные объекты, файлы общих папок или сообщений (очередей).

    >**Приступая к работе**. Выберите один из этих типов: хранилище [BLOB-объектов](../../storage/blobs/storage-dotnet-how-to-use-blobs.md), [таблиц](../../cosmos-db/table-storage-how-to-use-dotnet.md), [очередей](../../storage/queues/storage-dotnet-how-to-use-queues.md) или [файлов](../../storage/files/storage-dotnet-how-to-use-files.md).

-   **Azure DocumentDB**. Полностью управляемая и масштабируемая служба базы данных NoSQL, поддерживающая SQL-запросы данных объектов. Для доступа к DocumentDB можно использовать имеющиеся драйверы MongoDB.
    >**Когда toouse:** когда приложению может tooexecute toobe SQL-запросов к документам JSON, или если вы используете MongoDB.

    >**Приступая к работе**. [Создайте консольное приложение C# для DocumentDB](../../documentdb/documentdb-get-started.md). Если вы являетесь разработчиком для MongoDB, ознакомьтесь с разделом [Знакомство с API для MongoDB в Azure Cosmos DB](../../documentdb/documentdb-protocol-mongodb.md).

Можно использовать [фабрики данных Azure](../../data-factory/data-factory-introduction.md) toomove существующих локальных данных tooAzure. Если вы не готовы toomove данных toohello облака, [гибридных подключений](../../biztalk-services/integration-hybrid-connection-overview.md) в службы BizTalk позволяет подключаться приложение tooon локальные ресурсы, размещенные службе приложений. Можно также подключиться tooAzure данных службы и службы хранилища из локальных приложений.

#### <a name="docker-support"></a>Поддержка Docker

Контейнеры Docker, представляющие собой разновидность виртуализации ОС, позволяют развертывать приложения более эффективным и предсказуемым способом. Контейнерного приложение работает в рабочей среде hello таким же способом, как и в системах разработки и тестирования. Контейнерами можно управлять с помощью стандартных инструментов Docker. Можно использовать существующие навыки и toodeploy популярных средств с открытым исходным кодом и управлять приложениями на основе контейнера в Azure.

Azure предоставляет несколько способов toouse контейнеры в ваших приложениях.

-   **Расширение виртуальной Машины Docker Azure**: позволяет настраивать ВМ с tooact средств Docker узел Docker.

    >**Когда toouse**: при необходимости развертывания toogenerate согласованное контейнера для приложений на виртуальной Машине, или при необходимости toouse [Docker Compose](https://docs.docker.com/compose/overview/).

    >**Приступая к работе**: [создать среду Docker в Azure с помощью расширения виртуальной Машины Docker hello](../../virtual-machines/virtual-machines-linux-dockerextension.md).

-   **Служба Azure контейнер**: позволяет создавать, настраивать и управлять кластером виртуальных машин, которые являются предварительно настроенных toorun контейнерных приложений. toolearn Дополнительные сведения о службе контейнера в разделе [введение контейнера службы Azure](../../container-service/container-service-intro.md).

    >**Когда toouse**: при необходимости toobuild средах рабочей среде, масштабируемые, обеспечивающие дополнительные средства планирования и управления, либо при развертывании кластера с помощью Docker Swarm.

    >**Приступая к работе**. [Разверните кластер службы контейнеров](../../container-service/dcos-swarm/container-service-deployment.md).

-   **Виртуальная машина Docker**. Позволяет установить подсистему Docker и управлять ей на виртуальных узлах с помощью команд виртуальной машины Docker.

    >**Когда toouse**: при необходимости tooquickly прототип приложения путем создания единого узла Docker.

-   **Пользовательский образ Docker для службы приложений**. Позволяет использовать контейнеры Docker из реестра контейнеров или контейнер клиента при развертывании веб-приложения на платформе Linux.

    >**Когда toouse**: при развертывании веб-приложения на образа Docker tooa Linux.

    >**Приступая к работе**. [Используйте пользовательский образ Docker для службы приложений на платформе Linux](../../app-service-web/app-service-linux-using-custom-docker-image.md).

### <a name="authentication"></a>Аутентификация

Важно toonot только знать, кто использует вашего приложения, но также tooprevent несанкционированный доступ к ресурсам tooyour. Azure предоставляет несколько способов tooauthenticate клиентов из вашего приложения.

-   **Azure Active Directory (Azure AD)**: hello многопользовательских, облачной идентификацией и доступом служба управления Microsoft. Можно добавить единого входа (SSO) tooyour приложений путем интеграции с Azure AD. Можно получить доступ к свойствам каталога с помощью hello Azure AD Graph API напрямую или hello Microsoft Graph API. Можно интегрировать с Azure AD поддержку инфраструктуры авторизации hello OAuth2.0 и откройте идентификатор подключения с использованием собственного конечных точек HTTP/REST и hello библиотеки проверки подлинности многоплатформенных Azure AD.

    >**Когда toouse**: при необходимости tooprovide Единого входа, работать с данными на основе графа, либо проверки подлинности пользователей на основе домена.

    >**Приступая к работе**: hello, см. toolearn [руководство разработчика Azure Active Directory](../../active-directory/active-directory-developers-guide.md).

-   **Проверка подлинности службы приложения**: при выборе службы приложений toohost приложения можно также получить поддержку встроенной проверки подлинности для Azure AD, вместе с поставщиками удостоверений из социальных сетей, включая Facebook, Google, Microsoft и Twitter.

    >**Когда toouse**: при необходимости tooenable проверки подлинности в приложении службы приложений с помощью Azure AD Поставщики удостоверений из социальных сетей, или оба.

    >**Приступая к работе**: toolearn Дополнительные сведения о проверки подлинности в службе приложений. в разделе [проверки подлинности и авторизации в службе приложений Azure](../../app-service/app-service-authentication-overview.md).

toolearn Дополнительные сведения о рекомендации по обеспечению безопасности в Azure, в разделе [шаблоны и рекомендации по безопасности Azure](../../security/security-best-practices-and-patterns.md).

### <a name="monitoring"></a>Мониторинг

Вместе с приложением работающий в Azure необходимо производительность может toomonitor toobe, отслеживание проблем и как клиенты, которые используют приложения. Azure предоставляет несколько вариантов мониторинга.

-   **Visual Studio Application Insights**: расширяемый аналитики, размещенной в Azure служба, которая интегрируется с Visual Studio toomonitor динамических веб-приложений. Он предоставляет hello данные, необходимые toocontinuously повысить hello производительность и удобство использования приложения, ли они размещены в Azure или нет.

    >**Приступая к работе**: hello выполните [учебника Application Insights](../../application-insights/app-insights-overview.md).

-   **Монитор Azure**: служба, которая позволяет вам toovisualize, запрос, маршрута, архивации и act с метриками hello и журналы, созданные инфраструктурой Azure и ресурсы. Монитор предоставляет hello представления данных, см. в hello портал Azure и имеет единый источник для наблюдения за ресурсами Azure.
 
    >**Приступая к работе**. [Начните работу с Azure Monitor](../../monitoring-and-diagnostics/monitoring-get-started.md).

### <a name="devops-integration"></a>Интеграция DevOps

Подготовка виртуальных машин или публикации веб-приложений с непрерывной интеграцией, Azure интегрируется с большинством популярных средств DevOps hello. Поддержка средств, таких как Jenkins, GitHub, Puppet, Chef, TeamCity, Ansible, VSTS и другие можно работать со средствами hello уже есть и максимизации ваши существующие навыки.

>**Попробуйте прямо сейчас:** [пробное использование нескольких средств интеграции hello DevOps](https://azure.microsoft.com/try/devops/).

>**Приступая к работе**: toosee DevOps параметры для приложения службы приложений. в разделе [tooAzure непрерывного развертывания службы приложений](../../app-service-web/app-service-continuous-deployment.md).


## <a name="azure-regions"></a>Регионы Azure

Azure является глобальной облачная платформа, которая находится в общем доступе во многих регионах вокруг Здравствуй, мир. При подготовке службы, приложения или виртуальной Машины в Azure, задаваемые tooselect области, которая представляет определенный центр обработки данных, где выполняется приложение или где хранятся данные. Эти области соответствуют toospecific расположения, которые публикуются на hello [регионах Azure](https://azure.microsoft.com/regions/) страницы.

### <a name="choose-hello-best-region-for-your-application-and-data"></a>Выберите регион наиболее hello для приложений и данных

Одним из преимуществ использования Azure hello является, что вы можете развернуть центрах обработки данных приложений toovarious земного шара hello. Hello регионе может повлиять на производительность приложения hello. Например это лучше toochoose регион, который находится ближе toomost задержке tooreduce вашей клиентов на сетевые запросы. Вы также можете tooselect области toomeet hello юридических требований для распространения приложения, в некоторых странах. Это всегда Лучшие учебные toostore данные приложения hello в одном центре обработки данных или в центре обработки данных в ближайшем как возможные toohello центра обработки данных, на котором размещается приложение.

### <a name="multi-region-apps"></a>Приложения с поддержкой нескольких регионов

Хотя и маловероятно, не невозможным toogo весь центр обработки данных в автономный режим из-за события, такого как стихийное бедствие или сбоя Интернета. Это лучший подход toohost жизненно важных бизнес-приложений в более одного центра обработки данных tooprovide максимальный уровень доступности. Размещение в нескольких регионах может также снизить задержку для глобальных пользователей и обеспечивает дополнительную гибкость при обновлении приложений.

Некоторые службы, такие как виртуальной машины и службы приложения, используйте [диспетчера трафика Azure](../../traffic-manager/traffic-manager-overview.md) tooenable поддержки нескольких регионах с переходом между областей toosupport высокого уровня доступности корпоративных приложений. С примером можно ознакомиться в разделе [Run a web application in multiple regions](../../guidance/guidance-web-apps-multi-region.md) (Выполнение веб-приложения в нескольких регионах).

>**Когда toouse**: при наличии enterprise и высокого уровня доступности приложений, которые выигрывают от репликации и отработки отказа.

## <a name="how-do-i-manage-my-applications-and-projects"></a>Как управлять приложениями и проектами?

Azure предоставляет широкий набор функций для вас toocreate и управление ресурсами Azure, приложений и проектов — программными средствами и в hello [портал Azure](https://portal.azure.com/).

### <a name="command-line-interfaces-and-powershell"></a>Интерфейсы командной строки и PowerShell

Azure предоставляет два способа toomanage вашим приложениям и службам из hello командной строки с помощью Bash, терминалов, hello командной строки или средством командной строки по выбору. Обычно hello же задачи можно выполнять из командной строки hello как hello портал Azure, таких как создание и настройка виртуальных машин, виртуальных сетей, веб-приложений и других служб.

-   [Azure интерфейс командной строки (CLI)](../../xplat-cli-install.md): позволяет подключаться tooan подписки Azure и программировать различные задачи в Azure с ресурсами из командной строки hello.

-   [Azure PowerShell](../../powershell-install-configure.md): toomanage Azure предоставляет набор модулей с командлетами, которые позволяют ресурсы с помощью Windows PowerShell.

### <a name="azure-portal"></a>Портал Azure

Hello портал Azure — это приложение веб сервера, можно использовать toocreate, управление и удаление ресурсов Azure и служб. Hello портал Azure находится в каталоге <https://portal.azure.com>. Он включает в себя настраиваемой панели мониторинга, средства для управления ресурсами Azure и параметры доступа toosubscription и информацией о выставлении счетов. Дополнительные сведения см. в разделе hello [портала для Azure — Обзор](../../azure-portal-overview.md).

### <a name="rest-apis"></a>Интерфейсы API REST

Azure основан на набор API REST, поддерживающих hello портал Azure пользовательского интерфейса. Большинство этих интерфейсов API REST, также поддерживаемые toolet программным способом подготовки и управления ресурсами Azure и приложениям с любого устройства с поддержкой Интернета. Hello весь набор документации по REST API см. в разделе hello [ссылки на пакет SDK Azure REST](https://docs.microsoft.com/rest/api/).

### <a name="apis"></a>Интерфейсы API

Кроме tooREST API-интерфейсы, многие службы Azure также позволяют программно управлять ресурсами из приложений с помощью Azure пакеты SDK платформ, включая пакеты SDK для hello следующие платформы для разработки:

-   [.NET](https://go.microsoft.com/fwlink/?linkid=834925)
-   [Node.js](http://azure.github.io/azure-sdk-for-node/)
-   [Java](https://docs.microsoft.com/java/api/)
-   [PHP](https://github.com/Azure/azure-sdk-for-php/blob/master/README.md)
-   [Python](http://azure-sdk-for-python.readthedocs.io/en/latest/)
-   [Ruby](https://github.com/Azure/azure-sdk-for-ruby/blob/master/README.md)

Службы, такие как [мобильные приложения](../../app-service-mobile/app-service-mobile-dotnet-how-to-use-client-library.md) и [служб мультимедиа Azure](../../media-services/media-services-dotnet-how-to-use.md) предоставляют toolet SDK на стороне клиента доступ к службам из веб- и мобильных клиентских приложений.

### <a name="azure-resource-manager"></a>Диспетчер ресурсов Azure 
    
Запуск приложения в Azure скорее всего, включает работу с несколькими службами Azure, все части, следующий за hello же жизненный цикл и может рассматриваться как логической единицей. Например, веб-приложение может использовать веб-приложения, Базу данных SQL, службу хранилища, кэш Redis для Azure и службы сети доставки содержимого Azure. [Диспетчер ресурсов Azure](../../azure-resource-manager/resource-group-overview.md) позволяет работать с ресурсами hello в приложении как группу. Можно развернуть, обновить или удалить все ресурсы hello в один, согласованной операции.

В дополнение к этому toologically группирование и управление связанных ресурсов, диспетчера ресурсов Azure включает возможности развертывания, которые позволяют настраивать hello развертывание и настройку связанные ресурсы. Например, с помощью Resource Manager можно развернуть и настроить приложение, состоящее из нескольких виртуальных машин, подсистемы балансировки нагрузки и базы данных SQL Azure, как единое целое.

Для разработки таких развертываний используется шаблон Azure Resource Manager, представляющий собой документ в формате JSON. Шаблоны позволяют определить развертывание и управлять приложениями с помощью декларативных шаблонов, а не сценариев. Шаблоны можно использовать для разных сред, в том числе для тестовой, промежуточной и рабочей. Например с помощью шаблонов, можно добавить репозиторий GitHub tooa кнопки, которая развертывает кода hello в наборе tooa hello репозитория служб Azure одним щелчком.

>**Когда toouse**: hello шаблоны диспетчера ресурсов используется при необходимости развертывания на основе шаблона для своего приложения, можно управлять программно с помощью API REST Azure CLI и Azure PowerShell.

>**Приступая к работе**: tooget работы с помощью шаблонов, в разделе [шаблоны разработки Azure Resource Manager](../../resource-group-authoring-templates.md).

## <a name="understanding-accounts-subscriptions-and-billing"></a>Сведения об учетных записях, подписках и выставлении счетов

Как разработчики нам нравятся toodive прямо в код hello и повторите tooget работы максимально быстро внесения запуска приложения. Безусловно, мы хотим tooencourage toostart так же просто, как можно работать в Azure. toohelp сделать его легко, Azure предлагает [бесплатной пробной версии](https://azure.microsoft.com/free/). Некоторые службы даже имеют «Частично на» функциональные возможности, например [службе приложений Azure](https://tryappservice.azure.com/), который не требует слишком даже создании учетной записи. Как fun как toodive в написании кода и развертывание tooAzure вашего приложения, это также важно tootake некоторые toounderstand времени, как работает Azure с точки зрения учетные записи пользователей, подписки и выставления счетов.

### <a name="what-is-an-azure-account"></a>Что такое учетная запись Azure?

может toocreate toobe или работы с подпиской Azure, необходимо иметь учетную запись Azure. Учетная запись Azure — это просто удостоверение в Azure AD или в каталоге, например компании или учебного заведения, которое является доверенным для Azure AD. Если вы не относитесь toosuch организации, всегда можно создать подписку с помощью учетной записи Майкрософт, который является доверенным для Azure AD. toolearn Дополнительные сведения об интеграции локальной Windows Server Active Directory с Azure AD, в разделе [интеграция локальных удостоверений с Azure Active Directory](../../active-directory/active-directory-aadconnect.md).

Между каждой подпиской Azure и экземпляром Azure AD установлено отношение доверия. Это означает, что она доверяет, tooauthenticate directory пользователей, служб и устройств. Несколько подписок могут доверять hello же каталоге, но подписка доверяет только одному каталогу. toolearn более, в разделе [подписки Azure связаны с Azure Active Directory](../../active-directory/active-directory-how-subscriptions-associated-directory.md).

Кроме удостоверения toodefining отдельную учетную запись Azure, также называемый *пользователей*, можно также определить *группы* в Azure AD. Создание групп пользователей — tooresources доступа хорошим способом toomanage в подписке с помощью управления доступом на основе ролей (RBAC). группы toocreate. в статье toolearn [создать группу в предварительной версии Azure Active Directory](../../active-directory/active-directory-groups-create-azure-portal.md). Создавать группы и управлять ими можно также [с помощью PowerShell](../../active-directory/active-directory-accessmanagement-groups-settings-v2-cmdlets.md).

### <a name="manage-your-subscriptions"></a>Управление подписками

Подписка — это логическая единица служб Azure, связанный tooan учетная запись Azure. Каждой связанной учетной записи назначена роль в подписке. Выставление счетов за службы Azure выполняется по подписке. Список предложений hello доступные подписки по типу, в разделе [сведения о Microsoft Azure предлагают](https://azure.microsoft.com/support/legal/offer-details/).

#### <a name="administrator-roles"></a>Роли администратора

В подписке Azure доступно несколько ролей учетной записи администратора, которые можно назначить в любое время.

-   **Учетная запись администратора**: Эта роль имеет полный контроль над hello подписки и учетная запись hello, который отвечает за выставление счетов.

-   **Администратор службы**: Эта роль имеет контроль над всем службам hello в подписке hello. По умолчанию это hello учетную запись как Здравствуйте, администратор учетной записи.

-   **Соадминистратор**: Эта роль имеет hello же получить доступ к имени hello администратора службы, за исключением того, что он не может изменить связь tooan подписки hello Azure directory hello.

toolearn Дополнительные сведения о ролях администратора см [как tooadd или изменение роли администратора Azure](../../billing/billing-add-change-azure-subscription-administrator.md#add-an-admin-for-a-subscription).

#### <a name="resource-groups"></a>Группы ресурсов

Подготовка новых служб Azure осуществляется в заданной подписке. Отдельных служб Azure, которые также называются ресурсов, создаются в контексте hello группы ресурсов. Группы ресурсов упрощают проще toodeploy и управлять ресурсами приложения. Группы ресурсов должно содержать все ресурсы hello для приложения, которое вы хотите toowork с как единое целое. Ресурсы можно перемещать между группами ресурсов и даже toodifferent подписки. toolearn о перемещении ресурсов, в разделе [переместить группу ресурсов toonew ресурсов или подписку](../../resource-group-move-resources.md).

Hello обозревателя ресурсов Azure — это мощный инструмент для визуализации hello ресурсы, которые вы уже создали в вашей подписке. toolearn более, в разделе [tooview использование обозревателя ресурсов Azure и изменять ресурсы](../../resource-manager-resource-explorer.md).

#### <a name="grant-access-tooresources"></a>Предоставление доступа к tooresources

При предоставлении доступа к ресурсам tooAzure рекомендуется всегда предоставляют пользователям с hello наименьших прав доступа с обязательным tooperform данной задачи.

-   **Управление доступом на основе ролей (RBAC)**: В Azure, вы можете предоставить доступ учетным записям toouser (участников) в указанной области: подписки, группы ресурсов или отдельных ресурсов. RBAC позволяет развернуть набор ресурсов в группу ресурсов и предоставить разрешения tooa конкретного пользователя или группы. Он также позволяют ограничивать доступ к ресурсам tooonly hello, принадлежащих toohello целевой группы ресурсов. Вы можете также предоставлять доступ tooa одного ресурса, например виртуальной машины или виртуальной сети. доступ toogrant, назначьте роль toohello пользователя, группы или субъекта-службы. Существует множество предопределенных ролей. Кроме того, можно определить собственные настраиваемые роли.

    >**Когда toouse**: при необходимости подробного управления доступом для пользователей и групп.

    >**Приступая к работе**: toolearn более, в разделе [Приступая к управлению доступом в hello портал Azure](../../active-directory/role-based-access-control-what-is.md).

-   **Объекты участника службы**: В дополнение к этому tooproviding доступ к toouser участников и групп, можно предоставить hello одного участника службы tooa доступа.

    > **Когда toouse**: Если вы будете программными средствами управления ресурсами Azure или предоставление доступа для приложений. Дополнительные сведения см. в разделе [Создание приложения Azure Active Directory и субъекта-службы с доступом к ресурсам с помощью портала](../../resource-group-create-service-principal-portal.md).

#### <a name="tags"></a>Теги

Диспетчер ресурсов Azure позволяет назначать настраиваемые теги tooindividual ресурсы. Теги, которые являются пары "ключ значение", можно при необходимости tooorganize ресурсы для выставления счетов или мониторинга. Теги предоставляют способ tootrack ресурсы в нескольких группах ресурсов. Можно назначить теги портала hello шаблона Azure Resource Manager hello или программно, с помощью API-интерфейса REST hello, hello Azure CLI или PowerShell. Можно назначить несколько тегов tooeach ресурсов. toolearn более, в разделе [использование теги tooorganize ресурсам Azure](../../resource-group-using-tags.md).

### <a name="billing"></a>Выставление счетов

Hello перемещение из локальных вычислительных служб, размещенных в toocloud, отслеживания и оценить использование службы и связанные затраты являются значительные проблемы. Это может tooestimate важные toobe какие новые ресурсы, стоимость toorun на ежемесячной основе. Необходимо также может tooproject toobe вида hello выставления счетов для данного месяца, основываясь на текущей тратит hello.

#### <a name="get-resource-usage-data"></a>Получение данных об использовании ресурсов

Azure предоставляет набор API REST выставления счетов, предоставьте доступ tooresource потребления и сведения о метаданных для подписки Azure. Эти предоставьте выставления счетов API-интерфейсы, hello toobetter возможности прогнозирования и управление затратами Azure. Можно отслеживать и анализировать затраты с шагом в один час, создавать оповещения о затратах и прогнозировать выставление счетов в будущем на основе текущих тенденций использования.

>**Приступая к работе**: toolearn Подробнее об использовании hello API, выставление счетов в разделе [Общие сведения об использовании выставления счетов Azure и API-интерфейсы RateCard](../../billing-usage-rate-card-overview.md).

#### <a name="predict-future-costs"></a>Прогнозирование будущих затрат

Несмотря на то, что он является довольно сложной tooestimate затраты заранее, Azure имеет [калькулятор](https://azure.microsoft.com/pricing/calculator/) , которые используются при оценке стоимости hello развернуты ресурсы. Также можно использовать колонки hello выставление счетов в портал hello и hello выставления счетов API-интерфейс REST tooestimate будущие затраты на основе текущего потребления.

>**Приступая к работе**. См. раздел [Использование API выставления счетов Azure для программного получения ценных сведений об использовании Azure](../../billing-usage-rate-card-overview.md).

#### <a name="set-up-billing-alerts"></a>Настройка платежных оповещений

После развертывания приложения или решения в Azure, можно создать оповещения, отправлять по электронной почте, когда подход hello, заданные в предупреждении hello Лимиты оплаты.

>**Приступая к работе**: toolearn более, в разделе [настроить оповещения для ваших подписок Microsoft Azure выставления счетов](../../billing-set-up-alerts.md).
