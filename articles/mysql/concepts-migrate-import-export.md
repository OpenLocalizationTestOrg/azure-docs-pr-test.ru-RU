---
title: "aaaImport и экспорта в базе данных Azure для MySQL | Документы Microsoft"
description: "В этой статье описываются распространенные способы tooimport и экспорт баз данных в базе данных Azure для MySQL, с помощью средств, таких как MySQL Workbench."
services: mysql
author: v-chenyh
ms.author: v-chenyh
manager: jhubbard
editor: jasonwhowell
ms.service: mysql-database
ms.topic: article
ms.date: 06/13/2017
ms.openlocfilehash: e2c036773d975df2eea2a59d166ea10567218a41
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="migrate-your-mysql-database-by-using-import-and-export"></a>Перенос базы данных MySQL с помощью импорта и экспорта
В этой статье объясняется две общие подходы tooimporting и экспорт данных tooan базы данных Azure для сервера MySQL с помощью MySQL Workbench. 

## <a name="before-you-begin"></a>Перед началом работы
toostep через этот как tooguide необходимо:
- База данных Azure для сервера MySQL. Дополнительные сведения см. в статье [Создание базы данных Azure для сервера MySQL с помощью портала Azure](quickstart-create-mysql-server-database-using-azure-portal.md).
- MySQL Workbench [загрузки](https://dev.mysql.com/downloads/workbench/), или другое средство tooimport MySQL и экспорта.

## <a name="use-common-tools"></a>Использование распространенных инструментов
Использование общих средств, таких как MySQL Workbench, Toad или Navicat tooremotely подключения и импорта или экспорта данных в базе данных Azure для MySQL. 

Используйте такие средства на клиентском компьютере с Интернет tooAzure tooconnect подключения базы данных для MySQL. Для обеспечения безопасности используйте подключение с SSL-шифрованием, как описано [этой статье](concepts-ssl-connection-security.md).

Не требуется toomove Импорт и расположение специальные облака tooany файлы экспорта, при миграции tooAzure базы данных для MySQL. 

## <a name="create-a-database-on-hello-azure-database-for-mysql-server"></a>Создание базы данных на hello базы данных Azure для сервера MySQL
Создайте пустую базу данных на hello базы данных Azure для MySQL server, где требуется сохранить данные hello toomigrate. С помощью средства, например MySQL Workbench, Toad или Navicat hello toocreate базы данных. Hello базы данных могут иметь одинаковые имена, как hello базы данных, содержащую hello дамп данных, или можно создать базу данных с другим именем hello.

tooget подключен, найдите сведения о соединении hello на hello **свойства** области в базе данных Azure для MySQL.

![Найти сведения о соединении hello в hello портал Azure](./media/concepts-migrate-import-export/1_server-properties-name-login.png)

Добавьте hello подключения сведения tooMySQL рабочей среды.

![Строка подключения MySQL Workbench](./media/concepts-migrate-import-export/2_setup-new-connection.png)

## <a name="determine-when-toouse-import-and-export-techniques-instead-of-a-dump-and-restore"></a>Определить, когда toouse импорта и экспорта методы вместо дампа и восстановления
Используйте средства tooimport MySQL и экспорта баз данных в базу данных MySQL в Azure в hello следующие сценарии. В других сценариях можно получить преимущество от использования hello [дампа и восстановление](concepts-migrate-dump-restore.md) вместо этого подхода. 

- При необходимости tooselectively выбрать несколько таблиц tooimport из существующей базы данных MySQL в базу данных MySQL в Azure, наиболее toouse hello импорта и экспорта прием.  Таким образом, можно пропустить все ненужные таблицы из hello миграции toosave время и ресурсы. Например, использовать hello `--include-tables` или `--exclude-tables` переключиться с [mysqlpump](https://dev.mysql.com/doc/refman/5.7/en/mysqlpump.html#option_mysqlpump_include-tables) и hello `--tables` переключиться с [mysqldump](https://dev.mysql.com/doc/refman/5.7/en/mysqldump.html#option_mysqldump_tables).
- При перемещении hello объектов базы данных, отличных от таблиц, создайте их явно. Включить ограничения (первичный ключ, внешний ключ, индексов), представления, функции, процедуры, триггеры и любые другие объекты баз данных, которые должны toomigrate.
- При перемещении данных из внешних источников данных, отличных от базы данных MySQL, создайте неструктурированные файлы и импортируйте их с помощью команды [mysqlimport](https://dev.mysql.com/doc/refman/5.7/en/mysqlimport.html).

Убедитесь, убедиться, что все таблицы в базе данных hello использует подсистема хранилища InnoDB hello, когда вы загружаете данные в базу данных Azure для MySQL. База данных Azure для MySQL поддерживает только hello подсистемой хранилища InnoDB, поэтому он не поддерживает модули альтернативные хранилища. Если таблицы требуется альтернативное хранилище ядер, быть tooconvert убедиться, что их формат engine InnoDB toouse hello перед tooAzure hello миграции базы данных MySQL. 

Например при наличии WordPress или веб-приложение, которое использует механизм MyISAM hello, сначала преобразуйте hello таблицы, перенос данных hello в InnoDB таблицы. Затем восстановите tooAzure базы данных для MySQL. Используйте предложение hello `ENGINE=INNODB` tooset hello механизм для создания таблицы и последующей передачи данных hello в таблицу совместимые hello перед миграцией hello. 

   ```sql
   INSERT INTO innodb_table SELECT * FROM myisam_table ORDER BY primary_key_columns
   ```

## <a name="performance-recommendations-for-import-and-export"></a>Рекомендации по повышению производительности импорта и экспорта
-   Создайте кластеризованные индексы и первичные ключи перед загрузкой данных. Загружайте данные в порядке первичных ключей. 
-   Отложите создание вторичных индексов до завершения загрузки данных. Создайте все вторичные индексы после загрузки. 
-   Отключите ограничения внешнего ключа перед загрузкой. Отключение проверки внешнего ключа обеспечивает значительный прирост производительности. Включить ограничения hello и проверка данных hello после hello нагрузки tooensure ссылочной целостности.
-   Загружайте данные в параллельном режиме. Избегайте слишком много параллелизма, которые бы привести к toohit ограничение ресурсов и отслеживать ресурсы с помощью метрики hello в hello портал Azure. 
-   Используйте секционированные таблицы, когда это необходимо.

## <a name="import-and-export-by-using-mysql-workbench"></a>Импорт и экспорт с помощью MySQL Workbench
Существует два способа tooexport и импорта данных в среде MySQL. каждый из которых предназначен для различных целей. 

### <a name="table-data-export-and-import-wizards-from-hello-object-browsers-context-menu"></a>Данные таблицы экспорта и импорта мастеров из контекстного меню обозревателя объектов hello
![Мастеры MySQL Workbench контекстного меню обозревателя объектов hello](./media/concepts-migrate-import-export/p1.png)

Hello мастеров для таблицы данных поддерживают импорта и экспорта с помощью файлов CSV и JSON. В них предусмотрено несколько параметров конфигурации, таких как разделители, выбор столбцов и кодировки. Операции каждого мастера можно выполнять на локальных или удаленно подключенных серверах MySQL. действия импорта Hello включает таблицу, столбец и сопоставление типов. 

Эти мастера доступны из контекстного меню обозревателя объектов hello щелкните правой кнопкой мыши таблицу. затем выбрав **Table Data Export Wizard** (Мастер экспорта данных таблиц) или **Table Data Import Wizard** (Мастер импорта данных таблиц). 

#### <a name="table-data-export-wizard"></a>Мастер экспорта данных таблиц
Hello следующем примере выполняется экспорт hello таблицы tooa CSV-файла: 
1. Щелкните правой кнопкой мыши таблицу hello toobe базы данных hello экспортированы. 
2. Выберите **Table Data Export Wizard** (Мастер экспорта данных таблиц). Выберите экспортированный toobe столбцы hello смещение строки (если таковые имеются) и число (если таковые имеются). 
3. На hello **выбрать данные для экспорта** щелкните **Далее**. Выберите путь к файлу hello, CSV или JSON тип файла. Также выберите разделитель строк hello, метод включающего строки и разделителя полей. 
4. На hello **выберите выходное расположение файла** щелкните **Далее**. 
5. На hello **экспортировать данные** щелкните **Далее**.

#### <a name="table-data-import-wizard"></a>Мастер импорта данных таблиц
Hello следующем примере выполняется импорт hello таблицы из CSV-файла:
1. Щелкните правой кнопкой мыши таблицу hello toobe базы данных hello импортированы. 
2. Обзор tooand выберите hello toobe файл CSV импортирован и нажмите кнопку **Далее**. 
3. Выберите hello целевой таблицы (новой или существующей) и установите или снимите hello **после усечения таблицы перед импортом** флажок. Щелкните **Далее**.
4. Выбор кодировки и hello toobe столбцы импортированы и нажмите кнопку **Далее**. 
5. На hello **импорта данных** щелкните **Далее**. Hello мастер импортирует данные hello соответствующим образом.

### <a name="sql-data-export-and-import-wizards-from-hello-navigator-pane"></a>Данные SQL Экспорт и импорт мастеров в области навигатора hello
Используйте мастер tooexport или импортируйте SQL из рабочей среды MySQL или создано из команды mysqldump hello. Доступ к мастерам из hello **Навигатор** области или выбрав **сервера** hello в главном меню. Затем выберите **Экспорт данных** или **Импорт данных**. 

#### <a name="data-export"></a>Экспорт данных
![Экспорт данных MySQL Workbench с помощью области навигатора hello](./media/concepts-migrate-import-export/p2.png)

Можно использовать hello **экспорта данных** вкладке tooexport данных MySQL. 
1. Выберите каждой схемы, что вы хотите tooexport, при необходимости выберите конкретную схему объектов и таблиц из каждой схемы и формировать hello экспорта. Параметры конфигурации включают папку проекта tooa экспорта или автономный файл SQL, дамп хранимых процедур и событий или пропустить данные таблицы. 
 
   Кроме того, использовать **экспортировать результирующий набор** tooexport определенный результат набора в hello SQL редактор tooanother формате, например CSV, JSON, HTML и XML. 
3. Выбор объектов tooexport hello базы данных и настройка параметров, связанных с hello.
4. Нажмите кнопку **обновление** tooload hello текущих объектов.
5. При необходимости откройте hello **Дополнительные параметры** вкладке операции экспорта toorefine hello. Например, можно добавить блокировки таблиц, использовать инструкцию replace вместо insert и заключить идентификаторы в кавычки в виде обратного апострофа.
6. Нажмите кнопку **начать экспорт** toobegin hello в процессе экспорта.


#### <a name="data-import"></a>Импорт данных
![Импорт данных в MySQL Workbench с помощью Management Navigator](./media/concepts-migrate-import-export/p3.png)

Можно использовать hello **импорта данных** вкладке tooimport или восстановить экспортированные данные из операции экспорта данных hello или команда mysqldump hello. 
1. Выберите папку проекта hello или автономный файл SQL, выберите tooimport схемы hello в или выберите **New** toodefine новую схему. 
2. Нажмите кнопку **запуск импорта** процесс импорта toobegin hello.

## <a name="next-steps"></a>Дальнейшие действия
Дополнительные сведения о другом способе переноса см. в статье [Перенос базы данных MySQL в базу данных Azure для MySQL с помощью дампа и восстановления](concepts-migrate-dump-restore.md). 
