---
title: "поток hello aaaUnderstanding OAuth2 неявных разрешений в Azure AD | Документы Microsoft"
description: "Дополнительные сведения о реализации Azure Active Directory потока неявного предоставления hello OAuth2, и подходит ли она для вашего приложения."
services: active-directory
documentationcenter: dev-center-name
author: jmprieur
manager: mbaldwin
editor: 
ms.assetid: 90e42ff9-43b0-4b4f-a222-51df847b2a8d
ms.service: active-directory
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: identity
ms.date: 11/15/2016
ms.author: jmprieur
ms.custom: aaddev
ms.openlocfilehash: 3402e6619709e1a5b1e790ffd79dc62139552d9d
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="understanding-hello-oauth2-implicit-grant-flow-in-azure-active-directory-ad"></a><span data-ttu-id="d282c-103">Основные сведения о hello OAuth2 неявное предоставление потока в Azure Active Directory (AD)</span><span class="sxs-lookup"><span data-stu-id="d282c-103">Understanding hello OAuth2 implicit grant flow in Azure Active Directory (AD)</span></span>
<span data-ttu-id="d282c-104">для предоставления hello с hello длинного списка проблемы безопасности в спецификации OAuth2 hello, важнее неявного предоставления Hello OAuth2.</span><span class="sxs-lookup"><span data-stu-id="d282c-104">hello OAuth2 implicit grant is notorious for being hello grant with hello longest list of security concerns in hello OAuth2 specification.</span></span> <span data-ttu-id="d282c-105">И еще, этот подход hello реализованные ADAL JS и hello, рекомендуется при написании приложений SPA.</span><span class="sxs-lookup"><span data-stu-id="d282c-105">And yet, that is hello approach implemented by ADAL JS and hello one we recommend when writing SPA applications.</span></span> <span data-ttu-id="d282c-106">Что это дает?</span><span class="sxs-lookup"><span data-stu-id="d282c-106">What gives?</span></span> <span data-ttu-id="d282c-107">Это делается путем компромиссы: и оказывается, неявного предоставления hello является лучшим подходом hello можно применять для приложений, использующих веб-API через JavaScript в браузере.</span><span class="sxs-lookup"><span data-stu-id="d282c-107">It’s all a matter of tradeoffs: and as it turns out, hello implicit grant is hello best approach you can pursue for applications that consume a Web API via JavaScript from a browser.</span></span>

## <a name="what-is-hello-oauth2-implicit-grant"></a><span data-ttu-id="d282c-108">Что такое неявного предоставления OAuth2 hello</span><span class="sxs-lookup"><span data-stu-id="d282c-108">What is hello OAuth2 implicit grant?</span></span>
<span data-ttu-id="d282c-109">Hello quintessential [предоставления кода авторизации OAuth2](https://tools.ietf.org/html/rfc6749#section-1.3.1) — hello предоставления авторизации, который использует две отдельные конечные точки.</span><span class="sxs-lookup"><span data-stu-id="d282c-109">hello quintessential [OAuth2 authorization code grant](https://tools.ietf.org/html/rfc6749#section-1.3.1) is hello authorization grant which uses two separate endpoints.</span></span> <span data-ttu-id="d282c-110">Конечная точка авторизации Hello используется на этапе взаимодействие пользователя hello, что приводит к код авторизации.</span><span class="sxs-lookup"><span data-stu-id="d282c-110">hello authorization endpoint is used for hello user interaction phase, which results in an authorization code.</span></span> <span data-ttu-id="d282c-111">Конечная точка маркера Hello затем используется клиентом hello для обмена кода hello для токена доступа и часто токен обновления.</span><span class="sxs-lookup"><span data-stu-id="d282c-111">hello token endpoint is then used by hello client for exchanging hello code for an access token, and often a refresh token as well.</span></span> <span data-ttu-id="d282c-112">Веб-приложения, свои собственные учетные данные toohello маркера конечной точки приложения, toopresent требуется, чтобы hello авторизации сервер может проверить подлинность клиента hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-112">Web applications are required toopresent their own application credentials toohello token endpoint, so that hello authorization server can authenticate hello client.</span></span>

<span data-ttu-id="d282c-113">Hello [неявного предоставления OAuth2](https://tools.ietf.org/html/rfc6749#section-1.3.2) вариант предоставляет другие авторизации.</span><span class="sxs-lookup"><span data-stu-id="d282c-113">hello [OAuth2 implicit grant](https://tools.ietf.org/html/rfc6749#section-1.3.2) is a variant of other authorization grants.</span></span> <span data-ttu-id="d282c-114">Он позволяет tooobtain клиента маркер доступа (и id_token при использовании [OpenId Connect](http://openid.net/specs/openid-connect-core-1_0.html)) непосредственно из конечной точки авторизации hello без обращения конечная точка маркера hello ни проверки подлинности клиента hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-114">It allows a client tooobtain an access token (and id_token, when using [OpenId Connect](http://openid.net/specs/openid-connect-core-1_0.html)) directly from hello authorization endpoint, without contacting hello token endpoint nor authenticating hello client.</span></span> <span data-ttu-id="d282c-115">Этот вариант был специально разработан для JavaScript на основе приложений, работающих в веб-браузере: hello исходной спецификацией OAuth2, возвращаются токены фрагмент URI.</span><span class="sxs-lookup"><span data-stu-id="d282c-115">This variant was specifically designed for JavaScript based applications running in a Web browser: in hello original OAuth2 specification, tokens are returned in a URI fragment.</span></span> <span data-ttu-id="d282c-116">Делает маркера bits hello доступных toohello код JavaScript в клиенте hello, но он гарантирует, что они не будут включены в перенаправления на сторону сервера hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-116">That makes hello token bits available toohello JavaScript code in hello client, but it guarantees they won’t be included in redirects toward hello server.</span></span> <span data-ttu-id="d282c-117">Возвращение токены через браузер перенаправляет непосредственно из конечной точки авторизации hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-117">Returning tokens via browser redirects directly from hello authorization endpoint.</span></span> <span data-ttu-id="d282c-118">Он также имеет преимущество hello устраняя все требования для кросс-origin вызовы, которые необходимы, если конечная точка маркера hello необходимые toocontact hello приложения JavaScript.</span><span class="sxs-lookup"><span data-stu-id="d282c-118">It also has hello advantage of eliminating any requirements for cross origin calls, which are necessary if hello JavaScript application is required toocontact hello token endpoint.</span></span>

<span data-ttu-id="d282c-119">Важной характеристикой неявного предоставления hello OAuth2 — hello факт, что такие потоки никогда не возвращает обновления токенов toohello клиента.</span><span class="sxs-lookup"><span data-stu-id="d282c-119">An important characteristic of hello OAuth2 implicit grant is hello fact that such flows never return refresh tokens toohello client.</span></span> <span data-ttu-id="d282c-120">Как будет показано в следующем разделе hello, не является обязательным, действительно и фактически может представлять угрозу безопасности.</span><span class="sxs-lookup"><span data-stu-id="d282c-120">As we will see in hello next section, that isn’t really necessary and would in fact be a security issue.</span></span>

## <a name="suitable-scenarios-for-hello-oauth2-implicit-grant"></a><span data-ttu-id="d282c-121">Предоставление подходящих сценариев для hello неявное OAuth2</span><span class="sxs-lookup"><span data-stu-id="d282c-121">Suitable scenarios for hello OAuth2 implicit grant</span></span>
<span data-ttu-id="d282c-122">Как объявляет самой спецификации OAuth2 hello, hello неявного предоставления был продумать tooenable агент пользователя приложения —, является toosay, JavaScript-приложений, выполняющихся в браузере.</span><span class="sxs-lookup"><span data-stu-id="d282c-122">As hello OAuth2 specification itself declares, hello implicit grant has been devised tooenable user-agent applications – that is toosay, JavaScript applications executing within a browser.</span></span> <span data-ttu-id="d282c-123">Определение характеристик такие приложения Hello является использование код JavaScript для доступа к ресурсам сервера (обычно веб-API) и обновление приложения hello UX соответствующим образом.</span><span class="sxs-lookup"><span data-stu-id="d282c-123">hello defining characteristic of such applications is that JavaScript code is used for accessing server resources (typically a Web API) and for updating hello application UX accordingly.</span></span> <span data-ttu-id="d282c-124">Представьте приложениями, например Gmail или Outlook Web Access: при выборе сообщение в папку «Входящие» панели визуализации изменяет toodisplay hello новое выделение, при hello остальной части страницы приветствия только приветственное сообщение остается без изменений.</span><span class="sxs-lookup"><span data-stu-id="d282c-124">Think of applications like Gmail or Outlook Web Access: when you select a message from your inbox, only hello message visualization panel changes toodisplay hello new selection, while hello rest of hello page remains unmodified.</span></span> <span data-ttu-id="d282c-125">Это в отличие от традиционных основе перенаправления веб-приложений, где каждый взаимодействие с пользователем приводит к обратной передачи целой страницы и подготовки в полностраничном hello новый ответ сервера.</span><span class="sxs-lookup"><span data-stu-id="d282c-125">This is in contrast with traditional redirect-based Web apps, where every user interaction results in a full page postback and a full page rendering of hello new server response.</span></span>

<span data-ttu-id="d282c-126">Приложения, использующие extreme tooits подход на основе JavaScript hello называются приложений на одной странице или SPAs: hello идея заключается в том, что эти приложения только обслуживать начальной страницы HTML и связанные JavaScript с определяются все последующие действия Веб-API-вызовов, выполняемых через JavaScript.</span><span class="sxs-lookup"><span data-stu-id="d282c-126">Applications that take hello JavaScript based approach tooits extreme are called Single Page Applications, or SPAs: hello idea is that those applications only serve an initial HTML page and associated JavaScript, with all subsequent interactions being driven by Web API calls performed via JavaScript.</span></span> <span data-ttu-id="d282c-127">Однако гибридное подходов, когда приложения hello по большей части управляемых обратной передачи, но выполняет случайные JS вызовов, не являются редкими — hello обсуждение использования неявного потока применимо для тех, кто также.</span><span class="sxs-lookup"><span data-stu-id="d282c-127">However, hybrid approaches, where hello application is mostly postback-driven but performs occasional JS calls, are not uncommon – hello discussion about implicit flow usage is relevant for those as well.</span></span>

<span data-ttu-id="d282c-128">Приложения на основе перенаправления традиционно защищают свои запросы с помощью файлов cookie, но этот метод не очень хорошо подходит для приложений JavaScript.</span><span class="sxs-lookup"><span data-stu-id="d282c-128">Redirect-based applications typically secure their requests via cookies, however, that approach does not work as well for JavaScript applications.</span></span> <span data-ttu-id="d282c-129">Файлы "cookie" работают только относительно домена hello, которую они были созданы, пока вызовов JavaScript может направить других доменов.</span><span class="sxs-lookup"><span data-stu-id="d282c-129">Cookies only work against hello domain they have been generated for, while JavaScript calls might be directed toward other domains.</span></span> <span data-ttu-id="d282c-130">На самом деле, часто оказываются hello случай: обработки приложений, вызов API API Graph Office API, Microsoft Azure — всех находящихся за пределами домена hello, из которой обслуживается приложение hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-130">In fact, that will frequently be hello case: think of applications invoking Microsoft Graph API, Office API, Azure API – all residing outside hello domain from where hello application is served.</span></span> <span data-ttu-id="d282c-131">Растущий тренд для приложения JavaScript является toohave не серверной части, проверяющей 100% по сторонних производителей веб-API tooimplement их бизнес-функции.</span><span class="sxs-lookup"><span data-stu-id="d282c-131">A growing trend for JavaScript applications is toohave no backend at all, relying 100% on 3rd party Web APIs tooimplement their business function.</span></span>

<span data-ttu-id="d282c-132">В настоящее время hello предпочтительный метод защиты tooa вызовы веб-API подход toouse hello OAuth2 носителя токена, где каждый вызов сопровождается маркер доступа OAuth2.</span><span class="sxs-lookup"><span data-stu-id="d282c-132">Currently, hello preferred method of protecting calls tooa Web API is toouse hello OAuth2 bearer token approach, where every call is accompanied by an OAuth2 access token.</span></span> <span data-ttu-id="d282c-133">Hello веб-API проверяет входящий токен доступа hello и, при обнаружении в его hello необходимые области, он предоставляет доступ toohello запрошенную операцию.</span><span class="sxs-lookup"><span data-stu-id="d282c-133">hello Web API examines hello incoming access token and, if it finds in it hello necessary scopes, it grants access toohello requested operation.</span></span> <span data-ttu-id="d282c-134">Hello неявного потока обеспечивает удобный механизм для приложений JavaScript tooobtain токены доступа для веб-API, предлагают многочисленные преимущества в учитывают toocookies:</span><span class="sxs-lookup"><span data-stu-id="d282c-134">hello implicit flow provides a convenient mechanism for JavaScript applications tooobtain access tokens for a Web API, offering numerous advantages in respect toocookies:</span></span>

* <span data-ttu-id="d282c-135">Токены можно надежно получить без каких-либо для кросс-вызовы источника — обязательной регистрации toowhich URI перенаправления hello, будут возвращать маркеры гарантирует, что токены не являются замещения</span><span class="sxs-lookup"><span data-stu-id="d282c-135">Tokens can be reliably obtained without any need for cross origin calls – mandatory registration of hello redirect URI toowhich tokens are return guarantees that tokens are not displaced</span></span>
* <span data-ttu-id="d282c-136">Приложения JavaScript могут получать любое необходимое количество маркеров доступа для обращения к любому количеству веб-API без ограничений по доменам.</span><span class="sxs-lookup"><span data-stu-id="d282c-136">JavaScript applications can obtain as many access tokens as they need, for as many Web APIs they target – with no restriction on domains</span></span>
* <span data-ttu-id="d282c-137">Функции HTML5, такие как сеанса или локальное хранилище предоставления полного доступа к кэширования токенов и управление временем существования, тогда как непрозрачный toohello приложения управления файлы cookie</span><span class="sxs-lookup"><span data-stu-id="d282c-137">HTML5 features like session or local storage grant full control over token caching and lifetime management, whereas cookies management is opaque toohello app</span></span>
* <span data-ttu-id="d282c-138">Маркеры доступа не подвержены tooCross сайт атак с подделкой (CSRF) запросов</span><span class="sxs-lookup"><span data-stu-id="d282c-138">Access tokens aren’t susceptible tooCross-site request forgery (CSRF) attacks</span></span>

<span data-ttu-id="d282c-139">поток неявного предоставления Hello не выдает токены обновления, главным образом из соображений безопасности.</span><span class="sxs-lookup"><span data-stu-id="d282c-139">hello implicit grant flow does not issue refresh tokens, mostly for security reasons.</span></span> <span data-ttu-id="d282c-140">В отличие от маркеров доступа, маркеры обновления не используются в узкой области. Они предоставляют больше возможностей, и поэтому их раскрытие причиняет больше вреда. В hello неявного потока токены доставляются в URL-АДРЕСЕ hello, поэтому hello риск перехвата выше, чем в предоставления кода авторизации hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-140">A refresh token isn’t as narrowly scoped as access tokens, granting far more power hence inflicting far more damage in case it is leaked out. In hello implicit flow, tokens are delivered in hello URL, hence hello risk of interception is higher than in hello authorization code grant.</span></span>

<span data-ttu-id="d282c-141">Тем не менее Обратите внимание, что приложение JavaScript другой механизм окон обновлении токены доступа без запроса учетных данных пользователя hello несколько раз.</span><span class="sxs-lookup"><span data-stu-id="d282c-141">However, note that a JavaScript application has another mechanism at its disposal for renewing access tokens without repeatedly prompting hello user for credentials.</span></span> <span data-ttu-id="d282c-142">Hello приложение может с помощью скрытого iframe tooperform новый токен запросов к hello конечную точку авторизации Azure AD: при условии, что браузере hello по-прежнему имеется активный сеанс (чтение: имеет файл cookie сеанса) относительно домена hello Azure AD hello запрос проверки подлинности может возникнуть успешно без необходимости вмешательства пользователя.</span><span class="sxs-lookup"><span data-stu-id="d282c-142">hello application can use a hidden iframe tooperform new token requests against hello authorization endpoint of Azure AD: as long as hello browser still has an active session (read: has a session cookie) against hello Azure AD domain, hello authentication request can successfully occur without any need for user interaction.</span></span>

<span data-ttu-id="d282c-143">Эта модель позволяет hello JavaScript приложения hello обладателям tooindependently обновите маркеры доступа и даже получить новые для нового API (при условии, что hello пользователя ранее согласие для них.</span><span class="sxs-lookup"><span data-stu-id="d282c-143">This model grants hello JavaScript application hello ability tooindependently renew access tokens and even acquire new ones for a new API (provided that hello user previously consented for them.</span></span> <span data-ttu-id="d282c-144">Это позволяет избежать hello добавлены нагрузку приобретение, обслуживание и защита артефакт высокое значение, такие как токен обновления.</span><span class="sxs-lookup"><span data-stu-id="d282c-144">This avoids hello added burden of acquiring, maintaining, and protecting a high value artifact such as a refresh token.</span></span> <span data-ttu-id="d282c-145">Hello артефакт, который позволяет hello автоматического обновления, файл cookie сеанса hello Azure AD, осуществляется вне приложения hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-145">hello artifact which makes hello silent renewal possible, hello Azure AD session cookie, is managed outside of hello application.</span></span> <span data-ttu-id="d282c-146">Еще одно преимущество этого подхода — пользователь может выйти из Azure AD, с помощью любого приложения hello, вход в Azure AD, любого hello вкладки браузера.</span><span class="sxs-lookup"><span data-stu-id="d282c-146">Another advantage of this approach is a user can sign out from Azure AD, using any of hello applications signed into Azure AD, running in any of hello browser tabs.</span></span> <span data-ttu-id="d282c-147">Это приведет к hello удаление файла cookie сеанса hello Azure AD и hello приложения JavaScript автоматически теряет возможность hello toorenew маркерах hello выход пользователя.</span><span class="sxs-lookup"><span data-stu-id="d282c-147">This results in hello deletion of hello Azure AD session cookie, and hello JavaScript application will automatically lose hello ability toorenew tokens for hello signed out user.</span></span>

## <a name="is-hello-implicit-grant-suitable-for-my-app"></a><span data-ttu-id="d282c-148">Hello неявного предоставления подходит для приложения?</span><span class="sxs-lookup"><span data-stu-id="d282c-148">Is hello implicit grant suitable for my app?</span></span>
<span data-ttu-id="d282c-149">неявного предоставления Hello содержит несколько рисков, чем другие предоставляет и hello областей требуется tooare внимания toopay подробно описаны в документации.</span><span class="sxs-lookup"><span data-stu-id="d282c-149">hello implicit grant presents more risks than other grants, and hello areas you need toopay attention tooare well documented.</span></span> <span data-ttu-id="d282c-150">Например [tooImpersonate неправильное использование из токена доступа владельца ресурса в неявного потока] [ OAuth2-Spec-Implicit-Misuse] и [модели угроз OAuth 2.0 и вопросы безопасности] [ OAuth2-Threat-Model-And-Security-Implications]).</span><span class="sxs-lookup"><span data-stu-id="d282c-150">For example, [Misuse of Access Token tooImpersonate Resource Owner in Implicit Flow][OAuth2-Spec-Implicit-Misuse] and [OAuth 2.0 Threat Model and Security Considerations][OAuth2-Threat-Model-And-Security-Implications]).</span></span> <span data-ttu-id="d282c-151">Однако hello выше профилю риска является главным образом из-за toohello факт, что он должен tooenable приложениях, выполняемых активном коде, обслуживаемых браузера tooa удаленный ресурс.</span><span class="sxs-lookup"><span data-stu-id="d282c-151">However, hello higher risk profile is largely due toohello fact that it is meant tooenable applications that execute active code, served by a remote resource tooa browser.</span></span> <span data-ttu-id="d282c-152">При планировании архитектуры SPA имеют нет внутренних компонентов или хотите tooinvoke веб-API через JavaScript, рекомендуется использовать для получения маркера hello неявного потока.</span><span class="sxs-lookup"><span data-stu-id="d282c-152">If you are planning an SPA architecture, have no backend components or intend tooinvoke a Web API via JavaScript, use of hello implicit flow for token acquisition is recommended.</span></span>

<span data-ttu-id="d282c-153">Если приложение является собственный клиент, hello неявного потока не отлично подходит.</span><span class="sxs-lookup"><span data-stu-id="d282c-153">If your application is a native client, hello implicit flow isn’t a great fit.</span></span> <span data-ttu-id="d282c-154">отсутствие Hello файла cookie сеанса hello Azure AD в контексте hello собственного клиента deprives приложения hello средства поддержки долговременной сеанса.</span><span class="sxs-lookup"><span data-stu-id="d282c-154">hello absence of hello Azure AD session cookie in hello context of a native client deprives your application from hello means of maintaining a long lived session.</span></span> <span data-ttu-id="d282c-155">Это означает приложения несколько раз запросит hello пользователя при получении маркера доступа для новых ресурсов.</span><span class="sxs-lookup"><span data-stu-id="d282c-155">Which means your application will repeatedly prompt hello user when obtaining access tokens for new resources.</span></span>

<span data-ttu-id="d282c-156">При разработке веб-приложения, который включает в себя серверной части и использует интерфейс API из внутреннего кода hello неявного потока также не является удачным выбором.</span><span class="sxs-lookup"><span data-stu-id="d282c-156">If you are developing a Web application which includes a backend, and consuming an API from its backend code, hello implicit flow is also not a good fit.</span></span> <span data-ttu-id="d282c-157">Другие варианты предоставления маркера дают намного больше возможностей.</span><span class="sxs-lookup"><span data-stu-id="d282c-157">Other grants give you far more power.</span></span> <span data-ttu-id="d282c-158">Например предоставления учетных данных клиента hello OAuth2 предоставляет hello возможность tooobtain токены, отражающие hello разрешений, назначенных toohello самого приложения, в противоположность toouser делегирования.</span><span class="sxs-lookup"><span data-stu-id="d282c-158">For example, hello OAuth2 client credentials grant provides hello ability tooobtain tokens that reflect hello permissions assigned toohello application itself, as opposed toouser delegations.</span></span> <span data-ttu-id="d282c-159">Это означает, что клиент hello имеет tooresources программный доступ toomaintain возможность hello, даже в том случае, если пользователь не активно участвует в сеансе и т. д.</span><span class="sxs-lookup"><span data-stu-id="d282c-159">This means hello client has hello ability toomaintain programmatic access tooresources even when a user is not actively engaged in a session, and so on.</span></span> <span data-ttu-id="d282c-160">Кроме того, такое предоставление обеспечивает более высокий уровень безопасности.</span><span class="sxs-lookup"><span data-stu-id="d282c-160">Not only that, but such grants give higher security guarantees.</span></span> <span data-ttu-id="d282c-161">Например токены доступа никогда не проходить через браузер пользователя hello, они не существует риск не будут сохранены в журнал браузера hello и т. д.</span><span class="sxs-lookup"><span data-stu-id="d282c-161">For instance, access tokens never transit through hello user browser, they don’t risk being saved in hello browser history, and so on.</span></span> <span data-ttu-id="d282c-162">Hello клиентское приложение может выполнять строгой проверки подлинности при запросе маркера.</span><span class="sxs-lookup"><span data-stu-id="d282c-162">hello client application can also perform strong authentication when requesting a token.</span></span>

## <a name="next-steps"></a><span data-ttu-id="d282c-163">Дальнейшие действия</span><span class="sxs-lookup"><span data-stu-id="d282c-163">Next steps</span></span>
* <span data-ttu-id="d282c-164">Полный список ресурсов для разработчиков, включая справочные сведения для протоколов hello и авторизации OAuth2 предоставлять поддержку потоков Azure AD, см. в разделе toohello [руководство разработчика Azure AD][AAD-Developers-Guide]</span><span class="sxs-lookup"><span data-stu-id="d282c-164">For a complete list of developer resources, including reference information for hello protocols and OAuth2 authorization grant flows support by Azure AD, refer toohello [Azure AD Developer's Guide][AAD-Developers-Guide]</span></span>
* <span data-ttu-id="d282c-165">В разделе [как toointegrate приложения с Azure AD] [ ACOM-How-To-Integrate] для дополнительных глубины на процесс интеграции приложения hello.</span><span class="sxs-lookup"><span data-stu-id="d282c-165">See [How toointegrate an application with Azure AD][ACOM-How-To-Integrate] for additional depth on hello application integration process.</span></span>

<!--Image references-->

<!--Reference style links in use-->
[AAD-Developers-Guide]: active-directory-developers-guide.md
[ACOM-How-And-Why-Apps-Added-To-AAD]: active-directory-how-applications-are-added.md
[ACOM-How-To-Integrate]: active-directory-how-to-integrate.md
[OAuth2-Spec-Implicit-Misuse]: https://tools.ietf.org/html/rfc6749#section-10.16
[OAuth2-Threat-Model-And-Security-Implications]: https://tools.ietf.org/html/rfc6819
