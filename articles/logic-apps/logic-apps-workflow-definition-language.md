---
title: "Схема языка определения aaaWorkflow - приложения логики Azure | Документы Microsoft"
description: "Определите процессы, основанные на hello схема определения рабочего процесса для приложения логики Azure"
services: logic-apps
author: jeffhollan
manager: anneta
editor: 
documentationcenter: 
ms.assetid: 26c94308-aa0d-4730-97b6-de848bffff91
ms.service: logic-apps
ms.workload: integration
ms.tgt_pltfrm: na
ms.devlang: multiple
ms.topic: article
ms.date: 03/21/2017
ms.author: LADocs; jehollan
ms.openlocfilehash: f12440e9ca269a9236132deeb888dbde7fb734d8
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="workflow-definition-language-schema-for-azure-logic-apps"></a>Схема языка определения рабочих процессов в Azure Logic Apps

Определение рабочего процесса содержит фактической логики hello, которое выполняется как часть логики приложения. Это определение включает один или несколько триггеров для запуска приложения логики hello и одно или несколько действий для hello логику приложения tootake.  
  
## <a name="basic-workflow-definition-structure"></a>Базовая структура определения рабочего процесса

Вот hello базовая структура определения рабочего процесса.  
  
```json
{
    "$schema": "<schema-of the-definition>",
    "contentVersion": "<version-number-of-definition>",
    "parameters": { <parameter-definitions-of-definition> },
    "triggers": [ { <definition-of-flow-triggers> } ],
    "actions": [ { <definition-of-flow-actions> } ],
    "outputs": { <output-of-definition> }
}
```
  
> [!NOTE]
> Hello [API REST управления рабочего процесса](https://docs.microsoft.com/rest/api/logic/workflows) документации содержит сведения о том, как toocreate и управление рабочими процессами логику приложения.
  
|Имя элемента|Обязательно|Описание|  
|------------------|--------------|-----------------|  
|$schema|Нет|Указывает расположение файла схемы hello JSON, описывающий hello версию языка определения hello hello. Это расположение требуется для ссылки на определение извне. Для этого документа — расположение hello: <p>`https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2015-08-01-preview/workflowdefinition.json#`|  
|contentVersion|Нет|Указывает версию определения hello. При развертывании с использованием hello определения рабочего процесса, можно использовать этот том, используется определение правой hello toomake значение.|  
|parameters|Нет|Указывает параметры tooinput данных в определение hello. Можно определить до 50 параметров.|  
|триггеры;|Нет|Указывает сведения о триггерах hello, которые запускают рабочий процесс hello. Можно определить до 10 триггеров.|  
|actions|Нет|Указывает действия, предпринимаемые hello потока выполнения. Можно определить до 250 действий.|  
|outputs|Нет|Задает сведения о hello развертывания ресурсов. Можно определить до 10 выводов.|  
  
## <a name="parameters"></a>Параметры

В этом разделе указывает все параметры hello, используемые в определении рабочего процесса hello во время развертывания. Все параметры должны объявляться в этом разделе, прежде чем они могут использоваться в других разделах определения hello.  
  
Hello ниже приведен пример hello структура определения параметра.  

```json
"parameters": {
    "<parameter-name>" : {
        "type" : "<type-of-parameter-value>",
        "defaultValue": <default-value-of-parameter>,
        "allowedValues": [ <array-of-allowed-values> ],
        "metadata" : { "key": { "name": "value"} }
    }
}
```

|Имя элемента|Обязательно|Description (Описание)|  
|------------------|--------------|-----------------|  
|type|Да|**Тип:** string. <p> **Объявление:** `"parameters": {"parameter1": {"type": "string"}` <p> **Спецификация:** `"parameters": {"parameter1": {"value": "myparamvalue1"}}` <p> **Тип:** securestring. <p> **Объявление:** `"parameters": {"parameter1": {"type": "securestring"}}` <p> **Спецификация:** `"parameters": {"parameter1": {"value": "myparamvalue1"}}` <p> **Тип:** int. <p> **Объявление:** `"parameters": {"parameter1": {"type": "int"}}` <p> **Спецификация:** `"parameters": {"parameter1": {"value" : 5}}` <p> **Тип:** bool. <p> **Объявление:** `"parameters": {"parameter1": {"type": "bool"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": true }}` <p> **Тип:** array. <p> **Объявление:** `"parameters": {"parameter1": {"type": "array"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": [ array-of-values ]}}` <p> **Тип:** object. <p> **Объявление:** `"parameters": {"parameter1": {"type": "object"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": { JSON-object } }}` <p> **Тип:** secureobject. <p> **Объявление:** `"parameters": {"parameter1": {"type": "object"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": { JSON-object } }}` <p> **Примечание:** hello `securestring` и `secureobject` типы не возвращаются в `GET` операций. Этот тип нужно использовать для всех паролей, ключей и секретов.|  
|defaultValue|Нет|Указывает hello значение по умолчанию для параметра hello, если значение не указано во время hello создания ресурса hello.|  
|allowedValues|Нет|Указывает массив допустимых значений для параметра hello.|  
|metadata|Нет|Указывает Дополнительные сведения о параметре hello, например понятное описание или данные времени разработки, используемые Visual Studio или других средств.|  
  
В этом примере показано, как можно использовать параметр в разделе текст hello действия:  
  
```json
"body" :
{
  "property1": "@parameters('parameter1')"
}
```

 Параметры также можно использовать в разделе outputs.  
  
## <a name="triggers-and-actions"></a>Триггеры и действия  

Триггеры и действия укажите hello вызовов, которые могут участвовать в выполнение рабочего процесса. Дополнительные сведения об этом разделе см. в статье [Действия и триггеры рабочих процессов в Azure Logic Apps](logic-apps-workflow-actions-triggers.md).
  
## <a name="outputs"></a>outputs  

В разделе outputs указываются сведения, которые могут быть возвращены из выполнения рабочего процесса. Например при наличии определенного состояния или значение, которое будет tootrack для каждого запуска, можно включить выполнение данные в hello выходные данные. Hello данных отображается в hello API REST управления для, где выполняется, а в hello управления пользовательского интерфейса, где выполняется в hello портал Azure. Также могут передаваться как PowerBI внешних систем tooother выходные данные для создания панелей мониторинга. Выходные данные будут *не* используется toorespond tooincoming запросы на hello API REST службы. toorespond tooan входящего запроса с помощью hello `response` тип действия, ниже приведен пример:
  
```json
"outputs": {  
  "key1": {  
    "value": "value1",  
    "type" : "<type-of-value>"  
  }  
} 
```

|Имя элемента|Обязательно|Описание|  
|------------------|--------------|-----------------|  
|key1|Да|Указывает идентификатор ключа hello hello выходных данных. Замените **key1** с именем, что необходимо выводить hello tooidentify toouse.|  
|value|Да|Указывает значение hello hello выходных данных.|  
|type|Да|Указывает тип hello для hello значение, которое было указано. Возможные типы значений: <ul><li>`string`</li><li>`securestring`</li><li>`int`</li><li>`bool`</li><li>`array`</li><li>`object`</li></ul>|
  
## <a name="expressions"></a>Выражения  

Значения JSON в определении hello может быть литералом или они могут быть выражения, оцениваемые при использовании определения hello. Например:  
  
```json
"name": "value"
```

 или  
  
```json
"name": "@parameters('password') "
```

> [!NOTE]
> Некоторые выражения получают свои значения из среды выполнения действия, которые могут не существовать в начале выполнения hello hello. Можно использовать **функции** toohelp получить некоторые из этих значений.  
  
Выражения могут встречаться в любом месте строкового значения JSON и всегда возвращают другое значение JSON. При значение JSON было определено toobe выражения, текст hello hello выражения извлекается путем удаления hello знака (@). Если требуется строковый литерал, начинающийся с @, эту строку необходимо экранировать с помощью "@@". Hello следующих примерах показано, как вычисляются выражения.  
  
|Значение JSON|Результат|  
|----------------|------------|  
|"parameters"|возвращаются символы Hello «параметры».|  
|"parameters[1]"|возвращаются Hello символы «parameters [1]».|  
|"@@"|Возвращается строка из 1 символа, содержащая символ @.|  
|"@"|Возвращается строка из 2 символов, содержащая символ @.|  
  
При *интерполяции строк* выражения также могут содержаться внутри строк, где они заключаются в структуру `@{ ... }`. Например: <p>`"name" : "First Name: @{parameters('firstName')} Last Name: @{parameters('lastName')}"`

Hello результат всегда представляет собой строку, что делает этот компонент как toohello `concat` функции. Предположим, что вы определили `myNumber` как `42`, а `myString` — как `sampleString`:  
  
|Значение JSON|Результат|  
|----------------|------------|  
|"@parameters('myString')"|Возвращает `sampleString` как строку.|  
|"@{parameters('myString')}"|Возвращает `sampleString` как строку.|  
|"@parameters('myNumber')"|Возвращает `42` как *номер*.|  
|"@{parameters('myNumber')}"|Возвращает `42` как *строку*.|  
|"Answer is: @{parameters('myNumber')}"|Здравствуйте, возвращает строку `Answer is: 42`.|  
|"@concat('Answer is: ', string(parameters('myNumber')))"|Возвращает строку hello`Answer is: 42`|  
|"Answer is: @@{parameters('myNumber')}"|Здравствуйте, возвращает строку `Answer is: @{parameters('myNumber')}`.|  
  
## <a name="operators"></a>Операторы  

Операторы — это hello символы, которые можно использовать в выражениях или функции. 
  
|operator|Описание|  
|--------------|-----------------|  
|.|оператор "точка" Hello позволяет tooreference свойства объекта|  
|?|оператор Hello вопросительный знак позволяет ссылаться на значение null свойства объекта без ошибку во время выполнения. Например, можно использовать это выражение toohandle null триггера выходные данные: <p>`@coalesce(trigger().outputs?.body?.property1, 'my default value')`|  
|'|одинарная кавычка Hello является hello единственным способом toowrap строковых литералов. Нельзя использовать двойные кавычки внутри выражения из-за этого маркера конфликтов с предложением hello JSON, которая служит оболочкой для hello всего выражения.|  
|[]|Hello квадратные скобки можно использовать tooget значения из массива с указанным индексом. Например, если у вас есть действие, которое передает `range(0,10)`в toohello `forEach` функции, можно использовать элементы tooget этой функции out массивов:  <p>`myArray[item()]`|  
  
## <a name="functions"></a>Функции  

Внутри выражений также можно вызывать функции. Hello следующей таблице приведены функции hello, которые могут использоваться в выражении.  
  
|Expression|Оценка|  
|----------------|----------------|  
|"@function('Hello')"|Вызовы hello функцию-член hello определение строки-литерала hello Hello как первый параметр hello.|  
|"@function('It's Cool!')"|Вызывает функцию-член hello определения hello hello строки-литерала «It's Cool!» как первый параметр hello|  
|"@function().prop1"|Здравствуйте, возвращает значение свойства prop1 hello hello `myfunction` член определения hello.|  
|"@function('Hello').prop1"|Вызывает функцию-член определения hello hello строки-литерала «Hello» hello как hello первого параметра и возвращает hello свойства prop1 hello объекта.|  
|"@function(parameters('Hello'))"|Вычисляет параметр Hello hello и передает значение toofunction hello|  
  
### <a name="referencing-functions"></a>Функции для ссылки  

Можно использовать эти функции tooreference выходные данные из других действий в приложении логику hello или значения, передаваемые в при создании приложения hello логики. Например, можно ссылаться на hello данные из одного шага toouse его в другой.  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|parameters|Возвращает значение параметра, который определен в определении hello. <p>`parameters('password')` <p> **Номер параметра:** 1. <p> **Имя:** параметр. <p> **Описание:** обязательно. Имя параметра hello, значения которых должны Hello.|  
|action|Включает выражение tooderive свое значение из другого имени JSON и пар «значение» или выходные данные hello hello текущей среды выполнения действия. Свойство Hello, представленное propertyPath в следующий пример hello является необязательным. Если propertyPath не указан, hello ссылка является объект toohello всей действия. Эта функция может использоваться только внутри условий do-until для действия. <p>`action().outputs.body.propertyPath`|  
|actions|Включает выражение tooderive свое значение из другого имени JSON и пар «значение» или выходные данные hello hello среды выполнения действия. Эти выражения явно объявляют, что одно действие зависит от другого. Свойство Hello, представленное propertyPath в следующий пример hello является необязательным. Если propertyPath не указан, hello ссылка является объект toohello всей действия. Либо этот элемент можно использовать или hello условия зависимости toospecify элемент, но не требуется для hello toouse оба же зависимый ресурс. <p>`actions('myAction').outputs.body.propertyPath` <p> **Номер параметра:** 1. <p> **Имя:** имя действия. <p> **Описание:** обязательно. Имя действия hello, значения которых должны Hello. <p> Доступные свойства для объекта действие hello:: <ul><li>`name`</li><li>`startTime`</li><li>`endTime`</li><li>`inputs`</li><li>`outputs`</li><li>`status`</li><li>`code`</li><li>`trackingId`</li><li>`clientTrackingId`</li></ul> <p>В разделе hello [API-интерфейса Rest](http://go.microsoft.com/fwlink/p/?LinkID=850646) подробные сведения об этих свойствах.|
|trigger|Включает выражение tooderive свое значение из другого имени JSON и пар «значение» или вывода hello hello среды выполнения триггера. Свойство Hello, представленное propertyPath в следующий пример hello является необязательным. Если propertyPath не указан, hello ссылка является объектом toohello триггер. <p>`trigger().outputs.body.propertyPath` <p>При использовании внутри триггера входов, hello функция возвращает выходные данные предыдущего выполнения hello hello. Однако при использовании внутри условие триггера hello `trigger` функция возвращает выходные данные hello hello текущего выполнения. <p> Перечислены доступные свойства hello объекта триггера. <ul><li>`name`</li><li>`scheduledTime`</li><li>`startTime`</li><li>`endTime`</li><li>`inputs`</li><li>`outputs`</li><li>`status`</li><li>`code`</li><li>`trackingId`</li><li>`clientTrackingId`</li></ul> <p>В разделе hello [API-интерфейса Rest](http://go.microsoft.com/fwlink/p/?LinkID=850644) подробные сведения об этих свойствах.|
|actionOutputs|Эта функция является сокращением для `actions('actionName').outputs`. <p> **Номер параметра:** 1. <p> **Имя:** имя действия. <p> **Описание:** обязательно. Имя действия hello, значения которых должны Hello.|  
|actionBody и body|Эти функции являются сокращением для `actions('actionName').outputs.body`. <p> **Номер параметра:** 1. <p> **Имя:** имя действия. <p> **Описание:** обязательно. Имя действия hello, значения которых должны Hello.|  
|triggerOutputs|Эта функция является сокращением для `trigger().outputs`.|  
|triggerBody|Эта функция является сокращением для `trigger().outputs.body`.|  
|item|При использовании внутри повторяющихся действий, эта функция возвращает hello элемента в массиве hello для данной итерации действие hello. Например, если имеется действие, которое выполняет для каждого элемента массив сообщений, можно использовать следующий синтаксис: <p>`"input1" : "@item().subject"`| 
  
### <a name="collection-functions"></a>Функции для коллекций  

Эти функции работы с коллекциями и обычно применяются tooArrays, строки и иногда словарей.  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|contains|Возвращает значение true, если словарь содержит ключ, список содержит значение или строка содержит подстроку. Например, эта функция возвращает `true`: <p>`contains('abacaba','aca')` <p> **Номер параметра:** 1. <p> **Имя:** в коллекции. <p> **Описание:** обязательно. toosearch коллекции Hello в пределах. <p> **Номер параметра:** 2. <p> **Имя:** объект поиска. <p> **Описание:** обязательно. Hello объекта toofind внутри hello **в коллекции**.|  
|длина|Возвращает hello количество элементов в массиве или строка. Например, эта функция возвращает `3`:  <p>`length('abc')` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Коллекция Hello для длиной tooget hello.|  
|empty|Возвращает значение true, если объект, массив или строка пуста. Например, эта функция возвращает `true`: <p>`empty('')` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Здравствуйте toocheck коллекции, если она пуста.|  
|пересечению|Возвращает массив или объект, имеющий общие элементы между переданными массивами или объектами. Например, эта функция возвращает `[1, 2]`: <p>`intersection([1, 2, 3], [101, 2, 1, 10],[6, 8, 1, 2])` <p>Hello параметров для функции hello может быть набор объектов или набор массивов (не сочетание обоих). При наличии двух объектов с hello одинаковые имена, hello последнего объект с таким именем отобразится в последний объект hello. <p> **Номер параметра:** 1… *n*. <p> **Имя:** коллекция *n*. <p> **Описание:** обязательно. Hello tooevaluate коллекций. Объект должен быть во всех коллекциях, переданный tooappear в результате hello.|  
|union|Возвращает массив или объект со всеми элементами hello, переданные в массива или объекта функции toothis. Например, эта функция возвращает `[1, 2, 3, 10, 101]`: <p>`union([1, 2, 3], [101, 2, 1, 10])` <p>Hello параметров для функции hello может быть набор объектов или набор массивов (не как его). Если имеются два объекта с hello точно такое же имя в итоговых данных hello, hello последний объект с таким именем отображается в результирующий объект hello. <p> **Номер параметра:** 1… *n*. <p> **Имя:** коллекция *n*. <p> **Описание:** обязательно. Hello tooevaluate коллекций. Объект, который отображается в одной из коллекций hello также появляется в результате hello.|  
|first|Возвращает первый элемент в массиве hello Здравствуйте, или строка, передаваемая в. Например, эта функция возвращает `0`: <p>`first([0,2,3])` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Hello коллекции tootake hello первый объект из.|  
|last|Возвращает hello последнего элемента в массиве hello, или строка, передаваемая в. Например, эта функция возвращает `3`: <p>`last('0123')` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Hello tootake hello последний объект коллекции из.|  
|take|Возвращает первые hello **число** переданный элементов из массива hello или строки. Например, эта функция возвращает `[1, 2]`:  <p>`take([1, 2, 3, 4], 2)` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Hello коллекции, из которой tootake hello сначала **число** объектов. <p> **Номер параметра:** 2. <p> **Имя:** число. <p> **Описание:** обязательно. Здравствуйте, число tootake объекты из hello **коллекции**. Принимаются только положительные целые числа.|  
|skip|Возвращает hello элементов в массиве hello, начиная с индекса **число**. Например, эта функция возвращает `[3, 4]`: <p>`skip([1, 2 ,3 ,4], 2)` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Сначала Hello hello коллекции tooskip **число** объекты из. <p> **Номер параметра:** 2. <p> **Имя:** число. <p> **Описание:** обязательно. Здравствуйте, число tooremove объектов из начала hello **коллекции**. Принимаются только положительные целые числа.|  
|join|Возвращает строку, где все элементы массива объединены с помощью разделителей, например `"1,2,3,4"` возвращается как:<br /><br /> `join([1, 2, 3, 4], ',')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** коллекция.<br /><br /> **Описание:** обязательно. Hello коллекции toojoin элементы.<br /><br /> **Номер параметра:** 2.<br /><br /> **Имя:** разделитель.<br /><br /> **Описание:** обязательно. элементы toodelimit строку Hello с.|  
  
### <a name="string-functions"></a>Строковые функции

следующие функции только Hello применяются toostrings. Вы также можете использовать в строках некоторые функции для коллекций.  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|concat|Объединяет любое число строк. Например, если параметр 1 — `p1`, эта функция возвращает `somevalue-p1-somevalue` так: <p>`concat('somevalue-',parameters('parameter1'),'-somevalue')` <p> **Номер параметра:** 1… *n*. <p> **Имя:** строка *n*. <p> **Описание:** обязательно. Hello toocombine строк в одну строку.|  
|substring|Возвращает подмножество символов из строки. Например, эта функция возвращает `abc`: <p>`substring('somevalue-abc-somevalue',10,3)` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка Hello, из которого hello извлекается подстрока. <p> **Номер параметра:** 2. <p> **Имя:** индекс начала. <p> **Описание:** обязательно. Индекс Hello которой начинается подстрока hello в параметре 1. <p> **Номер параметра:** 3. <p> **Имя:** длина. <p> **Описание:** обязательно. длина подстроки hello Hello.|  
|replace|Заменяет строку заданной строкой. Например, эта функция возвращает `hello new string`: <p>`replace('hello old string', 'old', 'new')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка Hello, просматриваемого 2 для параметра и обновлять с параметром 3, если параметр 2 в параметре 1. <p> **Номер параметра:** 2. <p> **Имя:** старая строка. <p> **Описание:** обязательно. tooreplace строка Hello с параметром 3, если найдено совпадение в параметре 1 <p> **Номер параметра:** 3. <p> **Имя:** новая строка. <p> **Описание:** обязательно. Hello строка, являющаяся используется tooreplace строку hello в параметр 2, если найдено совпадение в параметре 1.|  
|GUID|Эта функция создает глобально уникальную строку (идентификатор GUID). Например, эта функция может создавать такой идентификатор GUID: `c2ecc88d-88c8-4096-912c-d6f2e2b138ce` <p>`guid()` <p> **Номер параметра:** 1. <p> **Имя:** формат. <p> **Описание:** необязательно. Единственный описатель формата, указывающее [как tooformat hello значение этого Guid](https://msdn.microsoft.com/library/97af8hh4%28v=vs.110%29.aspx). параметр формата Hello может быть «N», «D», «B», «P» или «X». Если формат не указан, используется D.|  
|toLower|Преобразует toolowercase строки. Например, эта функция возвращает `two by two is four`: <p>`toLower('Two by Two is Four')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. регистр toolower tooconvert строку Hello. Если строка hello не имеет эквивалент в нижнем регистре, hello включен символ без изменений в вернул строку hello.|  
|toUpper|Преобразует toouppercase строки. Например, эта функция возвращает `TWO BY TWO IS FOUR`: <p>`toUpper('Two by Two is Four')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. регистр tooupper tooconvert строку Hello. Если строка hello не имеет эквивалента в верхнем регистре, hello включен символ без изменений в вернул строку hello.|  
|indexof|Найти индекс hello значения в случае строка их. Например, эта функция возвращает `7`: <p>`indexof('hello, world.', 'world')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка Hello, может содержать значение hello. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Hello значение toosearch hello индекс.|  
|lastindexof|Найти hello последний индекс точки данных в случае строка их. Например, эта функция возвращает `3`: <p>`lastindexof('foofoo', 'foo')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка Hello, может содержать значение hello. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Hello значение toosearch hello индекс.|  
|startswith|Проверяет, если строка hello начинается с варианта значение их. Например, эта функция возвращает `true`: <p>`startswith('hello, world', 'hello')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка Hello, может содержать значение hello. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка hello Hello значения может начинаться с.|  
|endswith|Проверяет строку hello, оканчивается ли значение варианта их. Например, эта функция возвращает `true`: <p>`endswith('hello, world', 'world')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка Hello, может содержать значение hello. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. может заканчиваться строка hello Hello значения.|  
|split|Разбивает строку hello, с помощью разделителя. Например, эта функция возвращает `["a", "b", "c"]`: <p>`split('a;b;c',';')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка Hello распределенный. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. разделитель Hello.|  
  
### <a name="logical-functions"></a>Логические функции  

Эти функции полезны в условиях и может быть tooevaluate используется логика любого типа.  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|equals (равно)|Возвращает значение true, если два значения равны. Например, если параметр 1 — someValue, эта функция возвращает `true`: <p>`equals(parameters('parameter1'), 'someValue')` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. слишком Hello объекта toocompare**2 объекта**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. слишком Hello объекта toocompare**объектов 1**.|  
|less|Возвращает значение true, если первый аргумент hello меньше hello секунды. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `true`: <p>`less(10,100)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Hello объекта toocheck, если это меньше, чем **2 объекта**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Hello объекта toocheck, если это значение превышает **объектов 1**.|  
|lessOrEquals|Возвращает значение true, если первый аргумент hello меньше или равен toohello секунды. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `true`: <p>`lessOrEquals(10,10)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Hello объекта toocheck, если он меньше или равен слишком**2 объекта**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Hello объекта toocheck, если оно больше или равен слишком**объектов 1**.|  
|greater|Возвращает значение true, если первый аргумент hello больше hello второй. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `false`:  <p>`greater(10,10)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Hello объекта toocheck, если это значение превышает **2 объекта**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Hello объекта toocheck, если это меньше, чем **объектов 1**.|  
|greaterOrEquals|Возвращает значение true, если первый аргумент hello больше или равно toohello второй. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `false`: <p>`greaterOrEquals(10,100)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Hello объекта toocheck, если оно больше или равен слишком**2 объекта**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Hello объекта toocheck, если он меньше или равно слишком**объектов 1**.|  
|и|Возвращает значение true, если оба параметра получают значение true. Оба аргумента должны toobe логические значения. Например, эта функция возвращает `false`: <p>`and(greater(1,10),equals(0,0))` <p> **Номер параметра:** 1. <p> **Имя:** логическое значение 1. <p> **Описание:** обязательно. Здравствуйте, первый аргумент, который должен быть `true`. <p> **Номер параметра:** 2. <p> **Имя:** логическое значение 2. <p> **Описание:** обязательно. Hello второй аргумент должен быть `true`.|  
|или|Возвращает значение true, если какой-либо из параметров получает значение true. Оба аргумента должны toobe логические значения. Например, эта функция возвращает `true`: <p>`or(greater(1,10),equals(0,0))` <p> **Номер параметра:** 1. <p> **Имя:** логическое значение 1. <p> **Описание:** обязательно. Здравствуйте, первый аргумент, который может быть `true`. <p> **Номер параметра:** 2. <p> **Имя:** логическое значение 2. <p> **Описание:** обязательно. Второй аргумент Hello может быть `true`.|  
|not|Возвращает значение true, если параметры hello `false`. Оба аргумента должны toobe логические значения. Например, эта функция возвращает `true`: <p>`not(contains('200 Success','Fail'))` <p> **Номер параметра:** 1. <p> **Имя:** логическое значение. <p> **Описание**: возвращает значение true, если параметры hello `false`. Оба аргумента должны toobe логические значения. Эта функция возвращает `true`: `not(contains('200 Success','Fail'))`|  
|if|Возвращает значение указанного по ли выражение hello привело к `true` или `false`.  Например, эта функция возвращает `"yes"`: <p>`if(equals(1, 1), 'yes', 'no')` <p> **Номер параметра:** 1. <p> **Имя:** выражение. <p> **Описание:** обязательно. Логическое значение, определяющее, какое именно выражение hello значение должна возвращать. <p> **Номер параметра:** 2. <p> **Имя**: true. <p> **Описание:** обязательно. Здравствуйте tooreturn значение, если выражение hello `true`. <p> **Номер параметра:** 3. <p> **Имя:** false. <p> **Описание:** обязательно. Здравствуйте tooreturn значение, если выражение hello `false`.|  
  
### <a name="conversion-functions"></a>Функции преобразования  

Эти функции являются используется tooconvert между каждым hello собственных типов в языке hello:  
  
- string  
  
- целое число  
  
- float;  
  
- Логическое  
  
- arrays;  
  
- dictionaries;  

-   forms.  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|int|Преобразует целое число tooan параметр hello. Например, эта функция возвращает 100 в виде числа, а не строки: <p>`int('100')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. значение Hello, преобразованный tooan целое число со знаком.|  
|string|Преобразование строки tooa параметр hello. Например, эта функция возвращает `'10'`: <p>`string(10)` <p>Можно также преобразовать строки tooa объекта. Например, если hello `myPar` параметр представляет собой объект, с одним свойством `abc : xyz`, то эта функция возвращает `{"abc" : "xyz"}`: <p>`string(parameters('myPar'))` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Hello значение, представляющее преобразованный tooa строки.|  
|json|Преобразовать значение типа JSON tooa параметра hello и — hello противоположного из `string()`. Например, эта функция возвращает `[1,2,3]` как массив, а не строку: <p>`json('[1,2,3]')` <p>Аналогично можно преобразовать объект tooan строки. Например, эта функция возвращает `{ "abc" : "xyz" }`: <p>`json('{"abc" : "xyz"}')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Hello строка, являющаяся tooa преобразованное значение собственного типа. <p>Hello `json()` функция поддерживает входные данные слишком XML. Здравствуйте, например, значение параметра: <p>`<?xml version="1.0"?> <root>   <person id='1'>     <name>Alan</name>     <occupation>Engineer</occupation>   </person> </root>` <p>— Преобразованный toothis JSON: <p>`{ "?xml": { "@version": "1.0" },   "root": {     "person": [     {       "@id": "1",       "name": "Alan",       "occupation": "Engineer"     }   ]   } }`|  
|float;|Преобразование числа с плавающей запятой tooa аргумент параметра hello. Например, эта функция возвращает `10.333`: <p>`float('10.333')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Hello значение, представляющее преобразованный tooa число с плавающей запятой.|  
|bool|Преобразуйте параметр hello tooa логическое значение. Например, эта функция возвращает `false`: <p>`bool(0)` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Здравствуйте, tooa преобразованное значение типа boolean.|  
|coalesce|Возвращает первый ненулевой объект hello hello аргументы, передаваемые в. **Примечание.** Пустая строка не имеет значение null. Например, если параметры 1 и 2 не определены, эта функция возвращает `fallback`:  <p>`coalesce(parameters('parameter1'), parameters('parameter2') ,'fallback')` <p> **Номер параметра:** 1… *n*. <p> **Имя:** объект *n* <p> **Описание:** обязательно. Hello toocheck объектов со значением NULL.|  
|base64|Возвращает hello представление base64 hello входной строки. Например, эта функция возвращает `c29tZSBzdHJpbmc=`: <p>`base64('some string')` <p> **Номер параметра:** 1. <p> **Имя:** строка 1. <p> **Описание:** обязательно. tooencode строка Hello в представление base64.|  
|base64ToBinary|Возвращает двоичное представление строки в кодировке Base64. Например, эта функция возвращает hello двоичное представление `some string`: <p>`base64ToBinary('c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка в кодировке Hello base64.|  
|base64ToString|Возвращает строковое представление строки в кодировке Base64. Например, эта функция возвращает `some string`: <p>`base64ToString('c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка в кодировке Hello base64.|  
|Binary|Возвращает двоичное представление значения.  Например, эта функция возвращает двоичное представление значения `some string`: <p>`binary('some string')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. значение Hello, преобразованный toobinary.|  
|dataUriToBinary|Возвращает двоичное представление URI данных. Например, эта функция возвращает hello двоичное представление `some string`: <p>`dataUriToBinary('data:;base64,c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. данные Hello представление toobinary tooconvert URI.|  
|dataUriToString|Возвращает строковое представление URI данных. Например, эта функция возвращает `some string`: <p>`dataUriToString('data:;base64,c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка.<p> **Описание:** обязательно. данные Hello представление tooString tooconvert URI.|  
|dataUri|Возвращает URI данных значения. Например, эта функция возвращает такой URI данных `text/plain;charset=utf8;base64,c29tZSBzdHJpbmc=`: <p>`dataUri('some string')` <p> **Номер параметра:** 1.<p> **Имя:** значение.<p> **Описание:** обязательно. toodata tooconvert Hello значение URI.|  
|decodeBase64|Возвращает строковое представление входной строки в кодировке Base64. Например, эта функция возвращает `some string`: <p>`decodeBase64('c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** возвращает строковое представление входной строки в кодировке Base64.|  
|encodeUriComponent|Строка hello Экранирует символы URL-адрес, передаваемый в. Например, эта функция возвращает `You+Are%3ACool%2FAwesome`: <p>`encodeUriComponent('You Are:Cool/Awesome')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Hello строка символов tooescape небезопасный URL-адрес из.|  
|decodeUriComponent|Отмена URL-адрес переходов hello строка, передаваемая в. Например, эта функция возвращает `You Are:Cool/Awesome`: <p>`encodeUriComponent('You+Are%3ACool%2FAwesome')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Hello toodecode hello небезопасный URL-адрес символов строки из.|  
|decodeDataUri|Возвращает двоичное представление входной строки URI данных. Например, эта функция возвращает hello двоичное представление `some string`: <p>`decodeDataUri('data:;base64,c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. toodecode dataURI Hello в двоичное представление.|  
|uriComponent|Возвращает закодированный URI. Например, эта функция возвращает `You+Are%3ACool%2FAwesome`: <p>`uriComponent('You Are:Cool/Awesome')` <p> **Номер параметра:** 1.<p> **Имя:** строка. <p> **Описание:** обязательно. toobe строка Hello закодированный URI.|  
|uriComponentToBinary|Возвращает двоичное представление строки с закодированным URI. Например, эта функция возвращает двоичное представление значения `You Are:Cool/Awesome`: <p>`uriComponentToBinary('You+Are%3ACool%2FAwesome')` <p> **Номер параметра:** 1. <p> **Имя:** строка.<p> **Описание:** обязательно. Строка в кодировке Hello URI.|  
|uriComponentToString|Возвращает строковое представление строки с закодированным URI. Например, эта функция возвращает `You Are:Cool/Awesome`: <p>`uriComponentToBinary('You+Are%3ACool%2FAwesome')` <p> **Номер параметра:** 1.<p> **Имя:** строка.<p> **Описание:** обязательно. Строка в кодировке Hello URI.|  
|xml|Возвращает XML-представление значение hello. Например, эта функция возвращает XML-содержимое, представленное `'\<name>Alan\</name>'`: <p>`xml('\<name>Alan\</name>')` <p>Hello `xml()` функция поддерживает входные данные слишком объекта JSON. Например, hello параметр `{ "abc": "xyz" }` -преобразованный tooXML содержимого:`\<abc>xyz\</abc>` <p> **Номер параметра:** 1.<p> **Имя:** значение.<p> **Описание:** обязательно. tooXML tooconvert значение Hello.|  
|xpath|Возвращает массив XML-узлы в выражении xpath hello значения, результатом которого является выражением xpath hello. <p> **Пример 1** <p>Предположим, значение параметра hello `p1` является строковым представлением этого XML: <p>`<?xml version="1.0"?> <lab>   <robot>     <parts>5</parts>     <name>R1</name>   </robot>   <robot>     <parts>8</parts>     <name>R2</name>   </robot> </lab>` <p>Код `xpath(xml(parameters('p1'), '/lab/robot/name')` <p>возвращает <p>`[ <name>R1</name>, <name>R2</name> ]` <p>А код <p>`xpath(xml(parameters('p1'), ' sum(/lab/robot/parts)')` <p>возвращает <p>`13` <p> <p> **Пример 2** <p>Следующий XML-содержимое заданного hello: <p>`<?xml version="1.0"?> <File xmlns="http://foo.com">   <Location>bar</Location> </File>` <p>Код `@xpath(xml(body('Http')), '/*[name()=\"File\"]/*[name()=\"Location\"]')` <p>или код <p>`@xpath(xml(body('Http')), '/*[local-name()=\"File\" and namespace-uri()=\"http://foo.com\"]/*[local-name()=\"Location\" and namespace-uri()=\"\"]')` <p>возвращает <p>`<Location xmlns="http://abc.com">xyz</Location>` <p>А код `@xpath(xml(body('Http')), 'string(/*[name()=\"File\"]/*[name()=\"Location\"])')` <p>возвращает <p>``xyz`` <p> **Номер параметра:** 1. <p> **Имя:** XML. <p> **Описание:** обязательно. Hello XML, на какие tooevaluate hello XPath выражения. <p> **Номер параметра:** 2. <p> **Имя:** XPath. <p> **Описание:** обязательно. tooevaluate выражение XPath Hello.|  
|array|Преобразуйте массив tooan параметров hello. Например, эта функция возвращает `["abc"]`: <p>`array('abc')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. значение Hello, преобразованный tooan массива.|
|createArray|Создает массив из параметров hello. Например, эта функция возвращает `["a", "c"]`: <p>`createArray('a', 'c')` <p> **Номер параметра:** 1… *n*. <p> **Имя:** параметр с любым *n*. <p> **Описание:** обязательно. Hello toocombine значений в массив.|
|triggerFormDataValue|Возвращает одно значение совпадает с именем ключа hello из данных формы или вывода закодированное триггера.  При наличии нескольких соответствий возникает ошибка.  Например, будут возвращены следующие hello `bar`:`triggerFormDataValue('foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа Hello tooreturn значение данных формы hello.|
|triggerFormDataMultiValues|Возвращает массив значений для сопоставления имени ключа hello из данных формы или вывода закодированное триггера.  Например, будут возвращены следующие hello `["bar"]`:`triggerFormDataValue('foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа Hello tooreturn значения данных формы hello.|
|triggerMultipartBody|Возвращает hello текста для части составного выходом hello триггера.<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** индекс.<br /><br />**Описание:** обязательно. Индекс части tooretrieve hello Hello.|
|formDataValue|Возвращает одно сопоставление имени ключа hello из данных формы или выходные данные действия закодированное значение.  При наличии нескольких соответствий возникает ошибка.  Например, будут возвращены следующие hello `bar`:`formDataValue('someAction', 'foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя действия.<br /><br />**Описание:** обязательно. Имя Hello hello действия с данные формы или закодированное ответом.<br /><br />**Номер параметра:** 2.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа Hello tooreturn значение данных формы hello.|
|formDataMultiValues|Возвращает массив значений для сопоставления имени ключа hello из данных формы или выходные данные действия, зашифрованные в форме.  Например, будут возвращены следующие hello `["bar"]`:`formDataMultiValues('someAction', 'foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя действия.<br /><br />**Описание:** обязательно. Имя Hello hello действия с данные формы или закодированное ответом.<br /><br />**Номер параметра:** 2.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа Hello tooreturn значения данных формы hello.|
|multipartBody|Возвращает hello текста для частей в составном вывода действия.<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя действия.<br /><br />**Описание:** обязательно. Имя действия hello в составном ответе Hello.<br /><br />**Номер параметра:** 2.<br /><br />**Имя:** индекс.<br /><br />**Описание:** обязательно. Индекс части tooretrieve hello Hello.|

### <a name="math-functions"></a>Математические функции  

Эти функции могут использоваться для любого типа чисел: **целых чисел** и **чисел с плавающей запятой**.  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|добавление|Возвращает hello результатом сложения двух чисел hello. Например, эта функция возвращает `20.333`: <p>`add(10,10.333)` <p> **Номер параметра:** 1. <p> **Имя:** слагаемое 1. <p> **Описание:** обязательно. Здравствуйте номеров tooadd слишком**слагаемое в большую сторону 2**. <p> **Номер параметра:** 2. <p> **Имя:** слагаемое 2. <p> **Описание:** обязательно. Здравствуйте номеров tooadd слишком**слагаемое в большую сторону 1**.|  
|sub|Возвращает hello результатом вычитания двух чисел. Например, эта функция возвращает `-0.333`: <p>`sub(10,10.333)` <p> **Номер параметра:** 1. <p> **Имя:** уменьшаемое. <p> **Описание:** обязательно. Номер Hello, **вычитаемое** удаляется из. <p> **Номер параметра:** 2. <p> **Имя:** вычитаемое. <p> **Описание:** обязательно. Здравствуйте, число tooremove из hello **Minuend**.|  
|mul|Возвращает hello результатом умножения двух чисел hello. Например, эта функция возвращает `103.33`: <p>`mul(10,10.333)` <p> **Номер параметра:** 1. <p> **Имя:** множитель 1. <p> **Описание:** обязательно. Здравствуйте, число toomultiply **множимое 2** с. <p> **Номер параметра:** 2. <p> **Имя:** множитель 2. <p> **Описание:** обязательно. Здравствуйте, число toomultiply **множимое 1** с.|  
|div|Возвращает результат hello от деления двух чисел hello. Например, эта функция возвращает `1.0333`: <p>`div(10.333,10)` <p> **Номер параметра:** 1. <p> **Имя:** делимое. <p> **Описание:** обязательно. Здравствуйте номеров toodivide по hello **делитель**. <p> **Номер параметра:** 2. <p> **Имя:** делитель. <p> **Описание:** обязательно. число toodivide hello Hello **делимое** по.|  
|mod (модуль)|Возвращает hello остаток после деления двух чисел hello (остаток от деления). Например, эта функция возвращает `2`: <p>`mod(10,4)` <p> **Номер параметра:** 1. <p> **Имя:** делимое. <p> **Описание:** обязательно. Здравствуйте номеров toodivide по hello **делитель**. <p> **Номер параметра:** 2. <p> **Имя:** делитель. <p> **Описание:** обязательно. число toodivide hello Hello **делимое** по. После разделения hello берется остаток hello.|  
|Min|Существует два разных метода вызова этой функции. <p>Здесь `min` принимает массив, и возвращает функции hello `0`: <p>`min([0,1,2])` <p>Кроме того, эта функция может принять разделенный запятыми список значений и также вернуть `0`: <p>`min(0,1,2)` <p> **Примечание**: все значения должны быть числами, поэтому если hello является массивом, hello массив имеет tooonly имеют номера. <p> **Номер параметра:** 1. <p> **Имя:** коллекция или значение. <p> **Описание:** обязательно. Либо массив значений toofind hello минимальное значение или hello первое значение набора. <p> **Номер параметра:** 2… *n*. <p> **Имя:** значение *n*. <p> **Описание:** необязательно. Если значение первого параметра hello, затем можно передать дополнительные значения и hello минимум всех значений, переданных возвращается.|  
|max|Существует два разных метода вызова этой функции. <p>Здесь `max` принимает массив, и возвращает функции hello `2`: <p>`max([0,1,2])` <p>Кроме того, эта функция может принять разделенный запятыми список значений и также вернуть `2`: <p>`max(0,1,2)` <p> **Примечание**: все значения должны быть числами, поэтому если hello является массивом, hello массив имеет tooonly имеют номера. <p> **Номер параметра:** 1. <p> **Имя:** коллекция или значение. <p> **Описание:** обязательно. Либо массив значений toofind hello максимальное значение или hello первое значение набора. <p> **Номер параметра:** 2… *n*. <p> **Имя:** значение *n*. <p> **Описание:** необязательно. Если значение первого параметра hello, затем можно передать дополнительные значения и hello максимум всех значений, переданных возвращается.|  
|range|Создает массив целых чисел, начиная с определенного числа. Определяется длина hello hello в возвращаемый массив. <p>Например, эта функция возвращает `[3,4,5,6]`: <p>`range(3,4)` <p> **Номер параметра:** 1. <p> **Имя:** индекс начала. <p> **Описание:** обязательно. Hello первое целое число в массиве hello. <p> **Номер параметра:** 2. <p> **Имя:** число. <p> **Описание:** обязательно. Это значение является числом hello целых чисел, в массиве hello.|  
|rand|Создает случайный целое число в hello указанный диапазон (включая границы только первой стороны). Например, эта функция может вернуть `0` или 1. <p>`rand(0,2)` <p> **Номер параметра:** 1. <p> **Имя:** минимальное значение. <p> **Описание:** обязательно. Hello наименьшее целое число, которое может быть возвращено. <p> **Номер параметра:** 2. <p> **Имя:** максимальное значение. <p> **Описание:** обязательно. Это значение является целым числом Далее hello после hello наибольшее целое число, которое может быть возвращено.|  
  
### <a name="date-functions"></a>Функции данных  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|utcnow|Возвращает hello Текущая отметка времени как строка, например: `2017-03-15T13:27:36Z`: <p>`utcnow()` <p> **Номер параметра:** 1. <p> **Имя:** формат. <p> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).|  
|addseconds|Добавляет целое число секунд отметка времени строки tooa переданный. Hello количество секунд может быть положительным или отрицательным. По умолчанию hello результатом является строка в формате ISO 8601 формата («o»), если иное не предусмотрено спецификатор формата. Например, `2015-03-15T13:27:00Z`: <p>`addseconds('2015-03-15T13:27:36Z', -36)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время hello. <p> **Номер параметра:** 2. <p> **Имя:** секунды. <p> **Описание:** обязательно. число секунд tooadd Hello. Может быть отрицательным toosubtract секунд. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).|  
|addminutes|Добавляет целое число минут отметка времени строки tooa переданный. Hello количество минут, может быть положительным или отрицательным. По умолчанию hello результатом является строка в формате ISO 8601 формата («o»), если иное не предусмотрено спецификатор формата. Например, `2015-03-15T14:00:36Z`: <p>`addminutes('2015-03-15T13:27:36Z', 33)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время hello. <p> **Номер параметра:** 2. <p> **Имя:** минуты. <p> **Описание:** обязательно. число минут tooadd Hello. Может быть отрицательным toosubtract минут. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).|  
|addhours|Добавляет часы отметка времени строки tooa переданный с целым числом. Hello количество часов, может быть положительным или отрицательным. По умолчанию hello результатом является строка в формате ISO 8601 формата («o»), если иное не предусмотрено спецификатор формата. Например, `2015-03-16T01:27:36Z`: <p>`addhours('2015-03-15T13:27:36Z', 12)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время hello. <p> **Номер параметра:** 2. <p> **Имя:** часы. <p> **Описание:** обязательно. Количество часов tooadd Hello. Может быть отрицательным toosubtract часов. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).|  
|adddays|Добавляет целое число дней отметка времени строки tooa переданный. Hello количество дней, может быть положительным или отрицательным. По умолчанию hello результатом является строка в формате ISO 8601 формата («o»), если иное не предусмотрено спецификатор формата. Например, `2015-02-23T13:27:36Z`: <p>`addseconds('2015-03-15T13:27:36Z', -20)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время hello. <p> **Номер параметра:** 2. <p> **Имя:** дни. <p> **Описание:** обязательно. количество дней tooadd Hello. Может быть отрицательным toosubtract дней. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).|  
|formatDateTime|Возвращает строку в формате даты. По умолчанию hello результатом является строка в формате ISO 8601 формата («o»), если иное не предусмотрено спецификатор формата. Например, `2015-02-23T13:27:36Z`: <p>`formatDateTime('2015-03-15T13:27:36Z', 'o')` <p> **Номер параметра:** 1. <p> **Имя:** дата. <p> **Описание:** обязательно. Строка, содержащая дату hello. <p> **Номер параметра:** 2. <p> **Имя:** формат. <p> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).|  
|startOfHour|Возвращает hello начала hello час tooa строки timestamp переданный. Например, `2017-03-15T13:00:00Z`:<br /><br /> `startOfHour('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время hello.<br /><br />**Номер параметра:** 2.<br /><br /> **Имя:** формат.<br /><br /> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).|  
|startOfDay|Возвращает hello начала hello день tooa строки timestamp переданный. Например, `2017-03-15T00:00:00Z`:<br /><br /> `startOfDay('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время hello.<br /><br />**Номер параметра:** 2.<br /><br /> **Имя:** формат.<br /><br /> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).| 
|startOfMonth|Возвращает hello начала переданный строки timestamp hello месяц tooa. Например, `2017-03-01T00:00:00Z`:<br /><br /> `startOfMonth('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время hello.<br /><br />**Номер параметра:** 2.<br /><br /> **Имя:** формат.<br /><br /> **Описание:** необязательно. Либо [одиночный символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [шаблона пользовательского формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx) , указывающее, каким образом tooformat hello значение этой метки времени. Если формат не указано, используется формат ISO 8601 hello («o»).| 
|dayOfWeek|Возвращает hello дня недели из строки отметки времени.  0 — воскресенье, понедельник — 1 и т. д. Например, `3`:<br /><br /> `dayOfWeek('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время hello.| 
|dayOfMonth|Возвращает hello день месяца компонента отметку времени строки. Например, `15`:<br /><br /> `dayOfMonth('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время hello.| 
|dayOfYear|Возвращает hello день года компонента отметку времени строки. Например, `74`:<br /><br /> `dayOfYear('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время hello.| 
|ticks|Возвращает hello такты свойство отметки времени строки. Например, `1489603019`:<br /><br /> `ticks('2017-03-15T18:36:59Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время hello.| 
  
### <a name="workflow-functions"></a>Функции рабочего процесса  

Эти функции позволяют получить сведения о рабочем процессе hello сам во время выполнения.  
  
|Имя функции|Описание|  
|-------------------|-----------------|  
|listCallbackUrl|Возвращает строку toocall tooinvoke hello триггера или действия. <p> **Примечание.** Эта функция может использоваться только в **httpWebhook** и **apiConnectionWebhook**, но не в **manual**, **recurrence**, **http** или **apiConnection**. <p>Например, hello `listCallbackUrl()` функции: <p>`https://prod-01.westus.logic.azure.com:443/workflows/1235...ABCD/triggers/manual/run?api-version=2015-08-01-preview&sp=%2Ftriggers%2Fmanual%2Frun&sv=1.0&sig=xxx...xxx` |  
|рабочий процесс|Эта функция предоставляет сведения о всех hello для hello самого рабочего процесса во время выполнения hello. <p> Перечислены доступные свойства hello объекта рабочего процесса. <ul><li>`name`</li><li>`type`</li><li>`id`</li><li>`location`</li><li>`run`</li></ul> <p> Здравствуйте, значение hello `run` свойство представляет собой объект со следующими свойствами: <ul><li>`name`</li><li>`type`</li><li>`id`</li></ul> <p>В разделе hello [API-интерфейса Rest](http://go.microsoft.com/fwlink/p/?LinkID=525617) подробные сведения об этих свойствах.<p> Например, tooget hello имя hello текущего выполнения, используйте hello `"@workflow().run.name"` выражение. |

## <a name="next-steps"></a>Дальнейшие действия

[Действия и триггеры рабочего процесса](logic-apps-workflow-actions-triggers.md)
