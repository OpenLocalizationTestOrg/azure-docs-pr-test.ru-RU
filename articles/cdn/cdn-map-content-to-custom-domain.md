---
title: "личный домен aaaMap Azure CDN содержимого tooa | Документы Microsoft"
description: "Узнайте, как toomap Azure CDN содержимого tooa пользовательского домена."
services: cdn
documentationcenter: 
author: zhangmanling
manager: erikre
editor: 
ms.assetid: 289f8d9e-8839-4e21-b248-bef320f9dbfc
ms.service: cdn
ms.workload: media
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 01/23/2017
ms.author: mazha
ms.openlocfilehash: d3ee77297f1dd7dbf31a9391191cc2910fbd2cee
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="map-azure-cdn-content-tooa-custom-domain"></a>Сопоставьте личный домен Azure CDN содержимого tooa
Можно сопоставить конечную точку CDN tooa пользовательский домен в порядке toouse собственное доменное имя в содержимого, а не с помощью поддомена azureedge.net toocached URL-адреса.

Существует два способа toomap конечной точкой CDN tooa пользовательского домена:

1. [Создайте запись CNAME у регистратора домена и сопоставить пользовательский домен и поддомен toohello конечной точкой CDN](#register-a-custom-domain-for-an-azure-cdn-endpoint).
   
    Запись CNAME является функцией DNS, которая сопоставляет исходный домен, например `www.contosocdn.com` или `cdn.contoso.com`, tooa домена назначения. В этом случае hello исходный домен является домен и поддомен (дочерний домен, таких как **www** или **cdn** всегда является обязательным). Hello целевым доменом является конечной точкой CDN.  
   
    Hello сопоставления конечной точки CDN tooyour пользовательского домена можно Однако в результате процесса в короткий период времени простоя для домена hello при регистрации домена hello в hello портала Azure.
2. [Добавление промежуточного этапа регистрации с использованием поддомена **cdnverify**](#register-a-custom-domain-for-an-azure-cdn-endpoint-using-the-intermediary-cdnverify-subdomain)
   
    Если пользовательский домен в настоящее время поддерживает приложения с соглашения об уровне обслуживания (SLA), требует наличия времени простоя, то можно использовать hello Azure **cdnverify** tooprovide поддомен промежуточного регистрации шаг так, чтобы пользователи могли tooaccess вашего домена при hello DNS, сопоставление выполняется.  

После регистрации вашего домена с помощью одного из hello выше процедуры, вы будете слишком[убедитесь, что hello пользовательского поддомена с конечной точкой CDN](#verify-that-the-custom-subdomain-references-your-cdn-endpoint).

> [!NOTE]
> Необходимо создать запись CNAME с вашей toomap регистратора домена конечной точки CDN toohello домена. Записи CNAME сопоставляют определенные поддомены, такие как `www.contoso.com` или `cdn.contoso.com`. Это не возможные toomap корневой домен tooa записей CNAME, такие как `contoso.com`.
> 
> Поддомен может быть связан только с одной конечной точкой CDN. Hello созданная запись CNAME будет пересылать весь трафик, направленный toohello поддомен toohello указан конечной точки.  Например, если сопоставить `www.contoso.com` с конечной точкой CDN, то его нельзя будет сопоставить с другими конечными точками Azure, к примеру с конечной точкой облачной службы или конечной точкой учетной записи хранения. Тем не менее, можно использовать разные поддомены из hello того же домена для разных конечных точек служб. Также можно сопоставлять разные поддомены toohello одной конечной точкой CDN.
> 
> Для **CDN Azure из Verizon** конечных точек (Standard и Premium) Обратите внимание, что он занимает слишком**90 минут** пользовательского домена изменится toopropagate tooCDN краевым узлам.
> 
> 

## <a name="register-a-custom-domain-for-an-azure-cdn-endpoint"></a>Регистрация личного домена для конечной точки Azure CDN
1. Войти в hello [портала Azure](https://portal.azure.com/).
2. Нажмите кнопку **Обзор**, затем **CDN профилей**, затем hello профиля CDN с конечной точкой hello требуется toomap tooa пользовательского домена.  
3. В hello **профиля CDN** колонка, щелкните hello конечной точки CDN, которые должны tooassociate hello поддомен.
4. Hello верхней части колонки hello конечной точки, нажмите кнопку hello **добавления пользовательского домена** кнопки.  В hello **добавить пользовательский домен** колонку, вы увидите имя узла конечной точки hello, производным от конечной точки CDN, toouse при создании новой записи CNAME. Формат Hello hello адрес имени узла будут отображаться как  **&lt;EndpointName >. azureedge.net**.  При создании записи CNAME hello можно скопировать этот toouse имя узла.  
5. Перейдите на веб-сайте регистратора домена tooyour и найдите раздел hello создания записей DNS. Это может быть такой раздел, как **Доменное имя**, **DNS** или **Управление сервером доменных имен**.
6. Найдите раздел hello управления записями CNAME. Могут содержать дополнительные параметры страницы toogo tooan и искать слова hello CNAME, псевдонима или поддомены.
7. Создание новой записи CNAME, которая сопоставляет выбранный вами поддомен (например, **www** или **cdn**) в hello не указано имя узла toohello **добавить пользовательский домен** колонку. 
8. Вернуть toohello **добавить пользовательский домен** колонке и введите свой домен, включая поддомен hello в диалоговое окно «hello». Например, введите имя домена hello в формате hello `www.contoso.com` или `cdn.contoso.com`.   
   
   Azure проверит наличие записи CNAME hello для hello доменное имя, которое вы ввели. Если правильно hello CNAME, проверяется вашего домена.  Для **CDN Azure из Verizon** конечных точек (Standard и Premium), может пройти до too90 минут для пользовательского домена toopropagate tooall CDN edge узлов параметров, однако.  
   
   Обратите внимание, что в некоторых случаях он может занять время для серверов tooname toopropagate записей CNAME hello на hello Интернета. Если домен не проверяется сразу, а вы считаете, что hello записи CNAME указано правильно, подождите несколько минут и повторите попытку.

## <a name="register-a-custom-domain-for-an-azure-cdn-endpoint-using-hello-intermediary-cdnverify-subdomain"></a>Регистрация пользовательского домена для конечной точки Azure CDN с помощью поддомена cdnverify hello
1. Войти в hello [портала Azure](https://portal.azure.com/).
2. Нажмите кнопку **Обзор**, затем **CDN профилей**, затем hello профиля CDN с конечной точкой hello требуется toomap tooa пользовательского домена.  
3. В hello **профиля CDN** колонка, щелкните hello конечной точки CDN, которые должны tooassociate hello поддомен.
4. Hello верхней части колонки hello конечной точки, нажмите кнопку hello **добавления пользовательского домена** кнопки.  В hello **добавить пользовательский домен** колонку, вы увидите имя узла конечной точки hello, производным от конечной точки CDN, toouse при создании новой записи CNAME. Формат Hello hello адрес имени узла будут отображаться как  **&lt;EndpointName >. azureedge.net**.  При создании записи CNAME hello можно скопировать этот toouse имя узла.
5. Перейдите на веб-сайте регистратора домена tooyour и найдите раздел hello создания записей DNS. Это может быть такой раздел, как **Доменное имя**, **DNS** или **Управление сервером доменных имен**.
6. Найдите раздел hello управления записями CNAME. Могут содержать дополнительные параметры страницы toogo tooan и искать слова hello **CNAME**, **псевдоним**, или **поддомены**.
7. Создайте новую запись CNAME и укажите псевдоним поддомена, который включает в себя hello **cdnverify** дочерний домен. Например, указываемый вами поддомен hello будет представлено в формате hello **cdnverify.www** или **cdnverify.cdn**. Введите имя узла hello, которое является конечной точкой CDN, в формате hello **cdnverify.&lt; EndpointName >. azureedge.net**. Ваше сопоставление DNS должно иметь следующий вид: `cdnverify.www.consoto.com   CNAME   cdnverify.consoto.azureedge.net`  
8. Вернуть toohello **добавить пользовательский домен** колонке и введите свой домен, включая поддомен hello в диалоговое окно «hello». Например, введите имя домена hello в формате hello `www.contoso.com` или `cdn.contoso.com`. Обратите внимание, что на этом шаге нет необходимости toopreface hello поддомен с **cdnverify**.  
   
    Azure проверит наличие записи CNAME hello для hello cdnverify доменное имя, которое вы ввели.
9. На этом этапе пользовательский домен проверен Azure, но домен tooyour трафика еще не была перенаправленное tooyour конечной точки CDN. После ожидания настолько долго, toopropagate параметры настраиваемого домена hello tooallow toohello CDN ребро узлов (90 минут для **CDN Azure из Verizon**, 1 – 2 минуты для **Azure CDN с Akamai**), возвращают tooyour DNS веб-сайт регистратора и создайте другую запись CNAME, сопоставляющий конечной точкой CDN tooyour дочерний домен. Например, укажите поддомен hello в виде **www** или **cdn**, и имя узла как hello  **&lt;EndpointName >. azureedge.net**. С помощью этого этапа регистрации hello своего пользовательского домена будет завершена.
10. Наконец, можно удалить запись CNAME hello, созданных с помощью **cdnverify**, поскольку она была нужна только в качестве промежуточного шага.  

## <a name="verify-that-hello-custom-subdomain-references-your-cdn-endpoint"></a>Убедитесь, что этот hello пользовательского поддомена с конечной точкой CDN
* После завершения регистрации hello своего пользовательского домена может обращаться к содержимому, кэшированному в конечной точкой CDN с помощью пользовательского домена hello.
  Во-первых убедитесь, что имеется общедоступное содержимое кэшируется в конечной точке hello. Например если конечная точка CDN связана с учетной записью хранилища, hello CDN кэшировать содержимое в большой двоичный объект открытого контейнеров. tootest hello пользовательского домена, убедитесь, что контейнер задано tooallow общего доступа и что он содержит по крайней мере один BLOB-объект.
* В браузере перейдите адрес toohello hello большого двоичного объекта, с помощью пользовательского домена hello. Например, если вашим доменом является `cdn.contoso.com`, hello URL-адрес tooa кэшированного BLOB-объект будет примерно toohello URL-адреса: http://cdn.contoso.com/mypubliccontainer/acachedblob.jpg

## <a name="see-also"></a>См. также
[Как tooEnable hello доставки содержимого сети (CDN) для Azure](cdn-create-new-endpoint.md)  

