---
title: "Статистика использования aaaAnalyze с Azure CDN расширенные отчеты HTTP | Документы Microsoft"
description: "Узнайте, как toocreate расширенные отчеты HTTP в Microsoft Azure CDN. Эти отчеты предоставляют подробные сведения об активности в сети CDN."
services: cdn
documentationcenter: 
author: zhangmanling
manager: erikre
editor: 
ms.assetid: ef90adc1-580e-4955-8ff1-bde3f3cafc5d
ms.service: cdn
ms.workload: tbd
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 01/23/2017
ms.author: mazha
ms.openlocfilehash: 3184ec00d089613e25c62762f93043cb4ba68394
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/06/2017
---
# <a name="analyze-usage-statistics-with-azure-cdn-advanced-http-reports"></a>Анализ статистики использования с помощью расширенных отчетов HTTP Azure CDN
## <a name="overview"></a>Обзор
В этом документе рассматриваются дополнительные возможности HTTP-отчетов в сети CDN Microsoft Azure. Эти отчеты предоставляют подробные сведения об активности в сети CDN.

[!INCLUDE [cdn-premium-feature](../../includes/cdn-premium-feature.md)]

## <a name="accessing-advanced-http-reports"></a>Доступ к расширенным HTTP-отчетам
1. В колонке профиля CDN hello выберите hello **управление** кнопки.
   
    ![Кнопка управления в колонке профиля CDN](./media/cdn-advanced-http-reports/cdn-manage-btn.png)
   
    Открытие портала управления CDN Hello.
2. Наведите указатель мыши hello **Analytics** , а затем наведите указатель мыши hello **дополнительные отчеты HTTP** всплывающим меню.  Выберите **Большая HTTP-платформа**.
   
    ![Портал управления CDN — меню дополнительных отчетов](./media/cdn-advanced-http-reports/cdn-advanced-reports.png)
   
    Отобразятся параметры отчета.

## <a name="geography-reports-map-based"></a>Географические отчеты (с использованием карт)
Существует пять отчетов, которые используют преимущества tooindicate карты hello регионов, из которых запрашивается контента. К ним относятся: «Мировая карта», «Карта США», «Карта Канады», «Карта Европы» и «Карта Азиатско-Тихоокеанского региона».

Каждый отчет на основе карты ранжирует географических объектов (т. е. странах, состояния и коды городов) в соответствии с toohello процент попаданий, созданные из этого региона. Кроме того карта предоставляется toohelp визуализировать hello расположения, из которых запрашивается контента. Это может toodo так, цветовое кодирование каждой области в соответствии с объемом toohello запросу возникла в этом регионе. Регионы, окрашенные в светлые тона, характеризуются пониженным потоком запросов, тогда как более темные тона указывают на повышенный интерес к вашему содержимому.

Прямо под карты hello предоставляется подробная информация трафика и пропускной способности для каждого региона. Это позволяет tooview hello общее число попаданий, hello процент попаданий, hello общий объем данных, передаваемых (в гигабайтах), а передано hello процент данных для каждого региона. Также вы можете просмотреть описание каждого из этих показателей. Наконец при наведении указателя мыши на область (т. е. страны, область или край) имя hello и hello процент попаданий, произошедших в регионе hello будет отображаться в виде всплывающей подсказки.

Ниже приводится краткое описание каждого типа географического отчета.

| Имя отчета | Описание |
| --- | --- |
| Карта мира |Этот отчет позволяет запросу по всему миру hello tooview для содержимого CDN. Каждая Страна имеет цветовую маркировку на hello world карты tooindicate hello процент попаданий, созданные из этого региона. |
| Карта США |Этот отчет позволяет запросу hello tooview для содержимого CDN в hello США. Каждое состояние является цветами на этот процент hello карты tooindicate попаданий, созданные из этого региона. |
| Карта Канады |Этот отчет позволяет запросу hello tooview для содержимого CDN в Канаде. Каждая область цветами на этот процент hello карты tooindicate попаданий, созданные из этого региона. |
| Карта Европы |Этот отчет позволяет запросу hello tooview для содержимого CDN в Европе. Каждая Страна цветами на этот процент hello карты tooindicate попаданий, созданные из этого региона. |
| Карта Азиатско-Тихоокеанского региона |Этот отчет позволяет запросу hello tooview для содержимого CDN в Азии. Каждая Страна цветами на этот процент hello карты tooindicate попаданий, созданные из этого региона. |

## <a name="geography-reports-bar-charts"></a>Географические отчеты (линейчатые диаграммы)
Существует два дополнительных отчетов, предоставляющих статистические сведения, в соответствии с toogeography, являющиеся верхней города и страны верхней. Эти отчеты ранжирования города и страны, соответственно, в соответствии с toohello число попаданий, созданные из этих областей. При создании такого отчета, линейчатая диаграмма покажет hello top 10 городов или стран, запросил содержимое по определенной платформе. Это гистограмма позволяет tooquickly оценки hello областей, формирующих hello максимальному числу запросов для содержимого.

Hello левой части диаграммы hello (ось y) указывает число попаданий в заданную область hello. Прямо под hello graph (ось x) вы обнаружите метку для каждого из hello top 10 регионов.

### <a name="using-hello-bar-charts"></a>С помощью hello линейчатые диаграммы
* При наведении указателя мыши на полосу имя hello и общее число попаданий, произошедших в регионе hello hello отображается в виде всплывающей подсказки.
* Hello всплывающей подсказкой для hello города Начало отчета определяет город по его имени, штат или провинция и сокращенное обозначение страны.
* Если не удается определить hello Город или область (т. е., штат или провинция), из которой был сформирован запрос, затем он покажет, они неизвестны. Если hello страной является unknown, то два вопросительными знаками (т. е.??), будет отображаться.
* Отчет может включать метрики для «Европа» или hello «Регион Азиатско-Тихоокеанский регион». Эти элементы не предназначены tooprovide статистические данные для всех IP-адресов в этих областях. Вместо этого они относятся только toorequests, берутся из IP-адреса которых находятся на Европе или Азиатско-Тихоокеанский регион вместо tooa определенного города и страны.

Hello данные, которые были используется toogenerate hello линейчатой диаграммы можно просматривать под ней. Приведены hello общее число попаданий, hello процент попаданий hello объем данных, передаваемых (в гигабайтах), и hello процент данных, переданных для hello верхней области 250. Также вы можете просмотреть описание каждого из этих показателей.

Ниже приводится краткое описание обоих отчетов.

| Имя отчета | Описание |
| --- | --- |
| Рейтинг городов |В этом отчете ранжирует городов, в соответствии с toohello число попаданий, созданные из этого региона. |
| Рейтинг стран |В этом отчете ранжирует стран, в соответствии с toohello число попаданий, созданные из этого региона. |

## <a name="daily-summary"></a>Сводка по дням
Hello ежедневные отчетов позволяет tooview hello общее число попаданий и передача данных по определенной платформы на ежедневной основе. Эти сведения можно использовать tooquickly распознавания CDN шаблонов действий. Например, используя этот отчет, вы можете определить дни с повышенным или пониженным объемом трафика в сравнении с ожидаемым объемом.

При создании такого отчета, линейчатую диаграмму предоставит визуальное как сумма toohello опытным спроса платформой на ежедневной основе через hello период времени, охватываемого hello отчета. Он будет сделано, отображая строку за каждый день в отчете hello. При выборе hello период вызвать, например, «Прошлая неделя» создаст линейчатую диаграмму с семью столбцами. В каждой строке появится hello общее число попаданий в результате в этот день.

Hello левой части диаграммы hello (ось y) указывает число попаданий произошло в указанный hello даты. Прямо под hello graph (ось x), можно найти метку, которая указывает дату hello (формат: гггг-мм-дд) для каждого дня, включены в отчет hello.

> [!TIP]
> При наведении указателя мыши на полосу hello общее число попаданий, произошедших в этот день будет отображаться в виде всплывающей подсказки.
> 
> 

Hello данные, которые были используется toogenerate hello линейчатой диаграммы можно просматривать под ней. Там найти hello общее число попаданий и hello объем данных, передаваемых (в гигабайтах) для каждого дня входящего hello отчет.

## <a name="by-hour"></a>Сводка по времени
Hello отчетов в час позволяет tooview hello общее число попаданий и передача данных по определенной платформы на почасовой основе. Эти сведения можно использовать tooquickly распознавания CDN шаблонов действий. Например в этом отчете позволяют определить периоды времени, в течение дня hello столкнуться выше или ниже, чем ожидаемого трафика hello.

При создании такого отчета, линейчатую диаграмму предоставит визуальное как toohello объем произошел ежечасно через hello период времени, охватываемого hello отчета запросу конкретную платформу. Он будет сделано, отображая строку для каждого часа входящего hello отчет. Например, если выбрать период «24 часа», будет создана линейчатая диаграмма с двадцатью четырьмя отрезками. В каждой строке появится hello общее число попаданий, возникших в течение этого часа.

Hello левой части диаграммы hello (ось y) указывает, произошло число попаданий в указанный час hello. Прямо под hello graph (ось x), можно найти метку, которая указывает hello даты и времени (формат: гггг-мм-дд чч: мм) для каждого часа в отчете hello. Затрачено времени с использованием 24-часовом формате и он указан с использованием часового пояса UTC-GMT hello.

> [!TIP]
> При наведении указателя мыши на полосу hello общее число попаданий, произошедших в течение этого часа будет отображаться в виде всплывающей подсказки.
> 
> 

Hello данные, которые были используется toogenerate hello линейчатой диаграммы можно просматривать под ней. Там найти hello общее число попаданий и hello объем данных, передаваемых (в гигабайтах) для каждого часа входящего hello отчет.

## <a name="by-file"></a>Сводка по файлам
Файл с отчет позволяет вам tooview hello объем трафика спроса и hello нагрузки на определенной платформе для hello наиболее Hello по запросу активы. При создании такого отчета, линейчатая диаграмма создается по средствам top 10 наиболее часто запрашиваемых hello за указанный период времени hello.

> [!NOTE]
> В целях hello этого отчета edge CNAME URL-адреса являются преобразованный tootheir соответствующие CDN URL-адреса. Это позволяет точных результатов голосования для hello общее число попаданий, связанную с актива независимо от hello CDN или край CNAME URL-адрес используется toorequest его.
> 
> 

Hello левой части диаграммы hello (ось y) указывает hello число запросов для каждого ресурса за указанный период времени hello. Прямо под hello graph (ось x) вы обнаружите метку, которая указывает имя файла hello для каждого из средств top 10 запрошенного hello.

Hello данные, которые были используется toogenerate hello линейчатой диаграммы можно просматривать под ней. Приведены следующие сведения для каждого из средств top 250 запрошенного hello hello: относительный путь, hello общее число попаданий, hello процент попаданий, hello объем данных, передаваемых (в гигабайтах) и процент hello переданных данных.

## <a name="by-file-detail"></a>Подробная сводка по файлам
Hello, подробные сведения о файла отчета позволяет tooview hello объем трафика спроса и hello нагрузки на определенной платформе для конкретного ресурса. Hello hello файл сведений для параметра является самой верхней части этого отчета. Этот параметр предоставляет список наиболее часто запрашиваемых ресурсов на выбранной платформе hello. В порядке toogenerate по подробного файла отчета необходимо будет tooselect hello нужное средство hello файл сведений для параметра. После чего линейчатая диаграмма покажет hello объем ежедневный спрос, вызвавшего за указанный период времени hello.

Hello левой части диаграммы hello (ось y) указывает hello общее число запросов, что ресурс возникла в определенный день. Прямо под hello graph (ось x), можно найти метку, которая указывает дату hello (формат: гггг-мм-дд) для которых потребность в CDN hello сообщалось активов.

Hello данные, которые были используется toogenerate hello линейчатой диаграммы можно просматривать под ней. Там найти hello общее число попаданий и hello объем данных, передаваемых (в гигабайтах) для каждого дня входящего hello отчет.

## <a name="by-file-type"></a>Сводка по типу файлов
Hello по типу файла отчета позволяет вам tooview hello объема спроса и hello трафика нагрузки по типу файла. При создании такого отчета, кольцевая диаграмма покажет hello процент попаданий в созданные hello top 10 файлов типов.

> [!TIP]
> При наведении сегмента в кольцевой график hello hello Интернет тип носителя, тип файла отображалось в виде всплывающей подсказки.
> 
> 

можно просмотреть Hello данные, которые были используется toogenerate hello кольцевой график под ней. Приведены тип носителя, Интернета расширение имени файла hello, общее число попаданий hello, hello процент попаданий, hello объем данных, передаваемых (в гигабайтах), а hello процент данных, переданных для каждого hello первые 250 типы файлов.

## <a name="by-directory"></a>Сводка по каталогам
отчет по Directory Hello позволяет tooview hello объем трафика спроса и hello нагрузки на определенной платформе для содержимого из конкретного каталога. При создании такого отчета, линейчатая диаграмма покажет hello общее число попаданий, созданное содержимое в hello top 10 каталогов.

### <a name="using-hello-bar-chart"></a>С помощью hello линейчатой диаграммы
* Наведите указатель мыши на полосу tooview hello относительный путь toohello соответствующий каталог.
* При расчете объема данных по каталогу содержимое из вложенных папок не учитывается. Этот расчет использует только hello число запросов, созданных для содержимого, хранящегося в действительных папках hello.
* В целях hello этого отчета edge CNAME URL-адреса являются преобразованный tootheir соответствующие CDN URL-адреса. Это позволяет точных результатов голосования для всей статистики, связанную с актива независимо от hello CDN или край CNAME URL-адрес используется toorequest его.

Hello левой части диаграммы hello (ось y) указывает общее число запросов hello содержимого, хранящегося в каталогах top 10 hello. Каждой линии на диаграмме hello представляет каталог. Используйте hello цветовое кодирование toomatch схема отрезки tooa каталога, указанные в разделе каталоги полный 250 верхней hello.

Hello данные, которые были используется toogenerate hello линейчатой диаграммы можно просматривать под ней. Приведены следующие сведения для каждого из каталогов верхней 250 hello hello: относительный путь, hello общее число попаданий, hello процент попаданий, hello объем данных, передаваемых (в гигабайтах) и процент hello переданных данных.

## <a name="by-browser"></a>Сводка по браузерам
Hello, обозреватель отчетов позволяет tooview используется toorequest содержимого были какие браузеры. При создании такого отчета, круговая диаграмма покажет hello процент запросов, обрабатываемых hello top 10 браузеры.

### <a name="using-hello-pie-chart"></a>С помощью hello круговой диаграммы
* Наведите указатель на сектора в круговой диаграмме tooview hello браузера имени и версии.
* В целях hello этого отчета каждой комбинации уникальный обозреватель считается другой браузер.
* Hello срез с названием «Другие» указывает hello процент запросов, обрабатываемых других браузеров и версий.

Hello данные, которые были используется toogenerate hello круговой диаграммы можно просматривать под ней. Приведены hello браузера типа/и номер версии, общее число попаданий hello hello процент обращений для каждого самые популярные браузеры 250 hello.

## <a name="by-referrer"></a>Сводка по источникам ссылок
Hello отчета по Referrer позволяет toocontent лучшие источники ссылок hello tooview на выбранной платформе hello. Источник ссылки указывает hello имя узла, из которого создается запрос. При создании такого отчета, линейчатая диаграмма покажет hello объем запросу (т. е. попаданий), созданные hello лучшие 10 источники ссылок.

Hello левой части диаграммы hello (ось y) указывает hello общее число запросов, что ресурс возникла для каждого источника ссылки. Каждый столбец на диаграмме hello представляет источник ссылки. Используйте hello цветовое кодирование toomatch схема отрезки tooa referrer, перечисленные в разделе верхнего 250 Referrer hello.

Hello данные, которые были используется toogenerate hello линейчатой диаграммы можно просматривать под ней. Там будет найти URL-адрес hello, общее число попаданий hello и hello процент попаданий, созданные из каждого hello лучшие 250 источники ссылок.

## <a name="by-download"></a>Сводка по загрузкам
Привет, загрузка отчета позволяет tooanalyze загрузки шаблоны для наиболее часто запрашиваемых содержимого. Hello верхней части отчета hello содержит линейчатую диаграмму, сравнивает попытка загрузки с завершенной загрузки для hello top 10 запрошенных ресурсов. Каждой линии цветами в соответствии с его toowhether попытки загрузки (синий цвет) или завершения загрузки (зеленый).

> [!NOTE]
> В целях hello этого отчета edge CNAME URL-адреса являются преобразованный tootheir соответствующие CDN URL-адреса. Это позволяет точных результатов голосования для всей статистики, связанную с актива независимо от hello CDN или край CNAME URL-адрес используется toorequest его.
> 
> 

Hello левой части диаграммы hello (ось y) указывает имя файла hello для каждого из средств top 10 запрошенного hello. Прямо под hello graph (ось x) вы обнаружите метки, которые указывают на общее количество попыток выполнить загрузку hello.

Непосредственно под hello линейчатую диаграмму, hello следующие сведения будут указаны для верхней hello 250 запрошенных ресурсов: относительный путь (включая имя файла), hello, сколько раз он был загруженного toocompletion hello, сколько раз она была запрошена и hello Процент запросов, которые привели к завершения загрузки.

> [!TIP]
> Наша сеть CDN не получает от HTTP-клиентов (т. е. браузеров) информацию о полном завершении загрузки. В результате у нас есть toocalculate ли актив полностью загружено соответствующим toostatus коды и запросы байтового диапазона. Hello первое, что мы найдите после внесения этого вычисления ли запрос hello приводит код состояния 200 OK. Если Да, затем мы рассмотрим tooensure запросы байтового диапазона, они охватывают весь активов hello. Наконец мы сравним hello объем данных, передаваемых toohello размер hello запрошенного ресурса. Если объем переданных данных hello равно tooor больше, чем размер файла hello и запросы байтового диапазона hello подходят для этого ресурса, то попадание hello будет считаться завершения загрузки.
> 
> Из-за характера interpretive toohello этот отчет следует иметь в виду hello после точки, которые могут изменяться согласованности hello и точность этого отчета.
> 
> * Из-за разного поведения пользовательских клиентов невозможно точно предсказать шаблоны потока трафика. В результате для завершенной загрузки можно получить значение, которое превышает 100 %.
> * Если ресурс загружался через HTTP, этот отчет не сможет точно отобразить состояние загрузки. Это происходит из-за toousers поиска toodifferent позиций в видео.
> 
> 

## <a name="by-404-errors"></a>Сводка по ошибкам 404
Hello, ошибки 404 отчетов позволяет tooidentify hello типа содержимого, которое приводит к возникновению ошибки hello большинство числовых кодов 404 не найдено состояние. Hello верхней части отчета hello содержит линейчатую диаграмму для hello десяти основных средств, для которых был возвращен код состояния 404 не найдено. Этот график сравнивает hello общее количество запросов с запросами, которые привели к 404 не найдено код состояния для этих средств. Каждый отрезок имеет свой цвет. Желтая полоска используется tooindicate, hello запрос завершился код состояния 404 не найдено. Красная черта используется tooindicate hello общее число запросов для средства hello.

> [!NOTE]
> Hello действий, описанных в этом отчете Обратите внимание на следующие hello:
> 
> * Переход означает любой запрос ресурса, независимо от кода состояния.
> * URL-адреса CNAME Edge, преобразованный tootheir соответствующие CDN URL-адреса. Это позволяет точных результатов голосования для всей статистики, связанную с актива независимо от hello CDN или край CNAME URL-адрес используется toorequest его.
> 
> 

Hello левой части диаграммы hello (ось y) указывает имя файла hello для каждого hello top 10 запрошенных ресурсов, приводящие к код состояния 404 не найдено. Прямо под hello graph (ось x) вы обнаружите метки, обозначающие hello общее число запросов и hello число запросов, которые привели к код состояния 404 не найдено.

Непосредственно под hello линейчатую диаграмму, hello следующие сведения будут указаны для верхней hello 250 запрошенных ресурсов: был hello число запросов, которые привели к 404 не найдено код состояния, общее число попыток hello активов hello относительный путь (включая имя файла) Запрошенный и hello процент запросов, которые привели к код состояния 404 не найдено.

## <a name="see-also"></a>См. также
* [Обзор Azure CDN](cdn-overview.md)
* [Статистика сети CDN Microsoft Azure в режиме реального времени](cdn-real-time-stats.md)
* [Переопределение поведения по умолчанию HTTP с помощью модуля правил hello](cdn-rules-engine.md)
* [Анализ производительности Edge](cdn-edge-performance.md)

