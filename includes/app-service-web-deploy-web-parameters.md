С помощью диспетчера ресурсов Azure можно определить параметры для значения требуется toospecify при развертывании шаблона hello. шаблон Hello имеется раздел с именем параметров, содержащий все значения параметров hello.
Следует определить параметр для те значения, которые будут различаться на основе hello проекта, в которой выполняется развертывание или на основании hello среды, в которой выполняется развертывание. Определяет параметры для значения, которые всегда останутся hello таким же. Каждое значение параметра используется в hello шаблона toodefine hello ресурсы, которые развертываются. 

При определении параметров следует использовать hello **allowedValues** toospecify поля, в которой значения пользователь может предоставить во время развертывания. Используйте hello **defaultValue** tooassign поля toohello значение параметра, если значение не указано во время развертывания.

Мы опишем каждого параметра в шаблоне hello.

### <a name="sitename"></a>siteName
Имя веб-приложения hello, что вы хотите toocreate Hello.

    "siteName":{
      "type":"string"
    }

### <a name="hostingplanname"></a>hostingPlanName
Имя Hello hello службы приложений плана toouse для размещения веб-приложения hello.

    "hostingPlanName":{
      "type":"string"
    }

### <a name="sku"></a>sku
Здравствуйте, ценовую категорию для плана размещения hello.

    "sku": {
      "type": "string",
      "allowedValues": [
        "F1",
        "D1",
        "B1",
        "B2",
        "B3",
        "S1",
        "S2",
        "S3",
        "P1",
        "P2",
        "P3",
        "P4"
      ],
      "defaultValue": "S1",
      "metadata": {
        "description": "hello pricing tier for hello hosting plan."
      }
    }

шаблон Hello определяет hello значения, разрешенные для этого параметра и назначает значение по умолчанию (S1), если значение не указано.

### <a name="workersize"></a>workerSize
размер экземпляра плана (небольшие, средние и большие) размещения hello Hello.

    "workerSize":{
      "type":"string",
      "allowedValues":[
        "0",
        "1",
        "2"
      ],
      "defaultValue":"0"
    }

шаблон Hello определяет hello значения, разрешенные для этого параметра (0, 1 или 2) и назначает значение по умолчанию (0), если значение не указано. Hello значения соответствуют toosmall, средний и большой.

