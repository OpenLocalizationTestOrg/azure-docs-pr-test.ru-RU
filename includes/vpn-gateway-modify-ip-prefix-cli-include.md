### <a name="noconnection"></a>toomodify локальной сети шлюза префиксов IP-адресов — подключение к шлюзу

Если вы не имеете подключения шлюза, при необходимости tooadd или удалить префиксов IP-адресов, используется hello же команды, которая используется шлюз локальной сети toocreate hello, [Создание локальной сети az-шлюз](https://docs.microsoft.com/cli/azure/network/local-gateway#create). Также можно использовать этот IP-адрес шлюза hello команда tooupdate для hello VPN-устройства. текущие параметры toooverwrite hello, используйте имя существующего hello шлюза локальной сети. Если используется другое имя, можно создать новый шлюз локальной сети, вместо переопределения Здравствуйте существующий.

Должно быть указано при каждом внесении изменений, hello весь список префиксов, не только hello префиксов, которые должны toochange. Укажите только префиксы hello, что требуется tookeep. В этом случае это 10.0.0.0/24 и 20.0.0.0/24.

```azurecli
az network local-gateway create --gateway-ip-address 23.99.221.164 --name Site2 --connection-name TestRG1 --local-address-prefixes 10.0.0.0/24 20.0.0.0/24
```

### <a name="withconnection"></a>toomodify локальной сети шлюза префиксов IP-адресов — существующие подключения шлюза

Подключение шлюза и tooadd или удалите префиксов IP-адресов, можно обновить с помощью префиксов hello [обновления шлюза локальной сети az](https://docs.microsoft.com/cli/azure/network/local-gateway#update). После этого VPN-подключение будет некоторое время недоступно. Когда изменение IP-адрес hello префиксов, не требуется toodelete hello VPN-шлюз.

Должно быть указано при каждом внесении изменений, hello весь список префиксов, не только hello префиксов, которые должны toochange. В этом примере 10.0.0.0/24 и 20.0.0.0/24 уже существуют. Мы Добавьте префиксы 30.0.0.0/24 hello и 40.0.0.0/24 и задайте все 4 hello префиксы при обновлении.

```azurecli
az network local-gateway update --local-address-prefixes 10.0.0.0/24 20.0.0.0/24 30.0.0.0/24 40.0.0.0/24 --name VNet1toSite2 --connection-name TestRG1
```